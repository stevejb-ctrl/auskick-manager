---
gsd_state_version: 1.0
milestone: v1.1
milestone_name: Match Day Changes
status: milestone_complete
stopped_at: Phase 13 ✓ COMPLETE (AUDIO-01 / B3 — iOS hype song survives backgrounding) — `/gsd-execute-phase 13` driven INLINE on main (gsd-sdk unavailable). ONE plan, ONE wave (`13-01-PLAN.md`) executed red-first: Task 1 RED `hypeSongController.test.ts` (committed `bf6e2e8`, failed against absent controller), Task 2 GREEN pure `reduceSongArm` controller + `useHypeSong` rewire (committed `dc9ca93`). The bug was a single-file fix in the ALREADY-SHARED `src/lib/live/useHypeSong.ts` (consumed verbatim by AFL `LiveGame.tsx:466/778` + netball `NetballLiveGame.tsx:288/1285/2445`; rugby league has NO hype song → no fork). Root cause: the audio session was never re-armed after iOS suspends it at a period transition / backgrounding (no `visibilitychange` handler; teardown only on gameId/songUrl/hydrated change) and the suspended-context rejection was swallowed (`:136` `.catch(()=>{})` + bare outer `catch {}` `:142`). FIX (D-01..D-07): a PURE `reduceSongArm(state,event)` controller (NEW `src/lib/live/hypeSongController.ts`; states idle/ready/suspended; events ready/play/hidden/visible/playSucceeded/playFailed; actions none/play/rearm-then-play/rearm) — node-unit-testable (Vitest is node-only, NO jsdom/renderHook); the hook is now a thin adapter wiring a `document` visibilitychange listener + the controller's actions to BOTH backends (YT wake via seekTo+playVideo; null `songAudioRef` so Audio is recreated fresh), mirroring the beep `_audioCtx` resume at `LiveGame.tsx:144`; the play rejection is surfaced (dispatch `playFailed` + non-prod `console.warn`, both swallow sites removed). Re-arm, NOT rework — YT-iframe/`new Audio()` mechanism, 1×1 sizing, auto-pause timer, `songUrl.ts` unchanged; hook public `{ containerRef, playSong }` unchanged so AFL+netball call sites untouched. The post-Q1 fix is `reduceSongArm("suspended","play")→rearm-then-play`. NO new e2e (iOS suspension not simulable in Playwright; song has no assertable DOM output — D-07); the pure-controller unit test is the regression gate, existing AFL+netball live specs are the no-regression guard. NO migration / GameEventType / store slice / server action. 4 DoD gates green (tsc 0, lint 0 errors, Vitest 889/889 +14, e2e live specs 16 pass/1 pre-existing skip). 13-01-SUMMARY.md + 13-VERIFICATION.md (PASS, all 4 ROADMAP criteria with file:line evidence, AUDIO-01) written. **This was the FINAL requirement in v1.1 — Match Day Changes is now 6/6 phases COMPLETE.** v1.0 Phase 6 still paused on user creds.
last_updated: "2026-06-02T18:30:00Z"
last_activity: 2026-06-02
progress:
  total_phases: 6
  completed_phases: 6
  total_plans: 13
  completed_plans: 13
  percent: 100
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-06-01)

**Core value:** Live-game time-on-ground fairness must be effortless and trustworthy across AFL, netball, and rugby league — coaches end every match confident every kid got their fair share.
**Current focus:** Milestone v1.1 — Match Day Changes (4 bugs + 4 features, all sport-agnostic). Spec: `.planning/MATCH-DAY-CHANGES-SPEC.md`.

## Current Position

Phase: Phase 13 — Hype song survives iOS backgrounding (AUDIO-01 / B3) — ✓ COMPLETE 2026-06-02 (1 wave executed red-first + verified PASS). This was the FINAL phase of milestone v1.1 — **Match Day Changes is now 6/6 phases COMPLETE.**
Plan: 1 plan, 1 wave (`13-01-PLAN.md`) executed: Task 1 RED `hypeSongController.test.ts` (`bf6e2e8`), Task 2 GREEN `reduceSongArm` controller + `useHypeSong` rewire (`dc9ca93`). Single-file fix in the shared `useHypeSong.ts` hook (AFL + netball benefit for free, call sites untouched; RL has no song → no fork). 13-01-SUMMARY.md + 13-VERIFICATION.md (PASS) written. v1.0 Phase 6 still paused on user creds.
Status (Phase 13 execution): `/gsd-execute-phase 13` driven INLINE on main (gsd-sdk unavailable). **Root cause (was confirmed in code, now fixed):** `useHypeSong.ts` never re-armed the audio element/context after iOS suspends the session at a period transition / backgrounding — no `visibilitychange` handler, teardown only on gameId/songUrl/hydrated change — and the suspended-context rejection was swallowed. **Fix shipped (D-01..D-07):** D-01 ONE wave/plan, fixed the shared hook (no per-sport fork — RL has no song); D-02 PURE `reduceSongArm(state,event)` controller in `src/lib/live/hypeSongController.ts` (states idle/ready/suspended; events ready/play/hidden/visible/playSucceeded/playFailed; actions none/play/rearm-then-play/rearm), node-unit-testable; D-03 re-arm via a `document` visibilitychange listener in the hook (registered + cleaned up, `useHypeSong.ts:144-156`), mirroring the beep `_audioCtx` resume at `LiveGame.tsx:144`, re-arming BOTH backends (YT wake via seekTo+playVideo `:176-177`; null `songAudioRef.current` so Audio is recreated fresh `:186-187`); D-04 surface the rejection — dispatch `playFailed` + non-prod `console.warn`, both swallow sites removed (`:190-216`); D-05 re-arm NOT rework — YT-iframe/`new Audio()` mechanism, 1×1 sizing, auto-pause timer, `songUrl.ts` all unchanged; the hook's public `{ containerRef, playSong }` unchanged (`:219`) so AFL+netball call sites untouched; D-06 RED-first `hypeSongController.test.ts` (14 tests) reproduces post-Q1 silence (`("suspended","play")→rearm-then-play`) + full suspend→re-arm→play cycle + purity; D-07 NO new e2e (iOS audio-session suspension not faithfully reproducible in Playwright/Chromium + song has no assertable DOM output) — the pure-controller node test is the regression gate, existing AFL+netball live specs are the no-regression guard. 4 DoD gates green: tsc 0, lint 0 errors, Vitest 889/889 (+14), e2e live specs (`live-scoring` + `netball-live-flow`) 16 pass/1 pre-existing skip `--workers=1`. NO migration / GameEventType / store slice / server action — client-only audio wiring.
Status: `/gsd-execute-phase 12` driven INLINE on main (gsd-sdk query unavailable; worktree-rooted agents → false negatives + wrong-dir writes). **Cross-sport host split (the pivotal design fact, confirmed in code):** AFL (`LiveGame.tsx`) + rugby league (`LeagueLiveGame.tsx`) both use the SHARED `LockModal`; netball (`NetballLiveGame.tsx`) uses a FORKED `NetballPlayerActions`. F3's reuse boundary is the **summary content** — a shared pure `buildPlayerInsight` builder (`src/lib/player-insight.ts:82`) + a shared `PlayerInsightSummary` component (`src/components/live/PlayerInsightSummary.tsx`, testids player-insight-ingame/-periods/-season, periods section gated on `vm.perPeriod.length > 0`) — embedded in each sport's EXISTING host via an optional `insight?: ReactNode` slot (`LockModal.tsx:93/147`; `NetballPlayerActions.tsx:64/129`). NO new modal, no fork of the summary. **The one MISSING datum** = per-period × per-zone minutes (replays only stored the ENDING zone) → derived additively as `playedZoneMsByPeriod` at each engine's EXISTING per-zone credit site: AFL `GameState.playedZoneMsByPeriod` in `addPlayed` (`fairness.ts`); exported netball `playedZoneMsByPeriod(events, periodSeconds, thirdLookup, inProgress?)` keyed by 3 third zone ids (credits closed quarters at `period_break_swap`/`game_finalised`, NOT quarter_end); exported league `playedZoneMsByPeriod(events)` single `"field"` zone per half — all red-first cross-checked to sum to the existing per-zone outputs (`playedZoneMsByPeriod.test.ts` / `.netball.test.ts` / `.league.test.ts`). Each host's `buildInsightInput` overlays the open period live (AFL `LiveGame.tsx:2087/2112`; netball `NetballLiveGame.tsx:1156/1201`, label `Q1`; league `LeagueLiveGame.tsx:441/493`, label `H1`). Time-since-last-sub reuses Phase-10 `lastSubbedOnMs` (`msSinceLastSub = max(0, nowAbsMs - lastSubbedOnMs)`, null when never subbed, `player-insight.ts:109-111`). Season = PERCENTAGES ONLY (`seasonZonePct` `pct` field only, `player-insight.ts:114-121`; no raw season ms ever surfaces). Zones from config (AFL `getSportConfig("afl").zones`∩ageGroup.zones `LiveGame.tsx:291`; netball via `primaryThirdFor` over positions `NetballLiveGame.tsx:1120`; RL `rugbyLeagueSport.zones`∩ageGroup.zones `LeagueLiveGame.tsx:431` — single field, honest degeneracy). NO migration / GameEventType / store slice / server action — read-only over already-loaded replay state + the already-fetched team-scoped `season` prop. 6 commits across 2 waves (12-01: 704c2ef/10bca9e/593d45d; 12-02: 26618a6/ee58f81/77d9f88). 12-01-SUMMARY.md + 12-02-SUMMARY.md + 12-VERIFICATION.md (PASS, all 4 success criteria with file:line evidence) written.
Last activity: 2026-06-02 — Phase 13 ✓ COMPLETE: `/gsd-execute-phase 13` driven INLINE. Red-first `reduceSongArm` controller (`bf6e2e8`) + `useHypeSong` re-arm rewire (`dc9ca93`); 13-01-SUMMARY.md + 13-VERIFICATION.md (PASS, all 4 ROADMAP criteria with file:line evidence, AUDIO-01) written; 4 DoD gates green. **v1.1 Match Day Changes is now 6/6 phases COMPLETE.**

Progress: [██████████] 100% (6/6 v1.1 phases complete; Phases 8–13 ✓ done — milestone v1.1 COMPLETE). v1.0 multi-sport merge still paused at Phase 6 on prod creds (separate milestone, unaffected).

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- v1.1 init: Match Day Changes is a NEW milestone; v1.0 (multi-sport merge) is paused, not abandoned. Phase numbering CONTINUES from v1.0 (next phase = 8); v1.0's phase dirs are preserved untouched.
- v1.1 init: Everything sport-agnostic by rule — never hardcode "quarter"; read `periodCount`/`periodSeconds`/`periodLabel` from age-group config; zones/positions come from `getAgeGroupConfig(sport, ageGroup).zones`.
- v1.1 init: F4 sub-interval floor lives PER AGE GROUP as `subIntervalFloorSeconds` with a ~240s (4-min) default.
- v1.1 init: B1 repro per user is "in the picker screen" — recon found no availability toggle on LineupPicker.tsx; reconcile during the B1 phase discussion (recon may have missed a control, or the user's mental model maps a different control to "the picker").
- v1.1 init: Research skipped (config default) — brownfield work on own codebase; read-only recon already mapped each item to its root cause.
- v1.1 roadmap: 11 v1.1 requirements mapped to 6 phases (8–13). CONFIG-01/02 = foundation (Phase 8); AVAIL-01/02 together (Phase 9); SUB-02/SUB-01 together as substitution timing (Phase 10); ROTPLAN-01/02 share one upcoming-rotation surface (Phase 11); PLAYERVIEW-01/02 one long-press summary (Phase 12); AUDIO-01 independent (Phase 13, can run any time). B4 recency signal is shared between Phase 10 and Phase 12's F3 last-sub derivation.

### Pending Todos

- ~~**WR-01:** `fairness.ts` compared `fullPeriodMs` (ms) against a season total in MINUTES → season-diversity nudge always-on.~~ RESOLVED in Phase 10 / 10-01 (commit `76b2050`): `fullPeriodMins = fullPeriodMs / 60000` then `seasonMins < fullPeriodMins`, with red-first `seasonDiversityUnits.test.ts`. Closed per 10-VERIFICATION.md.

### Blockers/Concerns

- **v1.0 COMPLETE — Phases 6 & 7 SUPERSEDED by continuous delivery (2026-06-02):** `/gsd-execute-phase 6` resumed, refresh authored (commit `79010a1`, `06-DEPLOY-REFRESH.md`), then the owner confirmed **production (`sirenfooty.com.au`) is already live on current `main` + current 47-migration schema**, auto-deployed from `main`. That makes the clone-and-rehearse runbook obsolete (it would rehearse a cutover already in prod). Closed Phases 6 (DEPLOY-01/02) + 7 (DEPLOY-03/04) as superseded — see `.planning/phases/06-preview-deploy-and-manual-validation/06-CLOSURE.md`. DEPLOY-01/03 met by the merged trunk being live in prod; DEPLOY-02/04 met by green e2e (16 live specs across AFL+netball+share tokens) + 889 unit tests + real-user production usage (owner chose "close on tests + live usage", no manual walkthrough). Plans 06-01/02/03 (deploy kit + `verify-prod-clone.mjs`, Q1 patched 27→47) authored + retained as a reference deploy kit; 06-04/05 deliberately NOT executed. **No prod action outstanding; v1.0 multi-sport merge is fully delivered.**
- ~~v1.1 B1: availability-control discrepancy to reconcile during the B1 phase (Phase 9).~~ RESOLVED in Phase 9 (09-CONTEXT.md §B1 D-01: "the picker" = the dedicated AvailabilityList/AvailabilityRow surface, NOT the zones LineupPicker; no new control added; the draft→picker→lineup_set path is reconciled server-side).

## Deferred Items

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Deploy | v1.0 preview deploy + prod cutover (Phases 6–7) | ✓ Resolved — superseded by continuous delivery; prod already live on current main (06-CLOSURE.md) | 2026-05-01 → closed 2026-06-02 |

## Session Continuity

Last session: 2026-06-02
Stopped at: Phase 13 ✓ COMPLETE (AUDIO-01 / B3 — iOS hype song survives backgrounding) — **the LAST phase of milestone v1.1, which is now 6/6 COMPLETE.** `/gsd-execute-phase 13` driven INLINE on main (gsd-sdk query unavailable). ONE plan, ONE wave (`13-01-PLAN.md`) executed red-first: Task 1 RED `hypeSongController.test.ts` (`bf6e2e8`, failed against absent controller — reproduced post-Q1 silence), Task 2 GREEN pure `reduceSongArm` controller + `useHypeSong` rewire (`dc9ca93`). The bug was a single-file fix in the ALREADY-SHARED `src/lib/live/useHypeSong.ts` (AFL `LiveGame.tsx:466/778` + netball `NetballLiveGame.tsx:288/1285/2445` consume it verbatim, untouched; rugby league has NO hype song → no fork/mirror). Fix shipped = pure `reduceSongArm` controller (`src/lib/live/hypeSongController.ts`) + hook rewire (a `document` visibilitychange listener `:144-156` re-arms both YT (seekTo+playVideo `:176-177`) + Audio (null `songAudioRef` `:186-187`) backends; the rejection is surfaced via `playFailed` + non-prod `console.warn` `:190-216`, both swallow sites removed), mirroring the beep `_audioCtx` resume at `LiveGame.tsx:144`; re-arm NOT rework (mechanism + 1×1 + auto-pause + `songUrl.ts` + public API unchanged). The post-Q1 fix = `reduceSongArm("suspended","play")→rearm-then-play`. NO new e2e (iOS suspension not simulable; D-07) — controller unit test is the regression gate, AFL+netball live specs the no-regression guard. NO migration/GameEventType/store slice/server action. 4 DoD gates green (tsc 0, lint 0 errors, Vitest 889/889 +14, e2e 16 pass/1 pre-existing skip). 13-01-SUMMARY.md + 13-VERIFICATION.md (PASS, all 4 ROADMAP criteria + AUDIO-01) written. NEXT (UPDATED 2026-06-02): v1.1 fully delivered AND v1.0 now closed too — Phases 6 & 7 SUPERSEDED by continuous delivery (prod already live on current `main`; `06-CLOSURE.md`), so the deploy is NOT resumed. Both milestones complete — start a new milestone with `/gsd-new-milestone`.
- **13-CONTEXT.md:** decisions D-01..D-07 folded. D-01 ONE wave/plan, fix the shared hook (RL has no song → no fork); D-02 extract PURE `reduceSongArm(state,event)` into `src/lib/live/hypeSongController.ts` (states idle/ready/suspended; events ready/play/hidden/visible/playSucceeded/playFailed; actions none/play/rearm-then-play/rearm), node-testable (Vitest is node-only, NO jsdom/renderHook); D-03 re-arm via `document` visibilitychange listener in the hook (registered+cleaned up), re-arming BOTH backends (YT seekTo+playVideo wake; null `songAudioRef.current` so Audio recreated fresh), mirroring beep `_audioCtx` resume `LiveGame.tsx:144`; D-04 surface the rejection (dispatch `playFailed` + non-prod `console.warn`; remove `.catch(()=>{})` `:136` + bare `catch {}` `:142`); D-05 re-arm NOT rework — YT-iframe/`new Audio()`/1×1/auto-pause/`songUrl.ts` unchanged, hook public API `{ containerRef, playSong }` unchanged; D-06 RED-first `hypeSongController.test.ts` (`("suspended","play")→rearm-then-play` reproduces post-Q1 silence + full suspend→re-arm cycle + playFailed→suspended + purity); D-07 NO new e2e (iOS audio suspension not reproducible in Playwright + song has no assertable DOM output) — node controller test is the regression gate, existing AFL+netball live specs the no-regression guard.
- **13-01 (single wave, AUDIO-01 / B3):** files_modified = `src/lib/live/hypeSongController.ts` + `src/lib/__tests__/hypeSongController.test.ts` + `src/lib/live/useHypeSong.ts`. 2 tasks: (1) RED `hypeSongController.test.ts` driving `reduceSongArm` — post-Q1 fix (`("suspended","play").action === "rearm-then-play"`), suspend on hidden, eager rearm on visible, playFailed→suspended chain, playSucceeded→ready, full cycle, purity; (2) GREEN implement `reduceSongArm` (pure switch, no react/DOM/Supabase) + rewire `useHypeSong.ts` (armStateRef + visibilitychange effect dispatching hidden/visible; playSong dispatches play → rearm-then-play re-arms both backends first; `.then(playSucceeded).catch(playFailed + warn)`, remove the two swallows; YT path dispatches playSucceeded + relies on visibilitychange for suspension) + 4 DoD gates (no new e2e; existing AFL+netball live specs stay green). Threat model T-13-01-A..D (DoS/autoplay-loop mitigate — rearm-on-visible never auto-plays; integrity mitigate — pure reducer red-first; info-disclosure accept — non-prod warn only; elevation accept — client-only, no server action/migration/event/store).
- **12-CONTEXT.md:** decisions D-01..D-08 folded. D-01 reuse the existing long-press host + add an optional `insight?: React.ReactNode` slot (LockModal for AFL+RL, NetballPlayerActions for netball) — NO new modal; D-02 one shared pure `buildPlayerInsight` (`src/lib/player-insight.ts`, no Supabase/React) + one shared `PlayerInsightSummary` (`src/components/live/PlayerInsightSummary.tsx`), consumed verbatim by all 3 sports; D-03 zones from `getAgeGroupConfig(...).zones` ∩ `SportConfig.zones` ZoneDef[], in config order, never hardcoded ALL_ZONES; D-04 season PERCENTAGES ONLY (no raw season ms field; all-zero when no season data); D-05 per-period × per-zone derived from the event replay via an additive `playedZoneMsByPeriod` accumulator at each engine's existing per-zone credit site, current period overlaid live in the component (same stint-overlay pattern as `zoneMsByPlayer`); D-06 time-since-last-sub reuses Phase-10 `lastSubbedOnMs` (`max(0, nowAbsMs - lastSubbedOnMs)`, null when never subbed); D-07 two waves serial (12-01 core+AFL; 12-02 netball+league mirror, depends_on [01], both touch the shared LockModal/PlayerInsightSummary seam); D-08 NO migration / GameEventType / store slice / server action — read-only over loaded replay state + the already-fetched team-scoped `season` prop.
- **12-01 (Wave 1, ROTPLAN n/a — PLAYERVIEW-01/02 / F3 shared core + AFL reference):** files_modified = `src/lib/player-insight.ts` + `src/lib/__tests__/playerInsight.test.ts` + `src/lib/fairness.ts` + `src/lib/__tests__/playedZoneMsByPeriod.test.ts` + `src/components/live/PlayerInsightSummary.tsx` + `src/components/live/LockModal.tsx` + `src/components/live/LiveGame.tsx` + `e2e/tests/player-insight.spec.ts`. 3 tasks: (1) RED `playerInsight.test.ts` (season %-only/no-ms, zones-from-config 3-zone+5-zone, msSinceLastSub incl. null+clamp, per-period rows, purity) + `playedZoneMsByPeriod.test.ts` (AFL replay per-period×per-zone, cross-check sum to `basePlayedZoneMs`); (2) GREEN implement `buildPlayerInsight` (pure) + AFL `GameState.playedZoneMsByPeriod` accumulated in `addPlayed` (existing outputs unchanged) + `PlayerInsightSummary` (testids `player-insight-ingame`/`-periods`/`-season`) + optional `insight?` on `LockModalProps`; (3) GREEN wire AFL LiveGame (config zones, `zoneMsByPlayer` in-game, perPeriod from replay + current overlay `displayNowMs - stintStartMs[pid]`, season→pct, `nowAbsMs = completedQuarterMs + displayNowMs*clockMultiplier`) into the EXISTING LockModal via `insight={<PlayerInsightSummary .../>}` + AFL e2e + 4 DoD gates. Builder interface in `<interfaces>`: input {zones ZoneDef[], inGameZoneMs, perPeriod[], seasonZoneMs, lastSubbedOnMs, nowAbsMs} → output {inGameZones[], inGameTotalMs, perPeriod[], msSinceLastSub, seasonZonePct[] with pct ONLY}. Threat model T-12-01-A..D (info-disclosure accept, integrity mitigate, elevation accept read-only, DoS mitigate).
- **12-02 (Wave 2, depends_on [01], netball + rugby-league mirror):** files_modified = `src/lib/sports/netball/fairness.ts` + `src/lib/sports/rugby_league/fairness.ts` + `playedZoneMsByPeriod.netball.test.ts` + `playedZoneMsByPeriod.league.test.ts` + `src/components/netball/NetballPlayerActions.tsx` + `src/components/netball/NetballLiveGame.tsx` + `src/components/league/LeagueLiveGame.tsx` + `e2e/tests/player-insight.spec.ts`. 3 tasks: (1) RED netball per-third + league per-field per-period replay specs (cross-check to `playerThirdMs` / `playerMsOnField`; league period count from `ageGroup.periodCount`, no literal); (2) GREEN extend `replayNetballGame`/`replayLeagueGame` with `playedZoneMsByPeriod` keyed by config zone id + add `insight?` slot to NetballPlayerActions + wire NetballLiveGame (config thirds, `playerThirdMs` in-game, season via `seasonPositionCounts`→thirds %) and LeagueLiveGame (single "field" config zone, `playerMsOnField`, existing LockModal slot) both reusing the SHARED `buildPlayerInsight`/`PlayerInsightSummary` verbatim; (3) GREEN netball + league e2e cases + 4 DoD gates. DEV note: RL single-"field" zone degeneracy is honest/in-config (forwards/backs are vests, re-modelling out of scope); meaningful RL signals = time-since-last-sub + per-period field minutes. Threat model T-12-02-A..D (incl. T-12-02-D cross-sport drift mitigated by reusing the shared core).
Both plans red-first (unit + e2e per task); threat models = no new auth/network/migration (read-only derivation + presentational reuse).
- **Phase 11 (prior, ✓ COMPLETE 2026-06-02):** ROTPLAN-01/F1 + ROTPLAN-02/F2 — F1+F2 share ONE projector-seeded surface = the extended `GamePlanModal` (no fork); `projectUpcomingRotation`/`resolveHonouredSwaps`/`diffPlanToSwaps` (`src/lib/game-plan/live.ts`) + `plannedRotation` store slice (partialize, gameId-keyed, NO migration/event) + pure `seedNextPeriodLineup` + each sport's break pre-seeded. 7 commits (11-01: ba5bcf1→0b8d9a1→050aeea→22eb1d9; 11-02: 26f42c5→f72973c→a138270). 11-01/11-02-SUMMARY.md + 11-VERIFICATION.md (PASS) written. Phase 10's `lastSubbedOnMs` recency signal (on all 3 replays) is what Phase 12's F3 last-sub derivation reuses.
**TOOLING NOTE (unchanged):** Glob/Grep/Read-without-abs-path default to a STALE/orphaned git worktree (`.claude/worktrees/exciting-hopper-7e19a5`, not in `git worktree list`); real work + commits live in the main checkout (`C:\Users\steve\OneDrive\Documents\Auskick manager`). Use Bash (cd into main checkout) or absolute paths for all file inspection; drive sub-agent-style steps INLINE.
Next: **BOTH milestones fully delivered AND shipped to production.** v1.1 Match Day Changes = 6/6 (phases 8–13). v1.0 multi-sport merge = COMPLETE — Phases 1–5 done; Phases 6 & 7 SUPERSEDED by continuous delivery 2026-06-02 (`06-CLOSURE.md`). **PUSHED 2026-06-02:** `git push origin main` fast-forwarded `origin/main` `0f1800c..fd75c56` (60 commits = all of v1.1 phases 8–13 feature code+tests + the v1.0 closure docs); pre-push hook green (lint 0 err/4 known warns, vitest 889/889); migration-free (zero `supabase/migrations/` changes in the range) so it's a pure-code deploy with no prod DB action. Vercel auto-deploys `main` → v1.1 now rolling out to production (`sirenfooty.com.au`). No prod action outstanding. Next option: start the next milestone with `/gsd-new-milestone`. The retained deploy kit (`06-DEPLOY-CHECKLIST.md`/`-RUNBOOK.md`/`-REFRESH.md` + `scripts/verify-prod-clone.mjs`) is available if a future high-risk migration ever warrants a real clone-and-rehearse.
Resume file: none — both milestones complete. v1.0 deploy closure recorded in `06-CLOSURE.md`; deploy kit retained as reference only.
