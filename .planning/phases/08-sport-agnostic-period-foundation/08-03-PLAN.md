---
phase: 08-sport-agnostic-period-foundation
plan: 03
type: execute
wave: 2
depends_on: [1]
files_modified:
  - src/components/live/LiveGame.tsx
  - src/components/netball/NetballLiveGame.tsx
  - src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx
requirements: [CONFIG-01]
autonomous: true

must_haves:
  truths:
    - "D-01: LiveGame.tsx takes the full ageGroup: AgeGroupConfig object as a new prop (matching NetballLiveGame/LeagueLiveGame) and reads periodCount off it"
    - "D-01a: live/page.tsx threads the already-resolved ageCfgSport into the <LiveGame> mount (beside quarterMs)"
    - "D-01b: LiveGame's existing scalar props (quarterMs, subIntervalSeconds, positionModel, defaultOnFieldSize) are KEPT as-is — not ripped out (collapsing is deferred)"
    - "D-07: LiveGame.tsx and NetballLiveGame.tsx derive isAtFullTime / isBetweenQuarters via the shared periodPhase() helper; no `currentQuarter >= 4` / `< 4` literal survives in either component"
    - "CONFIG-01: the two live/page.tsx sticky-bar `currentQuarter < 4` literals (netball :496, AFL :659) read periodCount (ageCfgN / ageCfgSport)"
    - "D-11: existing AFL/netball e2e specs stay green; tsc/lint/test green"
  artifacts:
    - path: "src/components/live/LiveGame.tsx"
      provides: "AFL live orchestrator consuming ageGroup.periodCount via periodPhase() — no hardcoded 4"
      contains: "periodPhase("
    - path: "src/components/netball/NetballLiveGame.tsx"
      provides: "Netball live component with the 3 render gates driven by periodCount (helper or ageGroup.periodCount), no hardcoded 4"
      contains: "ageGroup.periodCount"
    - path: "src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx"
      provides: "ageCfgSport threaded into <LiveGame>; both sticky-bar booleans read periodCount"
      contains: "ageGroup={ageCfgSport}"
  key_links:
    - from: "src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx"
      to: "src/components/live/LiveGame.tsx (ageGroup prop)"
      via: "ageGroup={ageCfgSport} at the LiveGame mount"
      pattern: "ageGroup=\\{ageCfgSport\\}"
    - from: "src/components/live/LiveGame.tsx"
      to: "src/lib/live/periodPhase.ts"
      via: "import + call with ageGroup.periodCount"
      pattern: "periodPhase\\(currentQuarter,\\s*ageGroup.periodCount"
---

<objective>
Replace the hardcoded `currentQuarter >= 4` / `< 4` period-count literals in the
two AFL/netball live surfaces and the shared sticky bars with values derived from
`ageGroup.periodCount`, using the `periodPhase()` helper from plan 01 (D-07).
Thread the full `ageGroup: AgeGroupConfig` object into AFL `LiveGame.tsx` (D-01)
so it has `periodCount` available — matching netball and league, which already
take the config object.

Purpose: After this plan, no live surface hardcodes the number 4 — period
structure comes entirely from age-group config (CONFIG-01, milestone
sport-agnostic rule). `LeagueLiveGame.tsx` stays the reference; AFL/netball now
mirror it.

Scope guard (D-01b): KEEP LiveGame's existing scalar props (`quarterMs`,
`subIntervalSeconds`, `positionModel`, `defaultOnFieldSize`) exactly as they are.
Do NOT collapse the redundancy — that cleanup is explicitly deferred.

Output: 3 files modified; all period literals replaced; AFL/netball e2e green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-sport-agnostic-period-foundation/08-CONTEXT.md
@.planning/phases/08-sport-agnostic-period-foundation/08-PATTERNS.md
@CLAUDE.md
@.planning/phases/08-sport-agnostic-period-foundation/08-01-SUMMARY.md

<interfaces>
<!-- The helper from plan 01 (already merged when this plan runs). -->
src/lib/live/periodPhase.ts:
```typescript
export function periodPhase(
  currentPeriod: number,
  periodCount: number,
  periodEnded: boolean,
  finalised: boolean,
): { isAtFullTime: boolean; isBetweenPeriods: boolean; isLastPeriod: boolean };
```

<!-- Prop pattern to copy from netball (it ALREADY takes ageGroup). -->
NetballLiveGame.tsx — prop + import (mirror in LiveGame):
```typescript
import type { AgeGroupConfig } from "@/lib/sports/types";
// in props interface:  ageGroup: AgeGroupConfig;
```

<!-- LiveGame current booleans (LiveGame.tsx:1019-1021) — REPLACE: -->
const isAtFullTime = !finalised && currentQuarter >= 4 && quarterEnded;
const isFinished = finalised || isAtFullTime;
const isBetweenQuarters = quarterEnded && currentQuarter >= 1 && currentQuarter < 4;

<!-- NetballLiveGame render gates — REPLACE the `4` with ageGroup.periodCount: -->
// :1659  if (quarterEnded && currentQuarter >= 4)   → >= ageGroup.periodCount
// :1696  if (quarterEnded && currentQuarter < 4)    → <  ageGroup.periodCount
// :1780  if (quarterEnded && currentQuarter >= 4)   → >= ageGroup.periodCount

<!-- live/page.tsx wiring facts (verified): -->
// :603  const ageCfgSport = getAgeGroupConfig("afl", ageGroup);  (already resolved)
// :693  <LiveGame ...  :718 quarterMs={quarterMs}  → add ageGroup={ageCfgSport}
// :493-496 netball sticky bar: state.currentQuarter < 4  → ageCfgN.periodCount
// :656-659 AFL sticky bar:     state.currentQuarter < 4  → ageCfgSport.periodCount
// LiveGame props interface :146-225 (quarterMs at :214); destructure block :235-263 (quarterMs at :261)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread ageGroup into LiveGame and drive its booleans off periodPhase()</name>
  <read_first>
    - src/components/live/LiveGame.tsx (props interface lines 146-225; destructure 235-263; booleans 1019-1021)
    - src/components/netball/NetballLiveGame.tsx (the ageGroup prop + AgeGroupConfig import it already has) — the exact pattern to copy
    - src/lib/live/periodPhase.ts — the helper signature
    - .planning/phases/08-sport-agnostic-period-foundation/08-CONTEXT.md (D-01, D-01b, D-07)
  </read_first>
  <behavior>
    - LiveGame accepts ageGroup: AgeGroupConfig as a prop and reads ageGroup.periodCount
    - isAtFullTime / isBetweenQuarters come from periodPhase(currentQuarter, ageGroup.periodCount, quarterEnded, finalised)
    - For a 4-period config the rendered behaviour is byte-for-byte identical to today (correct-by-coincidence: the helper at periodCount=4 reproduces the old >=4 / <4 logic — proven by plan 01's periodCount=4 unit test)
    - quarterMs / subIntervalSeconds / positionModel / defaultOnFieldSize props remain present and unchanged (D-01b)
  </behavior>
  <action>
    In `src/components/live/LiveGame.tsx`:
    1. Add `import { periodPhase } from "@/lib/live/periodPhase";` and, if not
       present, `import type { AgeGroupConfig } from "@/lib/sports/types";`.
    2. In `LiveGameProps` (:146-225) add `ageGroup: AgeGroupConfig;` alongside
       the existing `quarterMs: number;` (:214). Keep ALL existing scalar props
       (`quarterMs`, `subIntervalSeconds`, `positionModel`, `defaultOnFieldSize`)
       — do NOT remove them (D-01b).
    3. In the `export function LiveGame({ ... })` destructure (:235-263) add
       `ageGroup,` next to `quarterMs,` (:261).
    4. Replace the booleans at :1019-1021:
       ```typescript
       const { isAtFullTime, isBetweenPeriods: isBetweenQuarters }
         = periodPhase(currentQuarter, ageGroup.periodCount, quarterEnded, finalised);
       const isFinished = finalised || isAtFullTime;
       ```
       (`currentQuarter` is read from the store at :280; `quarterEnded` and
       `finalised` are already in scope at :1019.) Keep the existing
       `isBetweenQuarters` consumer name via the rename-in-destructure shown.
    Do NOT touch any other `>= 4` / scalar-prop logic. tsc will now flag the
    LiveGame mount in page.tsx as missing `ageGroup` — Task 3 supplies it.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "livegame\|ageGroup" || echo "no LiveGame-internal type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "periodPhase(currentQuarter, ageGroup.periodCount" src/components/live/LiveGame.tsx` returns 1
    - `grep -c "ageGroup: AgeGroupConfig" src/components/live/LiveGame.tsx` returns 1 (new prop in interface)
    - `grep -vE "^\s*//" src/components/live/LiveGame.tsx | grep -cE "currentQuarter >= 4|currentQuarter < 4"` returns 0 (no period-count literal survives in non-comment code)
    - `grep -c "quarterMs" src/components/live/LiveGame.tsx` is still > 0 (scalar prop kept — D-01b)
  </acceptance_criteria>
  <done>LiveGame takes ageGroup, derives full-time/between-period booleans via periodPhase(); no hardcoded 4 in code; scalar props retained.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace NetballLiveGame's 3 render-gate literals with periodCount</name>
  <read_first>
    - src/components/netball/NetballLiveGame.tsx (render gates at lines 1659, 1696, 1780; the ageGroup prop already in scope ~:233)
    - src/components/league/LeagueLiveGame.tsx (lines 1222-1239) — the reference pattern
    - src/lib/live/periodPhase.ts — helper (reuse preferred per D-07)
    - .planning/phases/08-sport-agnostic-period-foundation/08-CONTEXT.md (D-07)
  </read_first>
  <behavior>
    - The full-time review gate (:1659), quarter-break gate (:1696), and
      between-Q4-and-finalise gate (:1780) all key off ageGroup.periodCount
      instead of the literal 4
    - For a 4-quarter netball config behaviour is byte-for-byte identical to today
  </behavior>
  <action>
    In `src/components/netball/NetballLiveGame.tsx`, `ageGroup` is already a prop
    in scope. Replace the three literals (helper reuse preferred per D-07):
    Option A (preferred — single derivation): near the top of render where
    `currentQuarter` / `quarterEnded` / `finalised` are in scope, add
    `import { periodPhase } from "@/lib/live/periodPhase";` and
    `const { isAtFullTime: nbIsAtFullTime, isBetweenPeriods: nbIsBetweenPeriods }
      = periodPhase(currentQuarter, ageGroup.periodCount, quarterEnded, finalised);`
    then change `if (quarterEnded && currentQuarter >= 4)` (:1659, :1780) to
    `if (nbIsAtFullTime || (finalised && quarterEnded && currentQuarter >= ageGroup.periodCount))`
    only if the existing branch already excludes finalised; otherwise use the
    minimal swap (Option B) to preserve exact branch semantics.
    Option B (minimal, semantics-preserving): swap inline —
    `currentQuarter >= 4` → `currentQuarter >= ageGroup.periodCount` (:1659, :1780)
    and `currentQuarter < 4` → `currentQuarter < ageGroup.periodCount` (:1696).
    Pick whichever keeps the three existing branch conditions byte-identical for
    periodCount=4; the goal is ONLY to remove the hardcoded 4. Verify the
    existing netball period-boundary e2e (netball-quarter-break.spec.ts) stays
    green.
  </action>
  <verify>
    <automated>npx vitest run src/lib/live/__tests__/periodPhase.test.ts && npx tsc --noEmit 2>&1 | grep -i "netballlivegame" || echo "no NetballLiveGame type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vE "^\s*//" src/components/netball/NetballLiveGame.tsx | grep -cE "currentQuarter >= 4|currentQuarter < 4"` returns 0 (no period-count literal survives in non-comment code)
    - `grep -c "ageGroup.periodCount" src/components/netball/NetballLiveGame.tsx` returns >= 3 (all three gates now derive from periodCount, inline or via helper)
    - tsc reports no NetballLiveGame-internal type errors
  </acceptance_criteria>
  <done>All three netball render gates key off ageGroup.periodCount; no hardcoded 4 remains in netball live code.</done>
</task>

<task type="auto">
  <name>Task 3: Thread ageCfgSport into the LiveGame mount + de-hardcode both sticky bars</name>
  <read_first>
    - src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx (LiveGame mount 693-720; netball sticky bar 493-496; AFL sticky bar 656-659; ageCfgSport resolved :603; ageCfgN resolved :379)
    - .planning/phases/08-sport-agnostic-period-foundation/08-CONTEXT.md (D-01a, CONFIG-01)
    - .planning/phases/08-sport-agnostic-period-foundation/08-PATTERNS.md (live/page.tsx section)
  </read_first>
  <action>
    In `src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx`:
    1. (D-01a) In the `<LiveGame ... />` mount (:693-720), add `ageGroup={ageCfgSport}`
       directly beside `quarterMs={quarterMs}` (:718). `ageCfgSport` is already
       resolved at :603 and already passed to QuarterBreak at :845 — reuse the
       same value. This satisfies LiveGame's new required prop from Task 1.
    2. (CONFIG-01) Netball sticky bar (:493-496): change
       `state.currentQuarter < 4` → `state.currentQuarter < ageCfgN.periodCount`
       (`ageCfgN` is in scope here, resolved :379-390).
    3. (CONFIG-01) AFL sticky bar (:656-659): change
       `state.currentQuarter < 4` → `state.currentQuarter < ageCfgSport.periodCount`
       (`ageCfgSport` in scope, :603).
    Discretion (D-07): either swap the literal inline as above, or call
    `periodPhase(state.currentQuarter, ageCfgN.periodCount, state.quarterEnded, state.finalised).isBetweenPeriods`
    (helper reuse). Inline swap is acceptable here since these are simple
    isAtQbreak booleans. Do NOT touch the netball/league LiveGame mounts (they
    already pass ageGroup).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ageGroup={ageCfgSport}" "src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx"` returns 1 (LiveGame mount now gets the config — D-01a)
    - `grep -vE "^\s*//" "src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx" | grep -c "currentQuarter < 4"` returns 0 (both sticky-bar literals replaced)
    - `grep -cE "currentQuarter < ageCfg(N|Sport).periodCount" "src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx"` returns 2 (netball + AFL sticky bars)
    - `npx tsc --noEmit` exits 0 (LiveGame's required ageGroup prop is now satisfied)
  </acceptance_criteria>
  <done>ageCfgSport flows into LiveGame; both sticky bars read periodCount; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4: Run the DoD gates (e2e included) and confirm no period literal survives</name>
  <read_first>
    - CLAUDE.md ("Testing is part of done" — DoD gates)
    - .planning/phases/08-sport-agnostic-period-foundation/08-CONTEXT.md (D-11; "Test files to extend" — existing AFL/netball specs that must stay green)
  </read_first>
  <action>
    Run the full DoD gauntlet per CLAUDE.md: `npx tsc --noEmit`, `npm run lint`,
    `npm test`, then `npm run e2e`. The existing AFL/netball period-boundary specs
    (`live-full-time.spec.ts`, `live-quarters.spec.ts`,
    `netball-quarter-break.spec.ts`) MUST stay green — they exercise the
    4-period path that this refactor must preserve byte-for-byte. Do a final
    repo-wide grep to confirm no `currentQuarter >= 4` / `< 4` literal survives in
    the three modified files' non-comment code. Fix any regression before
    finishing (write a red-first regression test if a behaviour bug surfaces).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test && npm run e2e</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
    - `npm test` exits 0 (includes periodPhase.test.ts + sports.test.ts)
    - `npm run e2e` exits 0 — live-full-time.spec.ts, live-quarters.spec.ts, netball-quarter-break.spec.ts all pass
    - Combined non-comment grep across the 3 modified files for `currentQuarter >= 4` and `currentQuarter < 4` returns 0
  </acceptance_criteria>
  <done>All four DoD gates green; existing AFL/netball e2e specs unbroken; zero period-count literals remain in the modified live surfaces.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run lint` exits 0
- `npm test` exits 0
- `npm run e2e` exits 0 (existing AFL/netball period-boundary specs green)
- No `currentQuarter >= 4` / `currentQuarter < 4` in non-comment code of LiveGame.tsx, NetballLiveGame.tsx, live/page.tsx
</verification>

<success_criteria>
- LiveGame.tsx takes the full ageGroup object (D-01), wired from page.tsx (D-01a), with scalar props retained (D-01b)
- LiveGame + NetballLiveGame booleans derive from periodPhase()/periodCount; no hardcoded 4 (D-07, CONFIG-01)
- Both live/page.tsx sticky bars read periodCount (CONFIG-01)
- Existing AFL/netball e2e specs stay green; tsc/lint/test/e2e all green (D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/08-sport-agnostic-period-foundation/08-03-SUMMARY.md`
</output>
