---
phase: 08-sport-agnostic-period-foundation
plan: 04
type: execute
wave: 3
depends_on: [3]
files_modified:
  - src/lib/fairness.ts
  - src/components/live/QuarterBreak.tsx
  - src/components/live/LiveGame.tsx
  - src/components/live/LineupPicker.tsx
  - src/lib/game-plan/project.ts
  - src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx
  - src/app/run/[token]/lineup/page.tsx
  - e2e/tests/rugby-league-full-game-playthrough.spec.ts
requirements: [CONFIG-01]
autonomous: true

must_haves:
  truths:
    - "D-02/D-04: fairness.ts replaces the hardcoded FULL_QUARTER_MS = 12*60*1000 (:601) with a TRAILING OPTIONAL param `fullPeriodMs: number = 12 * 60 * 1000` added at the END of suggestStartingLineup's signature (after chipModeByKey); the default keeps the ~20 unit-test callers green unchanged"
    - "D-03: the three production callers (QuarterBreak.tsx:408, LineupPicker.tsx:278, game-plan/project.ts:165) pass the PER-GAME EFFECTIVE quarter ms where the effective resolver is available — the season diversity threshold tracks THIS game's clock. EXCEPTION: the legacy share-token lineup page (run/[token]/lineup/page.tsx) runs on the legacy `ageGroups` shape (no periodSeconds, no getEffectiveQuarterSeconds), so it passes the age-group DEFAULT (`ageCfg.quarterSeconds * 1000`) — an accepted divergence, documented below."
    - "D-04a: suggestSwaps (fairness.ts:839) is NOT touched — that cumulative-minutes ranking is Phase 10 / B4"
    - "D-10: a 2-period rugby-league e2e asserts the H1 boundary shows a between-periods break surface (NOT full time) and the H2 boundary shows full time"
    - "CONFIG-01: the sibling `gameMinutes={(ageCfg.quarterSeconds * 4) / 60}` period-COUNT literal is de-hardcoded where a sports-config shape with periodCount is in scope (live/page.tsx → ageCfgSport.periodCount); the legacy run/[token] page keeps `* 4` (no periodCount on the legacy shape — recorded as a known legacy-shape residual below)"
    - "D-11: all four DoD gates (tsc, lint, vitest, e2e) green; the ~20 fairness unit-test callers stay green via the back-compat default"
  artifacts:
    - path: "src/lib/fairness.ts"
      provides: "suggestStartingLineup with a trailing optional fullPeriodMs param replacing the hardcoded FULL_QUARTER_MS literal"
      contains: "fullPeriodMs: number = 12 * 60 * 1000"
    - path: "src/components/live/QuarterBreak.tsx"
      provides: "AFL Q-break suggester passing the per-game effective ms as the new last arg"
      contains: "fullPeriodMs"
    - path: "src/components/live/LineupPicker.tsx"
      provides: "Q1 starting-lineup suggester passing the per-game effective ms"
      contains: "fullPeriodMs"
    - path: "src/lib/game-plan/project.ts"
      provides: "Projector passing (input.periodSeconds ?? ag.periodSeconds) * 1000 as the effective full-period ms"
      contains: "suggestStartingLineup"
    - path: "src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx"
      provides: "LineupPicker mount passing the per-game EFFECTIVE ms (getEffectiveQuarterSeconds * 1000) for fullPeriodMs, and gameMinutes count derived from ageCfgSport.periodCount"
      contains: "ageCfgSport.periodCount"
    - path: "src/app/run/[token]/lineup/page.tsx"
      provides: "Legacy share-token LineupPicker mount passing the age-group DEFAULT (ageCfg.quarterSeconds * 1000) for fullPeriodMs — legacy shape has no effective resolver; gameMinutes keeps `* 4` (no periodCount on legacy shape)"
      contains: "ageCfg.quarterSeconds * 1000"
    - path: "e2e/tests/rugby-league-full-game-playthrough.spec.ts"
      provides: "2-period boundary assertions: H1 → between periods (not full time); H2 → full time"
      contains: "half"
  key_links:
    - from: "src/components/live/QuarterBreak.tsx (suggestStartingLineup call :408)"
      to: "src/lib/fairness.ts (fullPeriodMs param)"
      via: "new last positional arg = per-game effective quarter ms"
      pattern: "chipModeByKey,\\s*\\n\\s*fullPeriodMs"
    - from: "src/lib/fairness.ts (seasonBonus :607)"
      to: "fullPeriodMs param"
      via: "seasonMins < fullPeriodMs ? SEASON_DIVERSITY : 0"
      pattern: "seasonMins < fullPeriodMs"
---

<objective>
Make the fairness engine's season full-period threshold sport-agnostic and
per-game accurate, and add the 2-period rugby-league e2e that proves period
BOUNDARY semantics hold for a halves sport.

Replace the hardcoded `FULL_QUARTER_MS = 12 * 60 * 1000` in `suggestStartingLineup`
(fairness.ts:601, used at :607) with a TRAILING OPTIONAL param
`fullPeriodMs: number = 12 * 60 * 1000` added at the END of the signature, after
`chipModeByKey` (D-02/D-04). The back-compat default keeps the ~20 existing unit
callers green UNCHANGED; the production callers pass the PER-GAME EFFECTIVE
quarter ms where the effective resolver is in scope (D-03). Then extend
`rugby-league-full-game-playthrough.spec.ts` to assert the H1 boundary is a
between-periods break (NOT full time) and H2 is full time (D-10).

Purpose: D-03 is a deliberate user decision — the season diversity threshold now
tracks the actual per-game clock (including per-game / per-team overrides for
finals or short games) rather than a fixed 12 min. The e2e exercises the league
(periodCount=2) path through the shared page.tsx booleans that plan 03 made
sport-agnostic.

Scope guard (D-04a): Do NOT touch `suggestSwaps` (fairness.ts:839) — its
cumulative-minutes ranking is Phase 10 / B4.

Output: fairness param + 3 production call sites + 2 mount sites threading the
effective ms + the extended rugby-league e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-sport-agnostic-period-foundation/08-CONTEXT.md
@.planning/phases/08-sport-agnostic-period-foundation/08-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- fairness.ts current state (verified file:line). -->
suggestStartingLineup signature ENDS at :554 with:
```typescript
  chipModeByKey: Partial<Record<"a" | "b" | "c", ChipMode>> = {},
): Lineup {
```
:598-601 (the literal to replace), used at :607:
```typescript
// "Played this zone for >= a full quarter all season" threshold (in ms).
const FULL_QUARTER_MS = 12 * 60 * 1000;
...
const seasonBonus = seasonMins < FULL_QUARTER_MS ? SEASON_DIVERSITY : 0;
```

<!-- Production call sites — each already ends its arg list with chipModeByKey,
     so the new fullPeriodMs is appended as a NEW LAST positional arg. -->
QuarterBreak.tsx:408-420 — suggestStartingLineup(... chipModeByKey,) ends at :419.
LineupPicker.tsx:278-289 — suggestStartingLineup(... chipModeByKey) ends at :289.
game-plan/project.ts:165-177 — suggestStartingLineup(... chipModeByKey,) ends at :176.

<!-- Per-game effective ms sources (verified) — KEY ASYMMETRY between the two
     LineupPicker mount sites: -->
// LiveGame.tsx already has `quarterMs` (effective ms) in scope and mounts
//   QuarterBreak at :1242 — thread quarterMs in as a new QuarterBreak prop.
//
// LineupPicker mount site A — live/page.tsx:831 (AFL branch, SPORTS-CONFIG shape):
//   getEffectiveQuarterSeconds is imported at :19; the AFL branch already calls it
//   at :604-608 to compute `quarterMs`, using the sports-config `ageCfgSport`
//   (resolved :603, has periodCount). The effective resolver IS available here.
//
// LineupPicker mount site B — run/[token]/lineup/page.tsx:113 (LEGACY share-token,
//   LEGACY shape):
//   This page imports the LEGACY module `@/lib/ageGroups` (`AGE_GROUPS`,
//   `ageGroupOf` — :6). Its `ageGroup` is a STRING (:42) and its `ageCfg`
//   (`AGE_GROUPS[ageGroup]`, :43) is the LEGACY shape — `quarterSeconds`, NO
//   `periodSeconds`, NO `periodCount`. It does NOT import getEffectiveQuarterSeconds.
//   The sports-config resolver expects the sports-config shape, so it CANNOT be
//   used here without a larger refactor (out of Phase 8 scope). → pass the
//   age-group DEFAULT `ageCfg.quarterSeconds * 1000` (accepted divergence, D-03).
//
// getEffectiveQuarterSeconds is at src/lib/sports/index.ts:36-45 (game→team→age).
// project.ts uses input.periodSeconds / ag.periodSeconds already; pass
//   (input.periodSeconds ?? ag.periodSeconds) * 1000.

<!-- gameMinutes period-COUNT literal at the SAME two mount sites (Finding 3): -->
// live/page.tsx:840              gameMinutes={(ageCfg.quarterSeconds * 4) / 60}
//   → the `* 4` is a hardcoded period count. `ageCfgSport.periodCount` IS in
//     scope here (:603) → de-hardcode `* 4` → `* ageCfgSport.periodCount`.
//     (`ageCfg.quarterSeconds` stays — only the COUNT literal changes.)
// run/[token]/lineup/page.tsx:122  gameMinutes={(ageCfg.quarterSeconds * 4) / 60}
//   → only the LEGACY shape is in scope (no periodCount) → LEAVE `* 4` as a known
//     legacy-shape residual (see Deferred / known residuals below).

<!-- e2e analog (verified to exist via git ls-files): -->
e2e/tests/rugby-league-full-game-playthrough.spec.ts — drives a U10 (2×20-min
halves, periodCount=2) game end-to-end with clock_multiplier=60. Fixtures:
createAdminClient + makeTeam/makePlayers/makeGame.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add trailing optional fullPeriodMs param to suggestStartingLineup</name>
  <read_first>
    - src/lib/fairness.ts (signature 526-555; FULL_QUARTER_MS 598-601; usage 603-611)
    - src/lib/__tests__/*.test.ts — confirm the ~20 callers stop at chipModeByKey (so the default keeps them green)
    - .planning/phases/08-sport-agnostic-period-foundation/08-CONTEXT.md (D-02, D-04, D-04a)
  </read_first>
  <behavior>
    - suggestStartingLineup(... chipModeByKey, fullPeriodMs = 12*60*1000) — new param is LAST and OPTIONAL
    - Calling with no fullPeriodMs reproduces today's behaviour exactly (default = 12 min) → all existing fairness unit tests stay green UNCHANGED
    - seasonBonus uses `seasonMins < fullPeriodMs` instead of the deleted FULL_QUARTER_MS constant
    - suggestSwaps is unchanged (D-04a)
  </behavior>
  <action>
    In `src/lib/fairness.ts`:
    1. Add the new param at the END of `suggestStartingLineup`'s signature, after
       `chipModeByKey: Partial<...> = {},` (:554), before the `): Lineup {`:
       ```typescript
         /**
          * Effective full-period length in ms used by the season diversity
          * threshold ("has this player logged >= one full period in this zone all
          * season?"). Trailing optional param: the back-compat default (12 min)
          * keeps the ~20 unit-test callers green; production callers pass the
          * PER-GAME effective quarter ms (getEffectiveQuarterSeconds * 1000) so
          * the threshold tracks this game's actual clock (D-03).
          */
         fullPeriodMs: number = 12 * 60 * 1000,
       ```
    2. DELETE the `const FULL_QUARTER_MS = 12 * 60 * 1000;` line (:601) and its
       two preceding comment lines (:598-600).
    3. At :607 change `const seasonBonus = seasonMins < FULL_QUARTER_MS ? ...` to
       `const seasonBonus = seasonMins < fullPeriodMs ? SEASON_DIVERSITY : 0;`
    Do NOT touch `suggestSwaps` (:839) or any of its cumulative-minutes logic
    (D-04a). Run the full Vitest suite — the ~20 fairness callers must stay green
    via the default.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fullPeriodMs: number = 12 \* 60 \* 1000" src/lib/fairness.ts` returns 1 (trailing optional param with back-compat default)
    - `grep -c "FULL_QUARTER_MS" src/lib/fairness.ts` returns 0 (literal removed)
    - `grep -c "seasonMins < fullPeriodMs" src/lib/fairness.ts` returns 1
    - `npm test` exits 0 — all existing fairness unit tests pass UNCHANGED (default keeps them green; D-04)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>fullPeriodMs is the new trailing optional param; FULL_QUARTER_MS gone; suggestSwaps untouched; all unit callers green via default.</done>
</task>

<task type="auto">
  <name>Task 2: Pass per-game effective ms from the three production callers</name>
  <read_first>
    - src/components/live/QuarterBreak.tsx (call site 408-420; props interface 66-97; mounted from LiveGame.tsx:1242)
    - src/components/live/LineupPicker.tsx (call site 278-289; props interface 54-98; gameMinutes prop)
    - src/lib/game-plan/project.ts (call site 165-177; uses input.periodSeconds / ag.periodSeconds)
    - src/lib/sports/index.ts (lines 36-45) — getEffectiveQuarterSeconds signature
    - src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx (LineupPicker mount :831-840; getEffectiveQuarterSeconds import :19; the AFL-branch call at :604-608; ageCfgSport :603; ageCfg :594)
    - src/app/run/[token]/lineup/page.tsx (LineupPicker mount :113-125; legacy AGE_GROUPS/ageGroupOf import :6; ageCfg = AGE_GROUPS[ageGroup] :43, legacy shape — quarterSeconds only)
    - .planning/phases/08-sport-agnostic-period-foundation/08-CONTEXT.md (D-03, D-04)
  </read_first>
  <behavior>
    - Each production suggestStartingLineup call passes the per-game full-period ms as the new last arg
    - QuarterBreak receives the effective ms from LiveGame (which already holds quarterMs)
    - LineupPicker receives the ms from its two mount sites: the AFL sports-config page passes the EFFECTIVE ms; the legacy share-token page passes the age-group DEFAULT (accepted divergence)
    - project.ts passes (input.periodSeconds ?? ag.periodSeconds) * 1000
  </behavior>
  <action>
    1. QuarterBreak (`src/components/live/QuarterBreak.tsx`):
       - Add `fullPeriodMs: number;` to `QuarterBreakProps` (near the other
         numeric props, ~:86). Destructure it in the `QuarterBreak({ ... })` block.
       - Append it as the NEW LAST arg of the suggestStartingLineup call (:408-420),
         immediately after `chipModeByKey,` (:419):
         ```typescript
             chipModeByKey,
             fullPeriodMs,
         );
         ```
       - In `src/components/live/LiveGame.tsx`, at the `<QuarterBreak ... />` mount
         (:1242-1256), pass `fullPeriodMs={quarterMs}` (LiveGame's `quarterMs` IS
         the per-game effective ms from getEffectiveQuarterSeconds * 1000 — see its
         doc comment at :211-214).
    2. LineupPicker (`src/components/live/LineupPicker.tsx`):
       - Add `fullPeriodMs: number;` to `LineupPickerProps` (~:64, beside gameMinutes).
         Destructure it. Append `fullPeriodMs` as the new last arg of the
         suggestStartingLineup call (:278-289, after `chipModeByKey` :289).
       - Mount site A — `src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx:831`
         (AFL branch, sports-config shape): this file already imports
         `getEffectiveQuarterSeconds` (:19) and the AFL branch already calls it at
         :604-608. Pass the EFFECTIVE ms using the EXACT same arg list that call uses:
         ```tsx
           fullPeriodMs={
             getEffectiveQuarterSeconds(
               { quarter_length_seconds: (teamRow as { quarter_length_seconds?: number | null } | null)?.quarter_length_seconds ?? null },
               ageCfgSport,
               { quarter_length_seconds: g.quarter_length_seconds },
             ) * 1000
           }
         ```
         (Identical to the `quarterMs` computation already in scope at :604-608 —
         `quarterMs` itself is `… * 1000`, so reusing the local `quarterMs` variable
         directly is also acceptable and preferred if it is in scope at the mount.
         The effective per-game/per-team override flows through, satisfying D-03.)
       - Mount site B — `src/app/run/[token]/lineup/page.tsx:113` (LEGACY share-token
         page, legacy shape): this page runs on the legacy `@/lib/ageGroups` shape
         (`ageCfg = AGE_GROUPS[ageGroup]`, :43) which has `quarterSeconds` but NO
         `periodSeconds`/`periodCount`, and does NOT import getEffectiveQuarterSeconds.
         The sports-config effective resolver is therefore UNAVAILABLE here. Pass the
         age-group DEFAULT:
         ```tsx
           fullPeriodMs={ageCfg.quarterSeconds * 1000}
         ```
         **D-03 limitation (documented, accepted divergence — NOT a bug):** the
         share-token lineup path uses the age-group DEFAULT quarter length, not the
         per-game / per-team EFFECTIVE override, because it runs on the legacy
         `ageGroups` shape that has no effective resolver. Wiring the sports-config
         resolver into this legacy page is a separate refactor, out of Phase 8 scope.
    3. Projector (`src/lib/game-plan/project.ts`): append the effective ms as the
       new last arg of the suggestStartingLineup call (:165-177, after `chipModeByKey`
       :176): `(input.periodSeconds ?? ag.periodSeconds) * 1000`.
    Do NOT alter the existing arg order/count before chipModeByKey.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fullPeriodMs" src/components/live/QuarterBreak.tsx` returns >= 3 (prop decl + destructure + call arg)
    - `grep -c "fullPeriodMs={quarterMs}" src/components/live/LiveGame.tsx` returns 1 (QuarterBreak fed the effective ms)
    - `grep -c "fullPeriodMs" src/components/live/LineupPicker.tsx` returns >= 3
    - `grep -c "getEffectiveQuarterSeconds" "src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx"` returns >= 4 (the existing 3 calls + the new fullPeriodMs prop on the LineupPicker mount; OR >= 3 if the local `quarterMs` variable is reused for the prop instead of a fresh call)
    - `grep -c "fullPeriodMs={ageCfg.quarterSeconds \* 1000}" src/app/run/[token]/lineup/page.tsx` returns 1 (legacy default — effective resolver unavailable on the legacy shape, accepted D-03 divergence)
    - `grep -cE "ag.periodSeconds\) \* 1000|periodSeconds \?\? ag.periodSeconds" src/lib/game-plan/project.ts` returns >= 1
    - `npx tsc --noEmit` exits 0 (new required props satisfied at all mounts)
    - `npm test` exits 0
  </acceptance_criteria>
  <done>All three production callers pass the full-period ms; the AFL sports-config mount supplies the EFFECTIVE ms, the legacy share-token mount supplies the age-group DEFAULT (documented divergence); tsc clean; unit suite green.</done>
</task>

<task type="auto">
  <name>Task 3: De-hardcode the sibling gameMinutes `* 4` period-count literal (conscious decision)</name>
  <read_first>
    - src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx (LineupPicker mount :831-840; gameMinutes :840; ageCfgSport :603 has periodCount; ageCfg :594 is legacy)
    - src/app/run/[token]/lineup/page.tsx (LineupPicker mount :113-125; gameMinutes :122; ageCfg :43 = AGE_GROUPS[ageGroup], legacy shape with NO periodCount)
    - .planning/phases/08-sport-agnostic-period-foundation/08-CONTEXT.md (CONFIG-01 — no hardcoded period-count literal)
  </read_first>
  <behavior>
    - At the AFL sports-config mount, the `gameMinutes` period count comes from
      `ageCfgSport.periodCount` instead of the literal 4
    - At the legacy share-token mount, `* 4` is intentionally retained (the legacy
      shape exposes no periodCount) and the decision is recorded in this plan
  </behavior>
  <action>
    Finding 3 — the sibling `gameMinutes={(ageCfg.quarterSeconds * 4) / 60}` prop
    at both LineupPicker mount sites embeds a hardcoded period count (`* 4`). Make
    a conscious, DOCUMENTED decision per mount based on which symbol is actually in
    scope (do NOT silently walk past it):

    1. `src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx:840` (AFL branch):
       a sports-config shape with `periodCount` IS in scope here (`ageCfgSport`,
       resolved :603). De-hardcode the count:
       ```tsx
         gameMinutes={(ageCfg.quarterSeconds * ageCfgSport.periodCount) / 60}
       ```
       (Keep `ageCfg.quarterSeconds` as the per-period length source — only the
       COUNT literal changes. `ageCfgSport.periodCount` is the same value already
       threaded into `<LiveGame>` by plan 03 / Task wiring.)

    2. `src/app/run/[token]/lineup/page.tsx:122` (legacy share-token page): ONLY the
       legacy shape is available (`ageCfg = AGE_GROUPS[ageGroup]`, :43 — NO
       `periodCount`). LEAVE `gameMinutes={(ageCfg.quarterSeconds * 4) / 60}` as-is.
       This is a known legacy-shape residual (see Deferred / known residuals below):
       the legacy `ageGroups` config has no `periodCount`, so de-hardcoding here
       would require the same sports-config refactor that the legacy page is out of
       scope for in Phase 8.

    The point of this task is that the decision is DOCUMENTED in the plan, not
    dropped: AFL path de-hardcoded, legacy path consciously retained with reason.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ageCfg.quarterSeconds \* ageCfgSport.periodCount" "src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx"` returns 1 (AFL gameMinutes count de-hardcoded)
    - `grep -vE "^\s*//" "src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx" | grep -c "quarterSeconds \* 4"` returns 0 (no `* 4` count literal survives in the sports-config page)
    - `grep -c "ageCfg.quarterSeconds \* 4" src/app/run/[token]/lineup/page.tsx` returns 1 (legacy residual intentionally retained — recorded in Deferred / known residuals)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>AFL gameMinutes count reads ageCfgSport.periodCount; legacy share-token `* 4` retained as a documented legacy-shape residual; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Extend the rugby-league e2e to assert H1=between-periods, H2=full-time</name>
  <read_first>
    - e2e/tests/rugby-league-full-game-playthrough.spec.ts (full file — existing 2×20-min halves playthrough)
    - e2e/README.md ("when to add a test" table)
    - .planning/phases/08-sport-agnostic-period-foundation/08-CONTEXT.md (D-10; "Why the e2e alone cannot prove CONFIG-01")
  </read_first>
  <behavior>
    - At the end of half 1 (periodCount=2, currentQuarter=1): a "between periods" /
      "Ready for half 2" break surface is shown, and the full-time / finalise
      review surface is NOT shown
    - At the end of half 2 (currentQuarter=2 = periodCount): the full-time / review
      (finalise) surface IS shown
  </behavior>
  <action>
    Extend `e2e/tests/rugby-league-full-game-playthrough.spec.ts`. The spec already
    drives a U10 (2×20-min halves) game end-to-end with clock_multiplier=60. Add
    explicit boundary assertions on the shared sticky-bar / break surface that
    plan 03 made sport-agnostic:
    - After the H1 hooter fires, assert the between-periods break surface is
      visible (the "Ready for half 2" / Q-break CTA) AND assert the full-time /
      "Finalise game" review surface is NOT visible. This is the assertion that
      proves `currentQuarter < periodCount` (1 < 2) → between periods on the league
      path through the de-hardcoded page.tsx booleans.
    - After the H2 hooter fires (currentQuarter=2 = periodCount), assert the
      full-time / review (finalise) surface IS visible.
    Reuse the existing `getByRole`/`getByText` locators the spec already uses for
    these surfaces; do not introduce a new fixture. Use the existing
    createAdminClient + makeTeam/makePlayers/makeGame setup. Run the spec.
  </action>
  <verify>
    <automated>npm run e2e -- rugby-league-full-game-playthrough</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ciE "half 2|between|full.?time" e2e/tests/rugby-league-full-game-playthrough.spec.ts` returns >= 2 (H1 between-periods + H2 full-time assertions present)
    - The spec contains an assertion that the full-time/finalise surface is NOT visible at the H1 boundary (e.g. `.not.toBeVisible()` or equivalent) and IS visible at the H2 boundary
    - `npm run e2e -- rugby-league-full-game-playthrough` passes green
  </acceptance_criteria>
  <done>The 2-period e2e asserts H1→between-periods (not full time) and H2→full time; spec passes.</done>
</task>

<task type="auto">
  <name>Task 5: Run all four DoD gates green</name>
  <read_first>
    - CLAUDE.md ("Testing is part of done" — DoD gates; commit style)
    - .planning/phases/08-sport-agnostic-period-foundation/08-CONTEXT.md (D-11; "Test files to extend" — existing AFL/netball specs that must stay green)
  </read_first>
  <action>
    Run the full DoD gauntlet per CLAUDE.md: `npx tsc --noEmit`, `npm run lint`,
    `npm test` (Vitest — confirms the ~20 fairness callers + periodPhase +
    sports floor assertions all pass), then `npm run e2e` (Playwright — the new
    rugby-league boundary assertions plus the existing AFL/netball period-boundary
    specs `live-full-time.spec.ts`, `live-quarters.spec.ts`,
    `netball-quarter-break.spec.ts` all green). Fix any regression before
    finishing; if a behaviour bug surfaces, write the regression test red-first
    per CLAUDE.md.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test && npm run e2e</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
    - `npm test` exits 0 (fairness unit callers unchanged-green via default; periodPhase + sports floor assertions pass)
    - `npm run e2e` exits 0 (rugby-league boundary spec + existing AFL/netball period specs all pass)
    - `grep -c "FULL_QUARTER_MS" src/lib/fairness.ts` returns 0 (the hardcoded literal is fully gone — CONFIG-01)
  </acceptance_criteria>
  <done>All four DoD gates green; CONFIG-01 fairness literal eliminated; no AFL/netball regressions.</done>
</task>

</tasks>

<deferred>
## Known residuals / accepted divergences (documented, not dropped)

- **Legacy share-token lineup uses the age-group DEFAULT, not the per-game
  effective override (D-03 limitation):** `src/app/run/[token]/lineup/page.tsx`
  runs on the legacy `@/lib/ageGroups` shape (`quarterSeconds` only, no
  `periodSeconds`/`periodCount`, no `getEffectiveQuarterSeconds`). Its
  `fullPeriodMs` is therefore fed `ageCfg.quarterSeconds * 1000` (age-group
  default) rather than `getEffectiveQuarterSeconds(...) * 1000`. Accepted
  divergence — wiring the sports-config resolver into this legacy page is a
  separate refactor out of Phase 8 scope. NOT a bug.

- **Legacy share-token `gameMinutes` retains `* 4` (CONFIG-01 residual):** the
  same legacy page keeps `gameMinutes={(ageCfg.quarterSeconds * 4) / 60}` at
  `:122` because the legacy shape exposes no `periodCount`. The AFL sports-config
  mount (`live/page.tsx:840`) IS de-hardcoded to `* ageCfgSport.periodCount`
  (Task 3). The legacy residual is recorded here so it is not silently walked
  past; it would be eliminated when the legacy page is migrated to the
  sports-config shape (future phase).

- **`src/lib/liveGameStore.ts:32` `export const QUARTER_MS = 12 * 60 * 1000`
  (conscious exclusion):** this is a period-LENGTH constant (not a period COUNT),
  already parameterised at runtime via `quarterMs`, and out of CONFIG-01's
  literal scope. Left as-is.
</deferred>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run lint` exits 0
- `npm test` exits 0 (existing fairness unit callers green via back-compat default — D-04)
- `npm run e2e` exits 0 (new rugby-league boundary assertions + existing AFL/netball specs green)
- `FULL_QUARTER_MS` no longer appears in fairness.ts; `suggestSwaps` is unchanged (D-04a)
- AFL `gameMinutes` count reads `ageCfgSport.periodCount`; legacy share-token residuals (`fullPeriodMs` default + `* 4` gameMinutes) documented in Deferred, not dropped
</verification>

<success_criteria>
- fairness.ts uses a trailing optional `fullPeriodMs` param; FULL_QUARTER_MS removed (D-02, D-04)
- Production callers pass the per-game EFFECTIVE quarter ms where the resolver is in scope; the legacy share-token page passes the age-group DEFAULT as a documented divergence (D-03)
- AFL `gameMinutes` period-count literal de-hardcoded via `ageCfgSport.periodCount`; legacy residual documented (CONFIG-01)
- suggestSwaps untouched (D-04a)
- Rugby-league 2-period e2e asserts H1=between-periods, H2=full-time (D-10)
- All four DoD gates green; ~20 fairness unit callers stay green unchanged (D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/08-sport-agnostic-period-foundation/08-04-SUMMARY.md`
</output>
