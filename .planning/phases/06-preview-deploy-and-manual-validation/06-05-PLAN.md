---
phase: 06-preview-deploy-and-manual-validation
plan: 05
type: execute
wave: 5
subsystem: manual-validation
tags: [validation, manual, blocked-on-creds, afl-flow, netball-flow, share-link, checkpoint-gated]
depends_on: ["06-04"]
files_modified:
  - .planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md
autonomous: false
blocked_on_user_creds: true
blocker_summary: "Live Vercel preview URL + Supabase prod clone (both produced by Plan 06-04). If Plan 06-04 stopped at its cred-readiness gate, Plan 06-05 cannot start."
requirements: [DEPLOY-02]
requirements_mapping:
  DEPLOY-02: "Preview manually validated — actual AFL game flow + actual netball game flow + share-link viewing — closes the manual-validation half of DEPLOY-02"
metrics:
  expected_files_changed: 1
  expected_loc: "+150 to +320 (single new validation evidence doc, populated as the user walks the flows)"

must_haves:
  truths:
    - "06-VALIDATION.md exists capturing the structured walkthrough evidence"
    - "An AFL game flow has been exercised end-to-end on the preview by a human (lineup, scoring, quarter-break, summary card, share-link)"
    - "A netball game flow has been exercised end-to-end on the preview by a human (lineup, scoring, quarter-break, stats dashboard, summary card)"
    - "An existing AFL share link `/run/[token]` has been opened on the preview without RLS errors or 404 (using the sample token surfaced by Plan 06-04 Task 6 §3)"
    - "Each flow's outcome (PASS / FAIL / N/A — e.g. no historical share tokens) is recorded with notes (issues observed, screenshots referenced if captured)"
    - "If any flow FAILed, Phase 6 STOPS and a Phase-7-blocker is logged; if all flows PASSed, Phase 6 closes and Phase 7 is ready to start"
  artifacts:
    - path: ".planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md"
      provides: "Structured manual-validation evidence — AFL flow + netball flow + share-link smoke; PASS/FAIL per surface"
      min_lines: 120
      contains:
        - "## AFL Flow"
        - "## Netball Flow"
        - "## Share Link"
        - "## Outcome"
        - "Preview URL"
  key_links:
    - from: ".planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md"
      to: ".planning/phases/06-preview-deploy-and-manual-validation/06-04-SUMMARY.md"
      via: "Reads Preview URL and sample share-token URL"
      pattern: "06-04-SUMMARY"
    - from: ".planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Closes DEPLOY-02 acceptance"
      pattern: "DEPLOY-02"
---

<objective>
A human walks the merged-trunk preview deploy through both sport flows end-to-end, on real prod-shape data, and records observed behaviour in `06-VALIDATION.md`. This is Phase 6's final acceptance gate — the difference between "code looks green in CI" and "code actually works in front of a coach on a Saturday morning."

**This plan is BLOCKED on Plan 06-04 having succeeded.** Specifically requires:
- Live Vercel preview URL (from 06-04-SUMMARY.md §1)
- A working sample share token URL (from 06-04-SUMMARY.md §3 Q5 — or noted as "no historical tokens, share-link smoke skipped")
- Active Supabase prod-clone backing the preview env

If Plan 06-04 stopped at its cred-readiness gate, Plan 06-05 cannot start. STATE.md flags both plans as blocked; resume with `/gsd-execute-phase 6 --wave 4` once Plan 06-04 completes.

Purpose: Close DEPLOY-02 (the only Phase 6 requirement still pending after 06-04 closes DEPLOY-01). Provides the evidence trail Phase 7 reads to decide whether the merged trunk is safe to fast-forward to production main.

Output: `.planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md` — a structured walkthrough document with PASS/FAIL per surface for each flow + a final outcome verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-preview-deploy-and-manual-validation/06-CONTEXT.md
@.planning/phases/06-preview-deploy-and-manual-validation/06-01-PLAN.md
@.planning/phases/06-preview-deploy-and-manual-validation/06-02-PLAN.md
@.planning/phases/06-preview-deploy-and-manual-validation/06-03-PLAN.md
@.planning/phases/06-preview-deploy-and-manual-validation/06-04-PLAN.md
@.planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md
@.planning/phases/04-netball-verification-on-merged-trunk/04-EVIDENCE.md
@.planning/phases/05-test-and-type-green/05-EVIDENCE.md

@CLAUDE.md

<flow_reference_specs>
<!-- The e2e specs are the canonical "what does a real flow look like" reference.
     These specs PASSed in Phase 5; the manual walkthrough mirrors their journey shape
     but exercises real prod-shape data (not local Kotara seed). -->

AFL flow shape (from PROD-01 specs that passed in Phase 5):
- `e2e/tests/availability.spec.ts` — toggle player availability, fill-in handling
- `e2e/tests/lineup.spec.ts` — lineup builder
- `e2e/tests/live-quarters.spec.ts` — Q1 → Q-break + rotation suggestion
- `e2e/tests/live-scoring.spec.ts` — record goal, undo
- `e2e/tests/live-swaps.spec.ts` — bench player onto field
- `e2e/tests/live-full-time.spec.ts` — finalise game
- `e2e/tests/runner-token.spec.ts` — share-link viewing

Netball flow shape (from Phase 4 NETBALL-N specs that passed in Phase 5):
- `e2e/tests/netball-walkthrough.spec.ts` — first-visit walkthrough
- `e2e/tests/netball-live-flow.spec.ts` — live shell, scoring, injury, late-arrival, finalise
- `e2e/tests/netball-quarter-break.spec.ts` — fairness suggestion + Start Qn
- `e2e/tests/netball-stats.spec.ts` — 5-section stats dashboard
- `e2e/tests/netball-summary.spec.ts` — summary card + share-link

Phase 6 manual validation does NOT re-run these specs against the preview (most require admin-login + DB seeding the prod clone won't support). The specs are the SHAPE reference for what the human walks through; the human exercises the same surfaces with real prod data instead of seeded fixtures.
</flow_reference_specs>

<context_validation_decisions>
<!-- From 06-CONTEXT.md <decisions> + <open_questions>.
     Open question: "should validation include creating a NEW netball team on the preview, or only loading existing AFL data?"
     Resolution (locked here for execute-time):
       - AFL flow: load EXISTING prod AFL team (this proves prod data survives the merge)
       - Netball flow: CREATE a new netball team on the preview (since prod has no historical netball teams; Phase 4 verified the shape against local Kotara seed, but Phase 6 must verify the create-new-team flow works against the prod-clone DB)
     The new netball team data lives on the clone (not prod) — it's safe to discard after validation. -->

Phase 6 manual validation strategy (locked):
1. AFL flow uses existing prod AFL team (clone has it)
2. Netball flow creates a NEW netball team on the preview (no historical netball data on prod)
3. Share-link smoke uses the sample token captured in 06-04-SUMMARY.md §3 (Q5 output)
</context_validation_decisions>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Plan-06-04-completed gate (BLOCKED on 06-04)</name>
  <what-built>Plan 06-04 has produced:
- A live Vercel preview URL (in 06-04-SUMMARY.md §1)
- A Supabase clone with 27 migrations applied (verify-prod-clone.mjs exited 0; in 06-04-SUMMARY.md §3)
- A sample share-token URL for the `/run/[token]` smoke (in 06-04-SUMMARY.md §3, OR a "no historical tokens" note)

This plan walks the human through the preview deploy and records evidence. The user must be ready for a 30–60 minute uninterrupted hands-on session with the preview open in a browser.</what-built>
  <how-to-verify>
Confirm BEFORE proceeding:

1. **Plan 06-04 closed PASS** — `06-04-SUMMARY.md` exists; verify-script exit code = 0; preview URL captured
2. **Preview URL responds** — `curl -fsS <preview-url>/` returns 200 right now (idempotent re-check; if it's been hours since 06-04, Vercel may have re-deployed automatically on a subsequent push)
3. **Browser ready** — User has a fresh browser session (or incognito) ready; DevTools console open to catch errors during walkthrough
4. **Time block** — User has 30–60 min uninterrupted; manual validation is not pause-friendly mid-flow (live game state lives in localStorage + DB; pausing mid-quarter introduces test artefacts)
5. **Login creds** — User has admin login credentials for the prod-clone Supabase project. The prod clone's auth.users table includes the same admin account that exists on prod (since the clone is a backup-restore). The user logs in with their normal prod admin credentials but pointed at the preview URL — this is the WHOLE POINT of validating against a clone, not local fixtures.

If ANY of the above is NO, signal "BLOCKED — not ready" and Plan 06-05 stops gracefully (06-04 is already done; 06-05 just hasn't started). Resume later with `/gsd-execute-phase 6 --wave 5`.

If ALL of the above is YES, signal "ready" and the executor walks you through the AFL + netball flows.
  </how-to-verify>
  <resume-signal>Type "ready" to start the manual validation walk, OR "BLOCKED — not ready" to defer Plan 06-05 to a future session</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Initialise 06-VALIDATION.md skeleton</name>
  <files>.planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md</files>
  <action>
Create the validation document skeleton. Tasks 2..4 will populate per-flow sections as the user walks the preview.

**Frontmatter top of file:**
```
---
phase: 06-preview-deploy-and-manual-validation
plan: 05
artifact: validation-evidence
generated: <execution date>
preview_url: <copy from 06-04-SUMMARY.md §1>
clone_project_ref: <copy from 06-04-SUMMARY.md §2>
deploy_head_sha: <copy from 06-04-SUMMARY.md §1>
afl_flow_outcome: PENDING
netball_flow_outcome: PENDING
share_link_outcome: PENDING
overall_outcome: PENDING
---
```

**§0 — Preface (4-6 lines)**

State explicitly:
- This document captures the manual validation walkthrough of the Phase 6 preview deploy.
- Preview URL, clone project ref, and HEAD SHA are pulled from 06-04-SUMMARY.md (the deploy execution evidence).
- Each flow section below is populated AS THE USER WALKS IT — the executor narrates the next step, the user confirms behaviour, and notes are recorded inline.
- An overall PASS / FAIL outcome at §4 gates Phase 6 close.

**§1 — AFL Flow** (skeleton — Task 2 populates)

Subsections (all start as "PENDING — to be exercised in Task 2"):
- §1.1 — Login to preview as admin (clone uses prod auth.users → existing admin account works)
- §1.2 — Load existing AFL team (one of the prod teams visible in dashboard)
- §1.3 — Toggle player availability (one player from available → unavailable, save, reload, persisted)
- §1.4 — Build lineup (drag/select 4 on-field per AFL rules, confirm)
- §1.5 — Start a live game; record one goal via Goal button or score-bug tap
- §1.6 — End Q1 → Q-break screen renders with rotation suggestion
- §1.7 — Start Q2; perform a swap (bench → on-field)
- §1.8 — End Q4 → finalise → summary card renders correctly with the recorded score
- §1.9 — Open share-link of the just-finalised game on a fresh tab → renders without RLS error

Each subsection has placeholder fields:
- **Result**: PENDING (becomes PASS / FAIL)
- **Notes**: (filled by user during walkthrough — observed errors, console warnings, UX oddities)
- **Screenshot**: (optional — if user captures, record the file path; otherwise skip)

**§2 — Netball Flow** (skeleton — Task 3 populates)

Subsections (all start as "PENDING — to be exercised in Task 3"):
- §2.1 — Create a NEW netball team via setup wizard (user picks name, age group "go", sport pill = Netball)
- §2.2 — Add 7+ players to the team (matches Kotara shape: 9 players ideal but 7 is the netball minimum)
- §2.3 — Build netball lineup (7 on-field positions: GS, GA, WA, C, WD, GD, GK)
- §2.4 — Start a live netball game; record one goal via GS or GA tap → confirm sheet → submit
- §2.5 — End Q1 → NetballQuarterBreak suggests rotation (5 fairness tiers)
- §2.6 — Accept suggestion or override; Start Q2
- §2.7 — Use long-press menu on a player (Mark injured, Lock for next break, Lend to opposition — pick one and exercise)
- §2.8 — Late-arrival menu (add a player who arrived after kickoff)
- §2.9 — End Q4 → finalise → netball summary card renders with goal count + per-player time + third %
- §2.10 — Open netball stats dashboard → all 5 sections render (player stats, minutes equity, chemistry, head-to-head, attendance)
- §2.11 — Open netball share-link of the just-finalised game on a fresh tab → renders correctly

Same placeholder fields as §1.

**§3 — Share Link** (skeleton — Task 4 populates)

- §3.1 — Open the sample AFL share-link URL (from 06-04-SUMMARY.md §3 Q5) in a fresh browser session (no auth)
- §3.2 — Confirm the page renders without RLS error and without 404
- §3.3 — Confirm the lineup / live state from prod is visible (this proves prod data survives the merge)

If 06-04-SUMMARY.md §3 noted "no historical share tokens", §3 is N/A — record N/A with the explanation.

**§4 — Outcome**

Final verdict (filled in Task 5):
- AFL flow: PASS / FAIL
- Netball flow: PASS / FAIL
- Share-link: PASS / FAIL / N/A
- Overall: PASS (all three PASS or share-link N/A) / FAIL (any FAIL)
- DEPLOY-02 status: closed/blocked

If FAIL: name the specific failure mode and reference the §-numbered subsection where it surfaced. Phase 6 does NOT close; STATE.md updates with a Phase-7-blocker entry.

**Length target for the skeleton**: ~120–180 lines pre-walkthrough. Tasks 2..4 populate notes inline (could grow to 200–320 lines depending on observation depth).
  </action>
  <verify>
    <automated>test -f ".planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md" &amp;&amp; grep -q "## §1 — AFL Flow\|## §1 - AFL Flow\|## AFL Flow" ".planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md" &amp;&amp; grep -q "## §2 — Netball Flow\|## §2 - Netball Flow\|## Netball Flow" ".planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md" &amp;&amp; grep -q "## §3\|## Share Link" ".planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md" &amp;&amp; grep -q "## §4\|## Outcome" ".planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md" &amp;&amp; grep -q "Preview URL\|preview_url" ".planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md"</automated>
  </verify>
  <done>
- 06-VALIDATION.md skeleton exists with §0..§4 sections
- Frontmatter populated from 06-04-SUMMARY.md (preview URL, clone project ref, deploy SHA)
- Each subsection has Result / Notes placeholder fields
  </done>
</task>

<task type="auto">
  <name>Task 2: Walk the AFL flow on the preview (record observations)</name>
  <files>.planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md</files>
  <action>
Walk the user through §1.1 → §1.9 sequentially. For each subsection:

1. Narrate the next step (referencing the e2e spec by name where the shape is canonical — e.g. "this is the same flow as `availability.spec.ts:13`")
2. Wait for the user to perform the action on the preview URL
3. Ask: "What did you see?" — record the observation in §1.X Notes
4. Mark §1.X Result as PASS, FAIL, or BLOCKED
5. If FAIL: ask the user to capture the failure mode (console error text, screenshot path, etc.); record verbatim in Notes
6. If BLOCKED (e.g. clone has zero AFL teams — wouldn't happen for a real prod clone, but flag it): mark BLOCKED with reason; do NOT proceed to subsequent subsections that depend on the same data; mark dependent subsections N/A

**Key surfaces to verify (from REQUIREMENTS.md PROD-01..03 + ROADMAP.md Phase 6 success criterion 2):**

- §1.1 Login: prod admin creds work against the clone (proves auth.users table came across in the snapshot restore)
- §1.2 Existing team load: prod team appears in dashboard team-list (proves SCHEMA-04 runtime side — `select * from teams` works through merged code)
- §1.3 Availability: PROD-01 fix from commit #52 (lineup availability) — toggle persists; reload shows persisted state
- §1.4 Lineup: AFL lineup with 4 on-field; no null-sport panic in `getEffectiveQuarterSeconds`
- §1.5 Live + scoring: PROD-01 fixes from commit #54 (live-scoring) + #55 (live-swaps preview) — goal records, score bug increments
- §1.6 Q-break: D-26 quarterMs wiring works for AFL (countdown ends at the AFL age-group default, e.g. 12 min for U10)
- §1.7 Swap: PROD-01 fix from commit #55 (live-swaps) — bench player onto field
- §1.8 Finalise + summary: existing AFL summary card; the trackScoring branch hit (AFL has trackScoring=true; goals visible)
- §1.9 Share-link of finalised game: a NEW share token created via the just-finished game — verify the `/run/[token]` resolves on a fresh tab (clean session)

After all subsections recorded, set §1 overall result based on the per-subsection results. Update frontmatter `afl_flow_outcome` field.

**If any subsection FAILs**: do not stop the walkthrough mid-flow — continue recording subsequent subsections (the user has already paid the context-switch cost; capture full evidence). The PASS/FAIL gate fires at Task 5, not here.
  </action>
  <verify>
    <automated>MISSING — operational task; verifies §1.1..§1.9 in 06-VALIDATION.md have non-PENDING Result fields after walkthrough completes.</automated>
  </verify>
  <done>
- §1.1..§1.9 each marked PASS / FAIL / BLOCKED / N/A
- §1 overall result recorded
- Frontmatter `afl_flow_outcome` updated
- All FAILed subsections have a verbatim failure-mode note
  </done>
</task>

<task type="auto">
  <name>Task 3: Walk the netball flow on the preview (record observations)</name>
  <files>.planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md</files>
  <action>
Walk the user through §2.1 → §2.11 sequentially. Same shape as Task 2.

**Important difference**: Netball flow CREATES a new netball team on the preview (clone has no historical netball teams — prod has none). The team is throwaway data on the clone; it does NOT need cleanup.

**Key surfaces to verify (from REQUIREMENTS.md NETBALL-01..08):**

- §2.1 Setup wizard: NETBALL-01 (sport pill — `<SportPill>` toggle works; "Netball" pill click → sport='netball' on the new team row)
- §2.2 Roster: same shape as AFL roster page; netball-specific UI affordances (no jersey-number requirement per ABSTRACT-01 `showJersey = sport === "afl"` UI toggle)
- §2.3 Lineup: 7 on-field, netball positions (GS/GA/WA/C/WD/GD/GK)
- §2.4 Goal: NETBALL-03 (GS/GA tap → confirm sheet → recordNetballGoal → 8s undo toast → goal count visible on bench strip)
- §2.5 Q-break: NETBALL-02 (5 fairness tiers — unplayed-third, same-position, same-third, friend-pair-split, season under-utilisation tiebreak)
- §2.6 Start Q2: D-26/D-27 quarterMs wiring works for netball (countdown lands at the netball age-group default for "go" age, which is 6 minutes per `src/lib/sports/netball.ts`)
- §2.7 Long-press menu: NETBALL-08 (Mark injured / Lend / Lock-for-break)
- §2.8 Late arrival: NETBALL-08 (player added after kickoff)
- §2.9 Summary card: NETBALL-06 (group-chat copyable text with 🏐 result, 🥅 goals, 👟 player count, ⏱ per-player time + third %)
- §2.10 Stats dashboard: NETBALL-05 (5 sections — player stats, minutes equity, chemistry, head-to-head, attendance, with per-position breakdown)
- §2.11 Netball share-link: same `/run/[token]` shape as AFL — token created when game is finalised; opens on fresh tab

After all subsections recorded, set §2 overall result. Update frontmatter `netball_flow_outcome`.
  </action>
  <verify>
    <automated>MISSING — operational task; verifies §2.1..§2.11 in 06-VALIDATION.md have non-PENDING Result fields after walkthrough completes.</automated>
  </verify>
  <done>
- §2.1..§2.11 each marked PASS / FAIL / BLOCKED / N/A
- §2 overall result recorded
- Frontmatter `netball_flow_outcome` updated
- All FAILed subsections have a verbatim failure-mode note
  </done>
</task>

<task type="auto">
  <name>Task 4: Walk the share-link smoke (record observations)</name>
  <files>.planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md</files>
  <action>
Walk the user through §3.1 → §3.3 sequentially.

The sample share-token URL came from 06-04-SUMMARY.md §3 Q5 (the verify-script's output). It points to a HISTORICAL prod AFL game (one that existed before the merge).

**If 06-04-SUMMARY.md §3 said "no historical tokens"**: §3 is N/A. The runner-token surface was already exercised in §1.9 (Task 2) for the freshly-finalised AFL game; record §3 as N/A with explanation. Update frontmatter `share_link_outcome=N/A`.

**Otherwise**:
- §3.1: User opens the sample token URL in a fresh browser session (incognito or different browser). Don't log in — share links are public-by-token.
- §3.2: Confirm: page renders; no 404; no RLS error in console; no null-sport panic
- §3.3: Confirm: prod data is visible (the historical game's lineup, score, etc.) — this proves the merge preserved historical share-token resolution

Record results. Update frontmatter `share_link_outcome`.

**Why this matters**: ROADMAP.md Phase 6 success criterion 4 = "Existing AFL share links (`/run/[token]` pattern) resolve correctly on the preview — no RLS errors, no 404s." This task is the direct test of that criterion.
  </action>
  <verify>
    <automated>MISSING — operational task; verifies §3.1..§3.3 in 06-VALIDATION.md have non-PENDING Result fields (or N/A with explanation).</automated>
  </verify>
  <done>
- §3.1..§3.3 each marked PASS / FAIL / N/A
- §3 overall result recorded
- Frontmatter `share_link_outcome` updated
  </done>
</task>

<task type="auto">
  <name>Task 5: Author §4 Outcome + finalise 06-VALIDATION.md</name>
  <files>.planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md</files>
  <action>
Aggregate the per-flow results from Tasks 2, 3, 4 into the §4 Outcome section.

**Outcome decision matrix:**

| AFL flow | Netball flow | Share-link | Overall |
|---|---|---|---|
| PASS | PASS | PASS or N/A | **PASS** — DEPLOY-02 closed; Phase 6 ready to close |
| PASS | PASS | FAIL | **FAIL** — share-link surface broken; Phase-7 blocker |
| FAIL | * | * | **FAIL** — AFL non-regression broken (PROD-01..03 risk); Phase-7 blocker |
| * | FAIL | * | **FAIL** — netball capability broken; Phase-7 blocker |

**§4 content:**

- AFL flow: <PASS / FAIL> (cross-ref to §1.X failures if any)
- Netball flow: <PASS / FAIL> (cross-ref to §2.X failures if any)
- Share-link: <PASS / FAIL / N/A> (cross-ref to §3.X)
- **Overall: <PASS / FAIL>**
- **DEPLOY-02 status: <closed / blocked>**

**If PASS**:
- Note "Phase 6 ready to close. Phase 7 (production cutover) is next."
- Do NOT update STATE.md or REQUIREMENTS.md from this plan — those updates belong in a Phase 6 close-out plan (out of scope for 06-05; the milestone-close orchestrator owns that).
- Update frontmatter `overall_outcome=PASS`.

**If FAIL**:
- List each FAILed surface with its observation note (verbatim from Tasks 2/3/4)
- Recommend next steps: which subsystem owns the fix; whether Phase 6 needs to re-run after the fix or whether Phase 7 absorbs it (depends on severity — UI polish goes to Phase 7 polish-pass; data-loss / RLS / null-sport panic stops Phase 7).
- Add a STATE.md `Blockers/Concerns` entry: "Phase 6 manual validation FAILed at <surface> (06-VALIDATION.md §X.Y); fix required before Phase 7 can start."
- Update frontmatter `overall_outcome=FAIL`.

**§5 — Hand-off (always present)**

- Cross-reference 06-04-SUMMARY.md (deploy evidence)
- Cross-reference 02-SCHEMA-PLAN.md §6 (acceptance criteria — confirm runtime side now closed by §1.9 + §3 + verify-prod-clone.mjs)
- Cross-reference REQUIREMENTS.md DEPLOY-01 (closed by Plan 06-04) + DEPLOY-02 (closed by this plan, on PASS)
- Note: Phase 7 (production cutover) reads §4 outcome to decide whether to proceed.

**Sensitivity guard (final pass)**: Re-grep the file for JWT pattern: `grep -E "eyJ[A-Za-z0-9_-]+\." 06-VALIDATION.md` should return zero matches. If a user pasted a console-error blob containing a JWT in §1/§2/§3 Notes, REDACT it (replace with `<REDACTED-jwt>`).
  </action>
  <verify>
    <automated>test -f ".planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md" &amp;&amp; grep -q "## §4\|## Outcome" ".planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md" &amp;&amp; grep -E "Overall:.*(PASS|FAIL)" ".planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md" &amp;&amp; ! grep -E "eyJ[A-Za-z0-9_-]+\." ".planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md"</automated>
  </verify>
  <done>
- §4 Outcome populated with per-flow results + overall verdict
- Frontmatter `overall_outcome` set (PASS or FAIL)
- §5 Hand-off cross-references in place
- Sensitivity guard: zero JWT matches in the file
- File is ~150–320 lines (skeleton + walkthrough notes)
  </done>
</task>

</tasks>

<verification>

**This plan is BLOCKED on Plan 06-04.** If 06-04 didn't run (creds gate stopped it), 06-05 can't start. Task 0's gate enforces this.

**Source-tree drift bound (HARD):**

- src/, scripts/, supabase/, e2e/, vercel.json, next.config.mjs, package.json — all UNCHANGED
- Only file added: `.planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md`

**Validation correctness:**

- Each per-flow subsection (§1.1..§1.9, §2.1..§2.11, §3.1..§3.3) is marked with a definitive Result (PASS / FAIL / BLOCKED / N/A) — no PENDING fields remain after Task 5
- §4 Overall outcome is one of: PASS, FAIL — never PENDING
- Frontmatter `overall_outcome` matches §4 verbatim

**Cred safety (final):**

- 06-VALIDATION.md does not contain JWT contents (re-verified at Task 5)
- Preview URL is captured but is non-sensitive (Vercel preview URLs are public by design — anyone with the URL can view, which is exactly what §3.1's incognito test exploits)
- Clone project ref is non-sensitive (it's the public identifier in the Supabase URL)

</verification>

<success_criteria>

1. Task 0 cred gate passed (Plan 06-04 closed; preview URL live; user has time block)
2. 06-VALIDATION.md exists with §0..§5 populated
3. AFL flow §1.1..§1.9 each have a non-PENDING Result
4. Netball flow §2.1..§2.11 each have a non-PENDING Result
5. Share-link §3.1..§3.3 each have a non-PENDING Result (or §3 N/A)
6. §4 Outcome verdict: PASS or FAIL (never PENDING)
7. DEPLOY-02 status recorded (closed on PASS; blocked on FAIL)
8. Source tree byte-identical to Phase 5 close + Plans 06-01..06-04 (only the new VALIDATION file appears in `git status`)
9. No JWT contents leaked into 06-VALIDATION.md

**Phase 6 close conditions** (orchestrator's responsibility; this plan does NOT close Phase 6 directly):
- Plan 06-04 PASS (DEPLOY-01 closed)
- Plan 06-05 PASS (DEPLOY-02 closed)
- ROADMAP.md Phase 6 marked complete
- STATE.md updates to Phase 7 ready position

If Plan 06-05 FAILs, the orchestrator does NOT close Phase 6; STATE.md flags the blocker; user decides whether to fix-and-rerun Phase 6 or absorb the fix into Phase 7.

</success_criteria>

<output>
After completion, the validation document IS the artefact (`06-VALIDATION.md`). No additional summary file required for this plan.

The Phase 6 close-out summary (combining 06-04 + 06-05 evidence + final ROADMAP/STATE updates) is the milestone-close orchestrator's job, not 06-05's. This plan's output ends at the populated 06-VALIDATION.md with a definitive overall verdict.
</output>
