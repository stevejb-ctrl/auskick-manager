---
phase: 06-preview-deploy-and-manual-validation
plan: 04
type: execute
wave: 4
subsystem: deploy-execute
tags: [deploy, blocked-on-creds, vercel-preview, supabase-clone, runbook-execute, checkpoint-gated]
depends_on: ["06-01", "06-02", "06-03"]
files_modified: []
autonomous: false
blocked_on_user_creds: true
blocker_summary: "Supabase prod clone (user provisions) + Vercel preview deploy creds (user configures). Phase 6 STOPS here per CONTEXT D-CONTEXT-cred-blocker if creds are not in place."
requirements: [DEPLOY-01]
requirements_mapping:
  DEPLOY-01: "Vercel preview deployment exists, built from merged trunk, pointing at Supabase env populated with recent prod snapshot — closes the artefact-existence half of DEPLOY-01"
metrics:
  expected_files_changed: 0
  expected_loc: "0 (no source / docs / scripts modified — runbook execution is operational, not authorial)"

must_haves:
  truths:
    - "A Vercel preview deployment of the merged trunk (HEAD `90364ee` or later) exists and responds 200 on `/`"
    - "The preview is backed by a Supabase environment that has all 27 migrations applied (verified by `node scripts/verify-prod-clone.mjs` exiting 0)"
    - "The Supabase env contains a recent prod snapshot (i.e. real prod-shape data — not the local Kotara seed)"
    - "The three Phase-6-CRITICAL Vercel env vars (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY) are set on the Preview environment scope and point at the Supabase clone"
    - "The preview URL is recorded somewhere durable (06-04-SUMMARY.md) for Plan 06-05's manual validation walk-through"
  artifacts:
    - path: ".planning/phases/06-preview-deploy-and-manual-validation/06-04-SUMMARY.md"
      provides: "Execution evidence — the preview URL, the clone Supabase project ref, the verify-script output, and any deviations from the runbook"
      min_lines: 40
      contains:
        - "Preview URL"
        - "Supabase clone"
        - "verify-prod-clone.mjs"
        - "Phase A"
        - "Phase B"
        - "Phase C"
        - "Phase D"
        - "Phase E"
  key_links:
    - from: ".planning/phases/06-preview-deploy-and-manual-validation/06-04-SUMMARY.md"
      to: ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md"
      via: "Summary records each runbook phase's outcome"
      pattern: "06-DEPLOY-RUNBOOK"
    - from: ".planning/phases/06-preview-deploy-and-manual-validation/06-04-SUMMARY.md"
      to: ".planning/phases/06-preview-deploy-and-manual-validation/06-05-PLAN.md"
      via: "Hands the preview URL to Plan 06-05"
      pattern: "Preview URL"
---

<objective>
Execute the deploy runbook (06-DEPLOY-RUNBOOK.md authored in Plan 06-02) end-to-end, with the user supplying credentials at the appropriate checkpoint gates. This plan does NOT author new code, scripts, or documentation — it is operational execution.

**This plan is BLOCKED on user creds. Specifically:**

1. **Supabase prod clone** — User must provision (or restore) a Supabase project with a recent prod snapshot. Per `06-CONTEXT.md` D-CONTEXT-cred-blocker. The plan pauses at a `checkpoint:human-verify` gate before any Phase A action.
2. **Vercel preview deploy creds** — User must have Vercel project access and (preferably) the `vercel` CLI authenticated. Per `06-CONTEXT.md` D-CONTEXT-cred-blocker.

If the user signals "creds NOT ready" at the gate, Phase 6 STOPS gracefully per `06-CONTEXT.md` D-CONTEXT-creds-not-ready-stop. STATE.md updates to flag Plans 06-04 + 06-05 as blocked; resume command is `/gsd-execute-phase 6 --wave 4` once prerequisites are in place.

Purpose: Close DEPLOY-01 (the deployment artefact exists). Plan 06-05 closes DEPLOY-02 (manual validation passes).

Output: A live Vercel preview URL backed by a prod-clone Supabase env, plus `06-04-SUMMARY.md` recording the preview URL + clone project ref + verify-script output for Plan 06-05's consumption.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-preview-deploy-and-manual-validation/06-CONTEXT.md
@.planning/phases/06-preview-deploy-and-manual-validation/06-01-PLAN.md
@.planning/phases/06-preview-deploy-and-manual-validation/06-02-PLAN.md
@.planning/phases/06-preview-deploy-and-manual-validation/06-03-PLAN.md
@.planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md
@.planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md
@.planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md

@scripts/verify-prod-clone.mjs
@vercel.json
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Cred-readiness gate (BLOCKED on user creds)</name>
  <what-built>Plans 06-01 + 06-02 + 06-03 have landed:
- `.planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md` (env-var matrix + vercel.json + next.config.mjs audit + migration enumeration)
- `.planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md` (step-by-step Phase A..E + Rollback)
- `scripts/verify-prod-clone.mjs` (read-only post-migration acceptance probe)

Plan 06-04 cannot make autonomous progress without creds. This is the explicit gate documented in `06-CONTEXT.md` D-CONTEXT-cred-blocker.</what-built>
  <how-to-verify>
Confirm BOTH credential sets are ready BEFORE answering the resume signal:

**1. Supabase prod clone**
   - Have you provisioned a new Supabase project (or restored an existing staging project) from a recent prod backup? (Per runbook §A)
   - Do you have the clone's URL + anon key + service-role key on hand?
   - Is the clone at the pre-merge schema state (last applied migration = `0023_perf_indexes`)? Verify in Supabase Console → Database → Migration History; the clone should NOT yet have 0024..0027 applied (those are Phase B's job).

**2. Vercel preview deploy creds**
   - Do you have access to the existing Siren Footy / Auskick Manager Vercel project?
   - Either: (a) the local `vercel` CLI is logged in (`vercel whoami` returns your account), OR (b) you're comfortable adding env vars via the Vercel dashboard and triggering deploy via `git push origin merge/multi-sport-trunk` to the connected GitHub remote.

**3. Optional but recommended**
   - The merge/multi-sport-trunk branch is pushed to the GitHub remote with HEAD at `90364ee` (Phase 5 close) or later (Phase 6 plans 06-01..06-03 commits add documentation+script — they don't change the source-tree green-bar).
   - You have ~30–60 minutes uninterrupted to walk through the runbook with the executor.

If ANY of the above is NO, signal "BLOCKED — creds not ready" and Phase 6 stops gracefully here per CONTEXT D-CONTEXT-creds-not-ready-stop. Resume later with `/gsd-execute-phase 6 --wave 4` once everything is in place.

If ALL of the above is YES, signal "ready" and the executor walks the runbook with you (prompting for creds at each step).
  </how-to-verify>
  <resume-signal>Type "ready" to proceed to Task 1, OR "BLOCKED — creds not ready" to stop Phase 6 gracefully (Plans 06-04 + 06-05 will resume in a future session)</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Walk runbook §A — Provision Supabase prod clone (with user)</name>
  <files></files>
  <action>
Read `06-DEPLOY-RUNBOOK.md` §A (Phase A — Provision Supabase prod clone). Walk the user through it interactively. The user makes all dashboard clicks; the executor narrates the step list, prompts for confirmation, and asks for outputs (clone URL, project ref) when needed.

DO NOT modify any files in this task. The output is captured in 06-04-SUMMARY.md (Task 6).

Pause for user confirmation between Path A1 (new project) and Path A2 (existing staging project). The user picks one based on what infrastructure already exists.

**Acceptance gate before moving to Task 2:**
- User confirms: clone is provisioned; URL + anon key + service-role key are in hand.
- The clone Supabase project is at the pre-merge schema state (0001..0023 applied; 0024..0027 PENDING).

If the user reports any issue (snapshot restore failed, schema not as expected, etc.), STOP and revise the runbook (Plan 06-02) — do NOT bash forward. Phase 6 is the dress rehearsal; surfacing issues here is a feature.
  </action>
  <verify>
    <automated>MISSING — operational task; runbook §A acceptance is human-confirmed (clone exists; pre-merge schema state). Captured in 06-04-SUMMARY.md.</automated>
  </verify>
  <done>
- User confirms clone provisioned with prod snapshot data
- Clone URL + anon key + service-role key in user's possession (DO NOT log to terminal; the executor records ONLY the project ref + URL host in 06-04-SUMMARY.md, never the keys)
- Clone schema state confirmed: 0001..0023 applied, 0024..0027 pending
  </done>
</task>

<task type="auto">
  <name>Task 2: Walk runbook §B — Apply migrations 0024..0027 + run verify-prod-clone.mjs</name>
  <files></files>
  <action>
Read `06-DEPLOY-RUNBOOK.md` §B. Walk the user through it. Two paths:
- **Path B1 (`supabase db push`)** — preferred when Supabase CLI is linked to the clone
- **Path B2 (manual SQL Editor)** — fallback if B1 hits the operator config

After migrations land, invoke `scripts/verify-prod-clone.mjs` (Plan 06-03) against the clone. Command:

```bash
SUPABASE_URL=<clone-url> \
SUPABASE_SERVICE_ROLE_KEY=<clone-service-role-key> \
node scripts/verify-prod-clone.mjs
```

The user pastes the clone creds into the shell environment (do NOT save to disk; the executor watches the script output).

**Acceptance gate before moving to Task 3:**
- `supabase migration list` (against the clone) shows 27 migrations applied
- `verify-prod-clone.mjs` exits 0 — Q3 (no null sports), Q4 (distinct contains 'afl') PASS; Q1 (migration count) PASS or WARN (PostgREST schema exposure edge); Q5 (share-token sample) PASS or WARN (zero historical tokens edge)

If verify-prod-clone.mjs exits non-zero with a FAIL on Q3 or Q4, the migration backfill broke — STOP and investigate; do NOT proceed to Phase D. Common causes: stale snapshot taken AFTER prior partial migration attempt (rare); manual SQL Editor application skipped a migration. Re-run from §A if needed.
  </action>
  <verify>
    <automated>MISSING — operational task; acceptance gate is `verify-prod-clone.mjs` exit 0 against the live clone (cannot be automated at plan-execute time without real creds).</automated>
  </verify>
  <done>
- All 27 migrations applied to the clone (verified via `supabase migration list`)
- `node scripts/verify-prod-clone.mjs` exits 0 against the clone
- Q5 sample token URL captured for Plan 06-05's manual smoke (or noted as "no historical tokens — share-link smoke skipped")
  </done>
</task>

<task type="auto">
  <name>Task 3: Walk runbook §C — Configure Vercel preview env vars</name>
  <files></files>
  <action>
Read `06-DEPLOY-RUNBOOK.md` §C. Walk the user through setting the three Phase-6-CRITICAL env vars on the Vercel project's "Preview" environment scope. Two paths:
- **Vercel dashboard** — Project → Settings → Environment Variables → Add New (per var) → check "Preview" only
- **Vercel CLI** — `vercel env add NEXT_PUBLIC_SUPABASE_URL preview` (etc.)

Use the values captured in Task 1 (clone URL, anon key, service-role key). Confirm SUPABASE_SERVICE_ROLE_KEY is marked sensitive in the Vercel UI.

**Critical**: do NOT touch the Production environment scope at any point. Phase 7 owns production cutover; Phase 6 only writes to the Preview scope.

**Acceptance gate before moving to Task 4:**
- `vercel env ls preview` (CLI) lists exactly the three Phase-6-CRITICAL vars (or the dashboard equivalent shows them under the Preview scope)
- Values match the clone provisioned in Task 1
- Production scope UNCHANGED — verify by inspecting `vercel env ls production` and confirming no values were added or modified
  </action>
  <verify>
    <automated>MISSING — operational task; acceptance is `vercel env ls preview` showing the three vars (cannot be automated at plan-execute time without Vercel auth).</automated>
  </verify>
  <done>
- Three Phase-6-CRITICAL vars present on Preview scope
- Production scope verified UNCHANGED (D-CONTEXT-no-prod-touch invariant)
  </done>
</task>

<task type="auto">
  <name>Task 4: Walk runbook §D — Trigger preview deploy</name>
  <files></files>
  <action>
Read `06-DEPLOY-RUNBOOK.md` §D. Walk the user through triggering the preview deploy. Two paths:
- **Path D1 (`git push`)** — preferred — passive Vercel git integration auto-triggers the deploy
- **Path D2 (`vercel deploy --target preview`)** — active local CLI invocation

If Path D1: confirm the merge/multi-sport-trunk branch is pushed to the GitHub remote (`git ls-remote origin merge/multi-sport-trunk` returns the local HEAD). Vercel's deploy starts within seconds; watch progress in the Vercel dashboard.

If Path D2: run `vercel deploy --target preview` from repo root. CLI prints the preview URL on success.

**Acceptance gate before moving to Task 5:**
- Vercel dashboard shows deploy status "Ready" (not "Building" or "Error")
- Build log shows zero errors; warnings should match the 3 pre-existing lint warnings from Phase 5 close (per 05-EVIDENCE.md §1) — if more or different warnings appear, investigate
- Preview URL is captured (paste into chat for the executor to record)

If the deploy errors out:
- Common cause 1: missing/wrong env vars → re-run Task 3
- Common cause 2: Node version mismatch → check Vercel build log; the repo doesn't pin Node version, Vercel defaults to Node 20+ which works
- Common cause 3: lockfile drift → unlikely (lockfile committed); if it happens, `npm install` locally and push the updated lockfile
- For any other error: STOP and investigate; do not iterate at the deploy layer
  </action>
  <verify>
    <automated>MISSING — operational task; acceptance is the live preview URL responding 200 (cannot be automated without Vercel deploy).</automated>
  </verify>
  <done>
- Vercel deploy status: "Ready"
- Preview URL captured (e.g. `siren-footy-<hash>-<gh-user>.vercel.app`)
- `curl -fsS <preview-url>/` returns 200 on the marketing landing page
  </done>
</task>

<task type="auto">
  <name>Task 5: Walk runbook §E — Smoke check + run verify-prod-clone.mjs against the live preview env</name>
  <files></files>
  <action>
Read `06-DEPLOY-RUNBOOK.md` §E. Walk the user through the smoke check.

Automated smoke:
1. `curl -fsS <preview-url>/` → expect 200
2. `curl -fsS <preview-url>/login` → expect 200 (auth route compiles)
3. Open `<preview-url>/` in browser → confirm marketing landing page renders, no console errors (note: open DevTools → Console; expected zero red errors; some warnings about service-worker / Speed Insights are tolerated)

Re-invoke `scripts/verify-prod-clone.mjs` with the same clone creds — confirms the clone hasn't drifted since Task 2 (idempotent re-run; should still exit 0).

**Acceptance gate before moving to Task 6:**
- Three curl probes return 200
- Browser load of preview URL has zero console errors
- `verify-prod-clone.mjs` re-run still exits 0

If smoke surfaces an issue (e.g. preview returns 500 on `/`), DO NOT proceed to Plan 06-05. Capture the failure mode in the summary; rollback per runbook §F if needed.
  </action>
  <verify>
    <automated>MISSING — operational task; acceptance is curl returning 200 against the live preview URL.</automated>
  </verify>
  <done>
- Three curl probes (200 on `/`, `/login`, optionally `/teams`) PASS
- Browser smoke: marketing landing renders with zero console errors
- `verify-prod-clone.mjs` re-run exits 0
  </done>
</task>

<task type="auto">
  <name>Task 6: Author 06-04-SUMMARY.md — execution evidence</name>
  <files>.planning/phases/06-preview-deploy-and-manual-validation/06-04-SUMMARY.md</files>
  <action>
Author the summary capturing Phase 6 deploy-execute evidence. Sections:

**§1 — Preview URL**
- Full URL (e.g. `siren-footy-merge-multi-sport-trunk-<hash>-<gh-user>.vercel.app`)
- HEAD SHA the deploy was built from (`git rev-parse HEAD` at the time of `git push`)
- Build duration (Vercel dashboard "Building" → "Ready" delta)

**§2 — Supabase clone**
- Project ref (e.g. `abcdefghijklmnop`) — NOT the URL hostname; the project ref is the public identifier, safe to record
- Path used: A1 (new project) or A2 (existing staging restored)
- Migration list state at completion: all 27 applied, no pending (paste output of `supabase migration list` if available, or note "verified via dashboard")
- Snapshot date: when was the prod backup taken (e.g. "2026-04-29 daily backup")

**§3 — verify-prod-clone.mjs output**
- Final exit code (must be 0)
- Per-query results: Q1 (PASS/WARN), Q3 (PASS), Q4 (PASS), Q5 (PASS/WARN)
- If Q5 PASSed: the sample token URL string for Plan 06-05's manual `/run/[token]` walk
- If any query WARNed, name the warn condition (e.g. "Q1 WARN — schema_migrations not exposed via PostgREST; operator manually verified `supabase migration list` shows 27 applied")

**§4 — Runbook phase outcomes**
- Phase A: PASS / FAIL (with notes)
- Phase B: PASS / FAIL
- Phase C: PASS / FAIL — list the three vars set + confirm Production scope UNCHANGED
- Phase D: PASS / FAIL — which path used (D1 git or D2 CLI)
- Phase E: PASS / FAIL — three curl probes + browser load + verify-script re-run

**§5 — Deviations from runbook**
- Anything that didn't go per the runbook (e.g. Path B1 errored, fell back to B2; Vercel build hit a previously-unseen warning; etc.)
- If zero deviations, state "None — runbook executed verbatim."

**§6 — Hand-off to Plan 06-05**
- Preview URL (re-stated for findability)
- Sample share-token URL from Q5 (for the `/run/[token]` manual smoke)
- Confirmation Plan 06-05 is now unblocked
- Note: Plan 06-05 is also `autonomous: false` (manual walk-through); the user signals "ready for validation" to start Plan 06-05.

**Frontmatter top of file:**
```
---
phase: 06-preview-deploy-and-manual-validation
plan: 04
artifact: execution-summary
generated: <execution date>
preview_url: <captured>
clone_project_ref: <captured>
deploy_head_sha: <captured>
verify_script_exit: 0
runbook_deviations: <count or "none">
---
```

**Length target**: ~40–80 lines. This is execution evidence, not narrative; bullets + values, no filler.

**Sensitivity guard**: Do NOT log the SUPABASE_SERVICE_ROLE_KEY or anon key contents anywhere in this summary. Project ref + URL hostname are safe; JWT contents are NOT.
  </action>
  <verify>
    <automated>test -f ".planning/phases/06-preview-deploy-and-manual-validation/06-04-SUMMARY.md" &amp;&amp; grep -q "Preview URL" ".planning/phases/06-preview-deploy-and-manual-validation/06-04-SUMMARY.md" &amp;&amp; grep -q "verify-prod-clone" ".planning/phases/06-preview-deploy-and-manual-validation/06-04-SUMMARY.md" &amp;&amp; ! grep -E "eyJ[A-Za-z0-9_-]+\." ".planning/phases/06-preview-deploy-and-manual-validation/06-04-SUMMARY.md"</automated>
  </verify>
  <done>
- 06-04-SUMMARY.md exists with all 6 sections
- Preview URL captured
- verify-prod-clone.mjs exit code = 0 recorded
- Sensitivity guard: no JWTs ("eyJ..." pattern) in the summary file
- File is ~40–80 lines
  </done>
</task>

</tasks>

<verification>

**This plan is BLOCKED on user creds.** If creds are not in place at Task 0's gate, the plan stops gracefully with no work done. STATE.md updates: "Phase 6 plans 1-3 ready; plans 4-5 blocked on user creds."

**If the plan runs to completion, validate:**

1. Preview URL is live: `curl -fsS <preview-url>/` returns 200 (Task 5)
2. `node scripts/verify-prod-clone.mjs` against the clone exits 0 (Task 2 + Task 5 re-run)
3. `06-04-SUMMARY.md` exists with all required sections; no JWT contents inadvertently logged
4. Source tree UNCHANGED — Plans 06-04 + 06-05 do not author code; `git status -- src/ scripts/ supabase/ e2e/ vercel.json next.config.mjs package.json` is clean (only `.planning/phases/06-preview-deploy-and-manual-validation/06-04-SUMMARY.md` is new)
5. Vercel Production environment scope UNCHANGED — D-CONTEXT-no-prod-touch invariant verified by the user inspecting `vercel env ls production` before/after Task 3

**Source-tree drift bound (HARD):**

- src/, scripts/, supabase/, e2e/, vercel.json, next.config.mjs, package.json — all UNCHANGED. Any modification here means the executor exceeded scope; revert and re-plan.

**Cred safety:**

- Service-role JWTs are SECRET. They go into Vercel dashboard / `vercel env add` only — NEVER into a committed file, NEVER into a terminal log captured in 06-04-SUMMARY.md.
- The clone's project ref + URL hostname are non-sensitive; safe to record in the summary.
- A defensive grep guards against accidental JWT logging: `grep -E "eyJ[A-Za-z0-9_-]+\." 06-04-SUMMARY.md` must return zero matches (matches the JWT compact-serialisation prefix).

</verification>

<success_criteria>

1. Cred-readiness gate (Task 0) passed — user signalled "ready"
2. Vercel preview deploy exists at a captured URL, status "Ready", responds 200 on `/`
3. Supabase clone has all 27 migrations applied; `verify-prod-clone.mjs` exits 0 against it
4. Three Phase-6-CRITICAL Vercel env vars set on Preview scope; Production scope unchanged
5. `06-04-SUMMARY.md` exists with all 6 sections + zero JWT leaks
6. Source tree byte-identical to Phase 5 close + Plans 06-01..06-03 (only the new SUMMARY file appears in `git status`)
7. DEPLOY-01 closed (preview deploy artefact exists)
8. Plan 06-05 is unblocked and ready to start

</success_criteria>

<output>
After completion, the summary file IS the artefact (`06-04-SUMMARY.md`). No additional output beyond what Task 6 authors.

Hand-off pointer: Plan 06-05 reads §1 (Preview URL) + §3 (sample share-token URL) directly to drive the manual validation walk-through.

If the plan stopped at Task 0 (creds not ready), update STATE.md `Blockers/Concerns`:
- "Phase 6 Plans 06-04 + 06-05 paused — user creds not in place; resume with `/gsd-execute-phase 6 --wave 4` once Supabase clone provisioned + Vercel preview env configured."
</output>
