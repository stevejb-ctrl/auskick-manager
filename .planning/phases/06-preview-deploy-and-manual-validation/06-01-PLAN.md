---
phase: 06-preview-deploy-and-manual-validation
plan: 01
type: execute
wave: 1
subsystem: deploy-hygiene
tags: [deploy, hygiene, env-matrix, vercel-config, documentation-only]
depends_on: []
files_modified:
  - .planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md
autonomous: true
requirements: [DEPLOY-01]
requirements_mapping:
  DEPLOY-01: "Pre-flight hygiene — vercel.json + next.config.mjs + env-var matrix + build-step sanity captured before any preview deploy"
metrics:
  expected_files_changed: 1
  expected_loc: "+150 to +280 (single new checklist doc)"

must_haves:
  truths:
    - "A pre-deploy hygiene checklist exists at .planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md"
    - "The checklist enumerates every env var the merged-trunk source code reads (verified by grep against src/), classified by whether the user must supply a prod-clone value or a Vercel-injected value"
    - "The checklist documents vercel.json's current shape (only the PlayHQ cron job; no preview-specific overrides) and confirms whether any change is required for the Phase 6 preview"
    - "The checklist documents next.config.mjs's current shape (empty config object) and confirms no preview-specific config is required"
    - "The checklist confirms `npm run build` works against the merged-trunk source with the existing .env.local.example shape (build-step sanity)"
    - "The checklist enumerates the migration set (27 files, 0001..0027) so the runbook (Plan 06-02) can reference it directly"
  artifacts:
    - path: ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md"
      provides: "Hygiene checklist + env-var matrix + vercel.json/next.config.mjs audit + build sanity result + migration enumeration"
      min_lines: 120
      contains:
        - "## Env Var Matrix"
        - "NEXT_PUBLIC_SUPABASE_URL"
        - "NEXT_PUBLIC_SUPABASE_ANON_KEY"
        - "SUPABASE_SERVICE_ROLE_KEY"
        - "CRON_SECRET"
        - "RESEND_API_KEY"
        - "TELEGRAM_BOT_TOKEN"
        - "## vercel.json audit"
        - "## next.config.mjs audit"
        - "## Build sanity"
        - "## Migration set"
  key_links:
    - from: ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md"
      to: ".planning/phases/06-preview-deploy-and-manual-validation/06-02-PLAN.md"
      via: "deploy runbook references env-var matrix from checklist"
      pattern: "06-DEPLOY-CHECKLIST.md"
    - from: ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md"
      to: "vercel.json"
      via: "audit section names the file verbatim"
      pattern: "vercel\\.json"
    - from: ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md"
      to: "supabase/migrations/"
      via: "migration enumeration section lists 0001..0027"
      pattern: "0027_game_quarter_seconds"
---

<objective>
Author the Phase 6 pre-deploy hygiene checklist — a single self-contained markdown document the user (and any future executor) reads before invoking the deploy runbook (Plan 06-02). The checklist is the audit layer between "Phase 5 closed green" and "we're ready to push the merged trunk to a Vercel preview."

Purpose: Surface every environmental input the deploy depends on (env vars, Vercel config, Next config, migration set) so a single page tells the operator everything they must verify in the Vercel + Supabase dashboards before clicking "Deploy." Phase 6 plans 06-02 + 06-04 reference this checklist directly; without it the runbook would have to re-derive the env-var matrix from scratch each time.

Output: `.planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md` — a fresh markdown doc with five top-level sections: (1) Env Var Matrix, (2) vercel.json audit, (3) next.config.mjs audit, (4) Build sanity, (5) Migration set. No source code, no scripts, no migrations touched. Pure documentation.

This plan is autonomous: no creds, no external services, no human signal needed. Plan 06-01 + 06-02 + 06-03 are the prep; 06-04 + 06-05 are the BLOCKED-on-creds execute.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-preview-deploy-and-manual-validation/06-CONTEXT.md
@.planning/phases/05-test-and-type-green/05-EVIDENCE.md
@.planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md

@vercel.json
@next.config.mjs
@.env.local.example
@.env.test.example
@package.json
@CLAUDE.md

<env_var_inventory>
<!-- Verified by grep on 2026-04-30 against src/ — all process.env.* references in the merged-trunk source.
     Executor should re-run the grep to pick up any env reads added since this plan was authored. -->

From `grep -rE "process\.env\.[A-Z_]+" src/`:

| Var | First-seen file | Notes |
|-----|-----------------|-------|
| `NEXT_PUBLIC_SUPABASE_URL` | src/lib/supabase/{client,server,middleware,admin}.ts | Browser + server + middleware + admin clients all read this |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | src/lib/supabase/{client,server,middleware}.ts | Browser + server + middleware (NOT admin — admin uses service role) |
| `SUPABASE_SERVICE_ROLE_KEY` | src/lib/supabase/admin.ts | Privileged operations only (super-admin routes, etc.) |
| `CRON_SECRET` | src/app/api/cron/sync-playhq/route.ts, src/app/api/admin/seed-demo/route.ts | Vercel auto-injects in prod; local sets via .env.local |
| `VERCEL_ENV` | src/app/layout.tsx (`IS_PROD_DEPLOY = process.env.VERCEL_ENV === "production"`) | Vercel auto-injects; gates Speed Insights / analytics on prod only |
| `NEXT_PUBLIC_DEFAULT_BRAND` | src/lib/supabase/middleware.ts | Optional — defaults via `??` if unset |
| `RESEND_API_KEY` | src/lib/resend.ts | Optional — `notifySignup` no-ops if unset |
| `RESEND_FROM_EMAIL` / `RESEND_TO_EMAIL` | src/lib/resend.ts | Optional — both default via `??` |
| `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` | src/lib/notifications/telegram.ts | Optional — telegram.ts no-ops if unset |
</env_var_inventory>

<vercel_json_current>
<!-- Current vercel.json (verbatim) — only PlayHQ cron, no preview overrides, no build command override. -->

```json
{
  "crons": [
    {
      "path": "/api/cron/sync-playhq",
      "schedule": "0 3 * * *"
    }
  ]
}
```

Implication: Vercel preview deployments inherit defaults — `npm run build` (= `next build`) is the build command; the cron only fires on production deployments per Vercel's default cron behaviour (preview deployments do not run crons). No preview-specific config is needed for Phase 6.
</vercel_json_current>

<next_config_current>
<!-- Current next.config.mjs (verbatim) — empty config object. -->

```js
/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
```

Implication: No experimental flags, no rewrites, no headers, no env-passthrough. Next.js stock behaviour. No preview-specific config is needed for Phase 6.
</next_config_current>

<package_json_scripts>
<!-- Build + deploy-relevant scripts (verbatim from package.json:scripts). -->

- `dev` → `next dev`
- `build` → `next build` (this is what Vercel runs; if it fails locally with the env vars set, it'll fail on Vercel too)
- `start` → `next start`
- `lint` → `next lint`
- `typecheck` → `tsc --noEmit`
- `test` → `vitest run`
- `e2e` → `node scripts/e2e-setup.mjs`
- `db:start` / `db:stop` / `db:reset` / `db:status` → Supabase CLI wrappers

There is NO existing `vercel`-prefixed script. Deploys are triggered by push-to-branch (Vercel's git integration) or `vercel --prebuilt` from the user's local CLI. The runbook (Plan 06-02) documents both paths.
</package_json_scripts>

<migration_set>
<!-- Verified `ls supabase/migrations/` on 2026-04-30 — 27 files. -->

Shared with prod (carried unchanged across the merge):
0001_initial_schema, 0002_games_availability, 0003_live_game, 0004_sub_interval,
0005_injury, 0006_share_token, 0007_on_field_size, 0008_age_group,
0009_playhq_external_id, 0010_team_playhq_url, 0011_team_song, 0012_song_duration,
0013_score_undo_and_field_zone_swap, 0014_game_fill_ins, 0015_squad_size,
0016_song_enabled, 0017_team_invites, 0018_crm_foundation, 0019_player_loan,
0020_jersey_number_nullable, 0021_demo, 0022_parent_mark_availability,
0023_perf_indexes (23 shared migrations)

Multi-sport / Phase 3 net-new (the four migrations the prod clone will see for the first time):
0024_multi_sport (sport + track_scoring + age_group constraint relax + period_break_swap event widen)
0025_super_admin (renumbered from main:0024 per Phase 2 §1 hash equivalence)
0026_team_quarter_seconds (per-team quarter override)
0027_game_quarter_seconds (per-game quarter override)

Phase 2 §6 acceptance carry-forward: After applying 0024..0027 to the prod clone, all five §6 queries must pass (verify-prod-clone.mjs from Plan 06-03 automates them).
</migration_set>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 06-DEPLOY-CHECKLIST.md</name>
  <files>.planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md</files>
  <action>
Create the checklist file with five required top-level sections, in this order:

**§1 — Env Var Matrix**

A markdown table with one row per env var the merged-trunk source reads. Columns: `Var`, `Required for Phase 6 preview?`, `Source`, `Sensitivity`, `Where to set in Vercel`, `Notes`.

Rows (every var below MUST appear; rows for vars that don't exist in the source MUST NOT appear — derive the list from the `<env_var_inventory>` block above, not from prior knowledge):

| Var | Required? | Source | Sensitivity | Where to set in Vercel | Notes |
|-----|-----------|--------|-------------|------------------------|-------|
| `NEXT_PUBLIC_SUPABASE_URL` | YES — required | User-supplied (Supabase prod-clone project URL) | Public (NEXT_PUBLIC_*) | Project Settings → Environment Variables → Preview | Per-CONTEXT D-CONTEXT-cred-blocker — user provisions the prod clone |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | YES — required | Supabase prod-clone anon key | Public | Project Settings → Environment Variables → Preview | Read by browser + middleware + server clients |
| `SUPABASE_SERVICE_ROLE_KEY` | YES — required | Supabase prod-clone service-role key | **SECRET** — never log, never commit | Project Settings → Environment Variables → Preview (mark as secret) | Required by `src/lib/supabase/admin.ts` for super-admin paths |
| `CRON_SECRET` | OPTIONAL on preview | Auto-injected on Vercel prod; preview gets undefined unless explicitly set | Secret | Vercel auto-injects on production; do NOT manually set on preview unless cron testing is in scope | Per `src/app/api/cron/sync-playhq/route.ts` — cron only fires on prod, not preview, by Vercel's default behaviour |
| `VERCEL_ENV` | AUTO | Vercel-injected — `production` / `preview` / `development` | N/A — read-only | Auto-injected by Vercel | Used by `src/app/layout.tsx` to gate Speed Insights to prod-only |
| `NEXT_PUBLIC_DEFAULT_BRAND` | OPTIONAL | User decision (defaults via `??` if unset) | Public | Optional | Brand multi-tenancy hint; safe to leave unset for Phase 6 |
| `RESEND_API_KEY` | OPTIONAL | Resend dashboard | Secret | Skip on preview unless contact-form testing is in scope | `notifySignup` + ContactForm no-op if unset; Phase 6 manual validation does NOT exercise the contact form |
| `RESEND_FROM_EMAIL` / `RESEND_TO_EMAIL` | OPTIONAL | User decision | Public-ish | Skip on preview | Defaults defined in `src/lib/resend.ts` if unset |
| `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` | OPTIONAL | Telegram + user account | Secret | Skip on preview | Sign-up notifier no-ops if unset; Phase 6 doesn't exercise sign-ups beyond the existing super-admin |

After the table, document:
- The Phase-6-CRITICAL three: NEXT_PUBLIC_SUPABASE_URL + NEXT_PUBLIC_SUPABASE_ANON_KEY + SUPABASE_SERVICE_ROLE_KEY are the only env vars without which the preview deploy will fail at runtime (every page that touches Supabase goes through `getSupabaseClient()` / `getSupabaseAdminClient()` which dereference these with `!`).
- The two Vercel-injected three: VERCEL_ENV + CRON_SECRET (when on prod) — these the operator does NOT set manually.
- Where Vercel stores per-environment vars: Vercel dashboard → Project → Settings → Environment Variables → checkbox the "Preview" environment scope.

**§2 — vercel.json audit**

Embed the current `vercel.json` verbatim (use the `<vercel_json_current>` block above). State explicitly:

- Current shape: only the PlayHQ cron, no build-command override, no per-environment overrides
- Phase 6 preview deploys do NOT run crons (Vercel runs crons only on Production by default — confirmed Vercel docs)
- No changes required to `vercel.json` for Phase 6
- If a future phase needs preview crons or per-environment build commands, this is where it lands; out of Phase 6 scope

**§3 — next.config.mjs audit**

Embed the current `next.config.mjs` verbatim (use the `<next_config_current>` block above). State explicitly:

- Current shape: empty config object (`{}`)
- No experimental flags, no rewrites, no headers, no asset-prefix overrides, no env-passthrough
- Next.js 14.2.29 stock behaviour
- No changes required for Phase 6

**§4 — Build sanity**

Document the build-step Vercel will run on the preview:

- Vercel default: `npm install && npm run build` → `next build`
- This is the same command Phase 5's gauntlet `tsc + lint + vitest + e2e` validated against (e2e ran `next build` indirectly via `next start` on port 3000 in scripts/e2e-setup.mjs)
- A fresh local `npm run build` is OPTIONAL at hygiene time but RECOMMENDED — confirms the merged trunk's source compiles with whatever env vars the operator has locally
- If `npm run build` succeeds locally with `.env.local.example` values copied to `.env.local`, it WILL succeed on Vercel preview with the equivalent prod-clone values configured in the dashboard
- The runbook (Plan 06-02) explicitly invokes `npm run build` as a pre-deploy local sanity step before pushing

**§5 — Migration set**

List all 27 migrations (use the `<migration_set>` block above), grouped:

- 23 shared / pre-fork migrations (0001..0023) — already on prod, will be NO-OPs against the prod clone (already applied during the snapshot restore)
- 4 net-new migrations (0024..0027) — the only migrations the operator must apply on top of the prod-clone snapshot

Note explicitly: `supabase/seed.sql` is NOT applied to the prod clone (per CONTEXT — Kotara seed is local-dev only; the prod clone uses real prod data via backup-restore). The runbook MUST NOT reference `npm run db:reset` against the prod clone.

Also note the Phase 2 §6 acceptance criteria are unchanged carry-forward — the verify script (Plan 06-03) automates queries 1..5.

**Frontmatter top of file:**
```
---
phase: 06-preview-deploy-and-manual-validation
artifact: hygiene-checklist
generated: <execution date — use shell `date -u +"%Y-%m-%d"`>
inputs:
  - vercel.json
  - next.config.mjs
  - .env.local.example
  - .env.test.example
  - package.json
  - supabase/migrations/ (27 files)
  - src/ (process.env reads — grep verified)
---
```

**Tone:** Operational reference, not narrative. Tables + bullet lists. The reader is the human about to provision a Supabase clone and a Vercel preview env — they need to scan-and-go.

**Length target:** ~150–280 lines. Long enough to enumerate everything; short enough to skim. Do NOT pad with explanatory prose where a bullet suffices.
  </action>
  <verify>
    <automated>test -f ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md" &amp;&amp; grep -q "## §1" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md" &amp;&amp; grep -q "## §2" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md" &amp;&amp; grep -q "## §3" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md" &amp;&amp; grep -q "## §4" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md" &amp;&amp; grep -q "## §5" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md" &amp;&amp; grep -q "NEXT_PUBLIC_SUPABASE_URL" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md" &amp;&amp; grep -q "SUPABASE_SERVICE_ROLE_KEY" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md" &amp;&amp; grep -q "0027_game_quarter_seconds" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md"</automated>
  </verify>
  <done>
- File exists at the canonical path
- All 5 numbered sections (§1..§5) present
- Env-var matrix references all three Phase-6-CRITICAL vars by exact name
- Migration set section names 0027_game_quarter_seconds (the last migration — proves the full set was enumerated, not truncated)
- File is ~150–280 lines (verified by `wc -l`)
- No source code touched; no scripts touched; no migrations touched (verified by `git status -- src/ scripts/ supabase/`)
  </done>
</task>

</tasks>

<verification>

**Phase 5 baseline must remain green** — Plan 06-01 is documentation-only and MUST NOT regress the gauntlet:

- `git status -- src/ scripts/ supabase/ e2e/ package.json` — must be clean (zero changes)
- `git status` — only `.planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md` should appear as new

**Source-tree drift bound (HARD — per CONTEXT):**

- src/ unchanged
- scripts/ unchanged
- supabase/ unchanged
- e2e/ unchanged
- vercel.json unchanged
- next.config.mjs unchanged
- package.json unchanged

If ANY file in the above directories changed, plan 06-01 has overrun its scope — STOP and revise.

**Checklist correctness:**

- Env-var matrix matches the live `grep -rE "process\.env\.[A-Z_]+" src/` result. Re-run the grep at execute time and reconcile any drift since plan authoring.
- vercel.json + next.config.mjs embedded blocks match the live files byte-for-byte. Re-read the files at execute time and reconcile.
- Migration set enumerates all 27 files; `ls supabase/migrations/ | wc -l` returns 27.

</verification>

<success_criteria>

1. `.planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md` exists with all 5 §-numbered sections.
2. Env-var matrix is comprehensive (every `process.env.*` reference in src/ is accounted for).
3. vercel.json + next.config.mjs audits each conclude "no changes required for Phase 6."
4. Build sanity section confirms `npm run build` is what Vercel will run, and points at Plan 06-02 for the actual build invocation as part of the runbook.
5. Migration set enumeration is complete (23 shared + 4 net-new = 27 total) and explicitly excludes `supabase/seed.sql` from prod-clone application.
6. Source tree (src/ + scripts/ + supabase/ + e2e/ + vercel.json + next.config.mjs + package.json) is byte-identical to Phase 5 close. Verified by `git status` showing only the new checklist file.

</success_criteria>

<output>
After completion, create `.planning/phases/06-preview-deploy-and-manual-validation/06-01-SUMMARY.md` summarising:
- Lines authored in 06-DEPLOY-CHECKLIST.md
- Any drift found between the embedded env-var/vercel/next-config blocks and the live source (should be ZERO at this point in time — Phase 5 close)
- Confirmation `git status -- src/ scripts/ supabase/ e2e/ package.json vercel.json next.config.mjs` is clean
- Hand-off pointer: Plan 06-02 (deploy runbook) consumes §1 (env-var matrix) + §5 (migration set) directly; Plan 06-03 (verify script) consumes §5 + Phase 2 §6 acceptance criteria.
</output>
