---
phase: 06-preview-deploy-and-manual-validation
plan: 02
type: execute
wave: 2
subsystem: deploy-runbook
tags: [deploy, runbook, supabase-clone, vercel-preview, documentation-only, user-facing]
depends_on: ["06-01"]
files_modified:
  - .planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02]
requirements_mapping:
  DEPLOY-01: "Step-by-step runbook for: provisioning Supabase prod clone, applying migrations, configuring Vercel preview env, triggering deploy"
  DEPLOY-02: "Runbook closes with hand-off to Plan 06-05 manual validation — names AFL + netball flows by spec/route"
metrics:
  expected_files_changed: 1
  expected_loc: "+220 to +400 (single new runbook doc)"

must_haves:
  truths:
    - "A step-by-step deploy runbook exists at .planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md"
    - "The runbook is structured as numbered phases (Phase A: Supabase clone; Phase B: migrations; Phase C: Vercel env config; Phase D: deploy trigger; Phase E: smoke check) — each with explicit shell commands or dashboard click-paths"
    - "The runbook references 06-DEPLOY-CHECKLIST.md (Plan 06-01) for the env-var matrix, not re-deriving it"
    - "The runbook references scripts/verify-prod-clone.mjs (Plan 06-03) as the automated post-migration acceptance probe"
    - "The runbook explicitly notes that Plan 06-04 (deploy execute) is BLOCKED on user creds and pauses for the user signal"
    - "The runbook covers BOTH deploy-trigger paths: (a) push-to-branch via Vercel git integration; (b) `vercel deploy --target preview` via local CLI"
    - "The runbook provides a rollback path: revert the Vercel deploy + (if needed) restore Supabase clone from a fresh snapshot"
  artifacts:
    - path: ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md"
      provides: "Step-by-step user-facing runbook for Phase 6 preview deploy"
      min_lines: 200
      contains:
        - "## Phase A — Provision Supabase prod clone"
        - "## Phase B — Apply migrations 0024..0027"
        - "## Phase C — Configure Vercel preview env vars"
        - "## Phase D — Trigger preview deploy"
        - "## Phase E — Smoke check"
        - "## Rollback path"
        - "supabase migration up"
        - "vercel deploy --target preview"
        - "verify-prod-clone.mjs"
        - "06-DEPLOY-CHECKLIST.md"
  key_links:
    - from: ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md"
      to: ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-CHECKLIST.md"
      via: "Phase C references the env-var matrix from the checklist"
      pattern: "06-DEPLOY-CHECKLIST"
    - from: ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md"
      to: "scripts/verify-prod-clone.mjs"
      via: "Phase E invokes the verify script"
      pattern: "verify-prod-clone\\.mjs"
    - from: ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md"
      to: ".planning/phases/06-preview-deploy-and-manual-validation/06-VALIDATION.md"
      via: "Runbook hand-off pointer to manual validation (Plan 06-05)"
      pattern: "06-VALIDATION"
---

<objective>
Author the Phase 6 preview-deploy runbook — the human-facing operational document the user follows to provision the Supabase prod clone, apply migrations, configure Vercel preview env vars, trigger the deploy, and smoke-check the result. This is the document Plan 06-04 (BLOCKED on creds) executes against; without it, the executor at deploy-time has no canonical step list.

Purpose: Convert the locked CONTEXT decisions (Vercel preview slot of existing project; user-provisioned Supabase clone via backup-restore; 3-layer validation) into an unambiguous phase-by-phase command sequence. Each step has either an exact shell command or an exact dashboard click-path — no "figure out the rest" gaps.

Output: `.planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md` — a fresh markdown doc with six top-level sections: (Phase A) Supabase prod clone provisioning, (Phase B) Migration application, (Phase C) Vercel preview env-var configuration, (Phase D) Deploy trigger (two-path: git or CLI), (Phase E) Smoke check, (Rollback path). No source code, no scripts, no migrations touched. Pure documentation.

Plan 06-02 is autonomous: documentation only, no creds needed. The runbook is what Plan 06-04 executes against, but authoring it requires zero credentials.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-preview-deploy-and-manual-validation/06-CONTEXT.md
@.planning/phases/06-preview-deploy-and-manual-validation/06-01-PLAN.md
@.planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md
@.planning/phases/05-test-and-type-green/05-EVIDENCE.md

@vercel.json
@package.json
@CLAUDE.md

<runbook_skeleton>
<!-- Reference structure for the runbook. Executor SHOULD follow this section ordering verbatim;
     the content under each section is what Plan 06-02 authors. -->

## Phase A — Provision Supabase prod clone (~10–20 min, USER ACTION)
   - Inputs needed: Supabase account, prod project access, recent backup
   - Two paths: (A1) new Supabase project initialised from prod snapshot; (A2) existing staging project restored from prod snapshot
   - Output: A Supabase project URL + anon key + service-role key, with prod data restored, schema at the pre-merge state (i.e. before 0024..0027 are applied)
   - Acceptance: `supabase migration list` against the clone shows up through 0023_perf_indexes (the last migration shared with prod); 0024..0027 are NOT applied yet (Phase B applies them)

## Phase B — Apply migrations 0024..0027 (~2–5 min)
   - Path 1: `supabase db push` from the merged-trunk repo with the clone's project linked via `supabase link`
   - Path 2: Manual application — paste each of the four SQL files into Supabase SQL Editor, in order
   - Acceptance: `supabase migration list` shows 27 migrations applied; runs `scripts/verify-prod-clone.mjs` (Plan 06-03) and exits 0

## Phase C — Configure Vercel preview env vars (~5 min, USER ACTION)
   - Per 06-DEPLOY-CHECKLIST.md §1, set the three Phase-6-CRITICAL vars on the "Preview" environment scope
   - Vercel dashboard click-path: Project → Settings → Environment Variables → "Add New" → check "Preview" environment scope
   - Acceptance: `vercel env ls preview` (Vercel CLI) lists the three Phase-6-CRITICAL vars

## Phase D — Trigger preview deploy
   - Path 1 (preferred — passive): `git push origin merge/multi-sport-trunk` triggers Vercel git integration; preview deploy URL appears in PR-equivalent comment or in Vercel dashboard
   - Path 2 (active — local CLI): `vercel deploy --target preview` from repo root
   - Acceptance: Vercel preview URL responds with 200 on `/` (marketing/landing page)

## Phase E — Smoke check
   - Auto: `curl -fsS <preview-url>/` exits 0 (preview is up)
   - Auto: `curl -fsS <preview-url>/login` exits 0 (auth route compiles)
   - Auto: `node scripts/verify-prod-clone.mjs` against the prod-clone Supabase env exits 0 (all 5 Phase 2 §6 acceptance queries pass)
   - Manual: Plan 06-05 owns the human walk-through

## Rollback path
   - Vercel: revert deploy via dashboard ("Promote previous" / "Rollback" in deploy history)
   - Supabase: if migrations went wrong, restore the clone from an earlier snapshot (Path A1: delete + recreate; Path A2: re-run snapshot restore)
   - Code: `git revert <merge-commit>` if a hotfix is needed; re-run from Phase B
</runbook_skeleton>

<env_var_critical_three>
<!-- Sourced from 06-DEPLOY-CHECKLIST.md §1 — Plan 06-01 is the authority. Don't re-derive. -->

The three vars without which preview deploy fails at runtime:
- NEXT_PUBLIC_SUPABASE_URL (clone's project URL)
- NEXT_PUBLIC_SUPABASE_ANON_KEY (clone's anon JWT)
- SUPABASE_SERVICE_ROLE_KEY (clone's service-role JWT — SECRET)

All other vars are optional for Phase 6 manual validation per 06-DEPLOY-CHECKLIST.md §1.
</env_var_critical_three>

<deploy_trigger_research>
<!-- Vercel deploy mechanism for the merge/multi-sport-trunk branch.
     Verified by reading vercel.json (no overrides) + Vercel git integration defaults. -->

- Vercel's git integration: Pushes to ANY branch in a connected repo trigger a deploy. Default behaviour:
  - Pushes to the production branch (typically `main`) → Production deploy
  - Pushes to any other branch → Preview deploy (with auto-generated `<branch>-<hash>.vercel.app` URL)
- For Phase 6: The merge/multi-sport-trunk branch is NOT main, so a `git push origin merge/multi-sport-trunk` to the connected GitHub remote produces a preview deploy. URL format: `siren-footy-<hash>-<git-username>.vercel.app` or similar.
- Alternative: `vercel deploy --target preview` from local CLI uploads the local working tree (or the `--prebuilt` output if `vercel build` was run first) and creates a preview deploy without needing a git push.
- Either path produces an artefact: a Vercel-hosted preview URL the user opens in a browser for manual validation.
</deploy_trigger_research>

<phase_2_section_6_acceptance>
<!-- Verbatim from 02-SCHEMA-PLAN.md §6 — the Phase 6 acceptance criteria for SCHEMA-04 runtime side. -->

1. Apply the merged migration set against a Supabase prod-clone DB. All 27 migrations apply in order with zero errors.
2. Load at least one pre-existing AFL team through the merged code. No RLS errors, no null-sport panics in dashboard team-list page or live-game shell.
3. Verify `select count(*) from teams where sport is null` returns 0.
4. Verify `select distinct sport from teams` returns only `'afl'`.
5. Verify at least one pre-existing AFL share token still resolves through `/run/[token]` — no 404, no RLS error, no null-sport panic.

Plan 06-03 (verify-prod-clone.mjs) automates queries 1, 3, 4. Queries 2 + 5 are manual (Plan 06-05).
</phase_2_section_6_acceptance>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 06-DEPLOY-RUNBOOK.md — Phases A through E + Rollback</name>
  <files>.planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md</files>
  <action>
Create the runbook file with the structure from the `<runbook_skeleton>` block above. Each section has the structure:

- **What**: 1-2 sentences naming the deliverable of this phase
- **Inputs**: bullet list of credentials / accesses required before starting
- **Steps**: numbered list of explicit shell commands or dashboard click-paths
- **Acceptance**: how to verify this phase succeeded before moving to the next
- **Estimated time**: realistic ballpark (e.g. "10–20 min" for snapshot restore)

**Frontmatter top of file:**
```
---
phase: 06-preview-deploy-and-manual-validation
artifact: deploy-runbook
generated: <execution date — use shell `date -u +"%Y-%m-%d"`>
audience: human operator (user); Plan 06-04 executor following user signal
prerequisites:
  - Phase 5 closed green (06-CONTEXT.md confirms)
  - Plan 06-01 06-DEPLOY-CHECKLIST.md committed (env-var matrix authority)
  - Plan 06-03 scripts/verify-prod-clone.mjs committed (post-migration acceptance probe)
  - User has: Supabase account access; access to a recent prod backup; Vercel project access
---
```

### §0 — Preface (3-6 lines)

State explicitly:
- This runbook is followed by a HUMAN OPERATOR (the user). Plan 06-04 (deploy execute) waits at a checkpoint:human-verify gate until the user signals "creds ready"; once signalled, the executor walks the runbook with the user's creds and prompts the user at any step requiring dashboard interaction.
- The merged-trunk source is at HEAD `90364ee` (Phase 5 close — captured in 05-EVIDENCE.md §5). Phase 6 plans 06-01 + 06-02 + 06-03 may add documentation/script commits on top, but the source code (src/, supabase/migrations/, e2e/) MUST remain at the Phase 5 baseline.
- Phase 6 is a DRESS REHEARSAL for Phase 7 production cutover. Anything that goes wrong here is in scope for fixing before Phase 7. Anything that goes right validates DEPLOY-01 + DEPLOY-02.

### §A — Phase A: Provision Supabase prod clone

**What**: Stand up a Supabase project (new or existing-restored) populated with a recent prod data snapshot, schema currently at the pre-merge state (last migration applied = `0023_perf_indexes`).

**Inputs**:
- Supabase account credentials
- Access to the production Supabase project (Auskick Manager prod) for snapshot export
- Decision: NEW project (cleanest isolation; recommended) OR EXISTING staging-project restored (faster if a staging project exists)

**Steps (Path A1 — new project)**:
1. Open Supabase Console → "New Project"
2. Name it e.g. `siren-footy-preview-clone` (the name appears in the project URL)
3. Choose a region close to the prod region (latency parity for honest dress rehearsal)
4. Set a strong DB password; record it in the user's password manager
5. Wait for project provision (~2 min)
6. From the prod project: Database → Backups → "Download" the most recent daily backup (`.sql` file)
7. From the new clone project: Database → Backups → "Restore" → upload the prod `.sql` → confirm
8. After restore completes, the clone now mirrors prod (schema + data + auth).

**Steps (Path A2 — existing staging project, restored)**:
1. Identify the existing staging Supabase project (user-only knowledge — check Vercel project's existing env vars on the "Preview" environment scope; the URL there names the staging project)
2. Repeat steps 6–8 from Path A1, restoring INTO the existing staging project (this OVERWRITES staging data — confirm with user before proceeding)

**Acceptance**:
- From the runbook executor's terminal, with `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` set to the clone's values:
- `supabase link --project-ref <clone-ref>` succeeds (links the merged-trunk repo to the clone)
- `supabase migration list` shows the migration list with 0001..0023 marked as applied (the prod snapshot brought them forward) and 0024..0027 marked as PENDING

**Estimated time**: 10–20 min (mostly waiting on snapshot download/restore)

### §B — Phase B: Apply migrations 0024..0027

**What**: Apply the four net-new migrations on top of the prod-cloned schema, atomically, leaving the clone at the merged-trunk schema state.

**Inputs**:
- Phase A complete (clone has 0001..0023 applied; 0024..0027 pending)
- Local checkout of `merge/multi-sport-trunk` at HEAD `90364ee` or later
- Supabase CLI authenticated and linked to the clone

**Steps (Path B1 — `supabase db push`)**:
1. From repo root with `supabase link --project-ref <clone-ref>` complete:
   - `supabase db push` (applies all pending migrations in order)
2. Watch for the four migrations to land:
   - `0024_multi_sport.sql`
   - `0025_super_admin.sql`
   - `0026_team_quarter_seconds.sql`
   - `0027_game_quarter_seconds.sql`
3. If `supabase db push` errors, fall back to Path B2.

**Steps (Path B2 — manual SQL Editor application)**:
1. Open Supabase Console → SQL Editor for the clone project
2. Open `supabase/migrations/0024_multi_sport.sql` from the local checkout, paste verbatim, run
3. Repeat for `0025_super_admin.sql`, `0026_team_quarter_seconds.sql`, `0027_game_quarter_seconds.sql` IN ORDER
4. After each, manually `INSERT INTO supabase_migrations.schema_migrations` if Supabase didn't auto-record (check Database → Migration History pane)

**Acceptance**:
- `supabase migration list` shows all 27 migrations applied; PENDING column empty
- `node scripts/verify-prod-clone.mjs` exits 0 (Plan 06-03 — runs the Phase 2 §6 queries: count(teams where sport is null) = 0; distinct sport contains 'afl'; etc.)
- Note: Plan 06-03 owns the verify script. Phase B's exit gate is the script returning exit code 0.

**Estimated time**: 2–5 min

### §C — Phase C: Configure Vercel preview env vars

**What**: Set the three Phase-6-CRITICAL env vars on the Vercel project's "Preview" environment scope, pointing at the clone provisioned in Phase A.

**Inputs**:
- Vercel project access (the existing Siren Footy / Auskick Manager Vercel project)
- Supabase clone URL + anon key + service-role key (from Phase A)
- 06-DEPLOY-CHECKLIST.md §1 (the env-var matrix is the authority — don't re-derive)

**Steps (Vercel dashboard)**:
1. Open Vercel dashboard → Project → Settings → Environment Variables
2. Click "Add New"
3. For each of the three vars (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`):
   - Name: exact var name
   - Value: paste from Phase A output
   - Environment: check "Preview" ONLY (do NOT touch Production — that's Phase 7's job)
   - For SUPABASE_SERVICE_ROLE_KEY: confirm Vercel's "Sensitive" toggle is set so the value is masked in the dashboard

**Steps (Vercel CLI alternative)**:
1. `vercel env add NEXT_PUBLIC_SUPABASE_URL preview` → paste value when prompted
2. `vercel env add NEXT_PUBLIC_SUPABASE_ANON_KEY preview` → paste value
3. `vercel env add SUPABASE_SERVICE_ROLE_KEY preview` → paste value

**Acceptance**:
- `vercel env ls preview` lists exactly the three Phase-6-CRITICAL vars (plus any Vercel-injected ones like VERCEL_ENV — those don't appear in `vercel env ls`)
- Optional optional vars (RESEND_*, TELEGRAM_*) deliberately NOT set per 06-DEPLOY-CHECKLIST.md §1 (Phase 6 doesn't exercise contact form or sign-up notification)

**Estimated time**: 5 min

### §D — Phase D: Trigger preview deploy

**What**: Build the merged trunk and deploy it to a Vercel preview URL backed by the env config from Phase C.

**Inputs**:
- Phases A + B + C complete
- `merge/multi-sport-trunk` branch pushed to GitHub remote (or `vercel` CLI logged in locally)

**Steps (Path D1 — git push, preferred)**:
1. From local checkout: `git push origin merge/multi-sport-trunk`
2. Vercel git integration auto-triggers a Preview deploy
3. Watch deploy progress: Vercel dashboard → Deployments tab → newest deploy → "View Function Logs"
4. Build target: `npm install && npm run build` (Vercel default — no override in vercel.json)
5. After ~2-4 min: deploy URL appears (e.g. `siren-footy-<hash>-<gh-user>.vercel.app`)

**Steps (Path D2 — local CLI)**:
1. From repo root with `vercel` CLI logged in:
   - `vercel deploy --target preview` (uploads working tree, builds on Vercel side)
   - OR `vercel build && vercel deploy --prebuilt --target preview` (builds locally, uploads pre-built artefact — faster on slow remote networks)
2. CLI prints preview URL on success

**Acceptance**:
- Deploy status: "Ready" in Vercel dashboard
- Build log shows zero errors (warnings expected — match the 3 pre-existing lint warnings from Phase 5 close per 05-EVIDENCE.md §1; if more warnings appear, investigate)
- Preview URL responds: `curl -fsS <preview-url>/` returns 200 on the marketing landing page

**Estimated time**: 5–10 min (mostly Vercel build wall-clock)

### §E — Phase E: Smoke check

**What**: Confirm the preview is functionally up before handing off to Plan 06-05's manual walk-through.

**Steps**:
1. **Preview alive**:
   - `curl -fsS <preview-url>/` → 200
   - `curl -fsS <preview-url>/login` → 200 (auth route compiles)
   - Open preview URL in browser → marketing landing page renders without console errors
2. **Migrations applied to clone (auto)**:
   - `node scripts/verify-prod-clone.mjs` (with `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` env vars set to the clone's values)
   - Expected exit 0 — all 5 Phase 2 §6 acceptance queries pass against the live clone
3. **Hand-off to manual validation**:
   - Plan 06-05 (06-VALIDATION.md) owns the AFL flow + netball flow + share-link smoke walk-through
   - At this point, Phase 6 plans 06-01..06-04 have done all the autonomous work; the human walks the preview from the validation checklist

### §F — Rollback path

**If Phase B migrations go wrong**:
- Restore the clone Supabase project from a fresh prod snapshot (Phase A all over again)
- Investigate the migration error locally against `npm run db:reset` first; do NOT iterate on the clone

**If Phase D deploy fails to build**:
- Check the Vercel build log; common causes: missing env vars (re-run Phase C), Node version mismatch (Vercel defaults to Node 20+, repo's `package.json:engines` is unset → defaults work)
- Fix locally, push a new commit, Vercel auto-redeploys

**If Phase E smoke surfaces an issue**:
- DO NOT promote to production
- Phase 6 STOPS until the issue is fixed; the Phase 5 baseline is the rollback target (`git checkout 90364ee` in a fresh worktree if needed)
- File a Phase-6-blocker entry in STATE.md "Blockers/Concerns"

**Vercel deploy revert**:
- Vercel dashboard → Deployments → previous green deploy → "Promote to Production" / "Rollback"
- Note: For Phase 6 there's no production promotion; this is purely about backing out a broken preview if it confuses team members. The previous preview deploy automatically becomes the active preview when a new one is rolled back.

### §G — Hand-off

State explicitly:
- After Phase E exits green, Plan 06-04 has completed its autonomous work (the executor was walking the runbook in lock-step with the user).
- Plan 06-05 owns the manual validation walk-through; the executor at that point reads `06-VALIDATION.md` and prompts the user step-by-step through the AFL + netball flows on the live preview URL.
- Phase 6 closes when both 06-04 (deploy) and 06-05 (validation) record PASS in their respective SUMMARYs.

**Tone**: Operational — imperative voice, numbered steps, exact commands. The reader is the human operator at the keyboard. Avoid narrative; favour bullets + code blocks.

**Length target**: ~220–400 lines. Long enough to leave nothing ambiguous; short enough to scan during execution.
  </action>
  <verify>
    <automated>test -f ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md" &amp;&amp; grep -q "Phase A" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md" &amp;&amp; grep -q "Phase B" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md" &amp;&amp; grep -q "Phase C" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md" &amp;&amp; grep -q "Phase D" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md" &amp;&amp; grep -q "Phase E" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md" &amp;&amp; grep -q "Rollback" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md" &amp;&amp; grep -q "supabase db push" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md" &amp;&amp; grep -q "vercel deploy --target preview" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md" &amp;&amp; grep -q "verify-prod-clone" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md" &amp;&amp; grep -q "06-DEPLOY-CHECKLIST" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md" &amp;&amp; grep -q "06-VALIDATION" ".planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md"</automated>
  </verify>
  <done>
- File exists at the canonical path
- All 6 sections (Phase A..E + Rollback) present
- References 06-DEPLOY-CHECKLIST.md (Plan 06-01 hand-off)
- References scripts/verify-prod-clone.mjs (Plan 06-03 hand-off)
- References 06-VALIDATION.md (Plan 06-05 hand-off)
- Both deploy paths documented (`git push` AND `vercel deploy --target preview`)
- Both migration-apply paths documented (`supabase db push` AND manual SQL Editor)
- File is ~220–400 lines (verified by `wc -l`)
- No source code touched (verified by `git status -- src/ scripts/ supabase/ e2e/`)
  </done>
</task>

</tasks>

<verification>

**Phase 5 baseline must remain green** — Plan 06-02 is documentation-only:

- `git status -- src/ scripts/ supabase/ e2e/ package.json vercel.json next.config.mjs` — must be clean
- Only file added: `.planning/phases/06-preview-deploy-and-manual-validation/06-DEPLOY-RUNBOOK.md`

**Source-tree drift bound (HARD):**

- src/ unchanged; scripts/ unchanged; supabase/ unchanged; e2e/ unchanged; vercel.json unchanged; next.config.mjs unchanged; package.json unchanged

**Runbook correctness:**

- All env-var references in §C match 06-DEPLOY-CHECKLIST.md §1 (no drift; do NOT re-derive the matrix here — link to the checklist)
- The verify-prod-clone.mjs reference path is `scripts/verify-prod-clone.mjs` (matches Plan 06-03's frontmatter `files_modified`)
- The hand-off pointer at the end names `06-VALIDATION.md` (Plan 06-05's deliverable file)

**Cross-plan consistency:**

- Plan 06-01 + Plan 06-02 + Plan 06-03 form a self-contained doc set: checklist → runbook → script. Each references the others by exact filename. If renaming any file, all three plans must be updated.

</verification>

<success_criteria>

1. `06-DEPLOY-RUNBOOK.md` exists with sections Phase A through Phase E + Rollback + Hand-off.
2. Each phase has: What / Inputs / Steps / Acceptance / Estimated time (or equivalent shape).
3. Both deploy paths (git push + CLI) are documented under §D.
4. Both migration-apply paths (`supabase db push` + manual SQL Editor) are documented under §B.
5. Hand-offs are explicit: §C points at 06-DEPLOY-CHECKLIST.md; §B + §E point at scripts/verify-prod-clone.mjs; §G points at 06-VALIDATION.md.
6. Source tree byte-identical to Phase 5 close + Plan 06-01 (only the new runbook file appears in `git status`).

</success_criteria>

<output>
After completion, create `.planning/phases/06-preview-deploy-and-manual-validation/06-02-SUMMARY.md` summarising:
- Lines authored in 06-DEPLOY-RUNBOOK.md
- Confirmation all three cross-plan hand-offs are in place (checklist + verify script + validation doc all referenced by exact filename)
- Confirmation the runbook does NOT re-derive the env-var matrix (it points at 06-DEPLOY-CHECKLIST.md §1 — that's the authority)
- Hand-off pointer: Plan 06-03 (verify-prod-clone.mjs) is the next autonomous prep; Plans 06-04 + 06-05 are BLOCKED on user creds.
</output>
