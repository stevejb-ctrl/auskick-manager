---
phase: 11-plan-the-rotation-ahead-of-the-break
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/live/LiveGame.tsx
  - src/components/live/QuarterBreak.tsx
  - src/components/netball/NetballLiveGame.tsx
  - src/components/netball/NetballQuarterBreak.tsx
  - src/components/league/LeagueLiveGame.tsx
  - src/lib/__tests__/nextPeriodPlanSeed.test.ts
  - e2e/tests/plan-ahead-rotation.spec.ts
autonomous: true
requirements: [ROTPLAN-02]

must_haves:
  truths:
    - "During the FINAL MINUTES of a period (resolved sport-agnostically from the live clock, never a hardcoded 'quarter') a coach can open the SAME shared plan-ahead surface from Wave 1 on the NEXT period and build/edit its lineup, across AFL, netball, and rugby league"
    - "The pinned next-period lineup is stored in the Wave-1 plannedRotation slice (nextPeriod* fields) — NO new component, NO new modal, NO new store slice, NO migration"
    - "When the break arrives, each sport's break path opens PRE-SEEDED with the pinned next-period lineup instead of recomputing its own suggestion — AFL QuarterBreak, netball NetballQuarterBreak, rugby-league LeagueLiveGame break path"
    - "A stale/invalid pinned next-period lineup (a player marked out/injured/loaned at the break, or off-field-size changed) is reconciled — invalid players drop to the bench / the suggester fills, never fielded"
    - "The next-period pin is cleared once that period starts (after the break's start commit) so it cannot leak into a later period"
  artifacts:
    - path: "src/lib/__tests__/nextPeriodPlanSeed.test.ts"
      provides: "Red-first cross-sport unit coverage: a pinned next-period lineup seeds the break draft; a stale pin reconciles invalid players out"
      contains: "plannedRotation"
    - path: "e2e/tests/plan-ahead-rotation.spec.ts"
      provides: "e2e build-next-period: pin the next period's lineup during play, reach the break, assert it opens pre-seeded (extends the Wave-1 spec)"
      contains: "next period"
  key_links:
    - from: "src/components/live/LiveGame.tsx"
      to: "src/lib/stores/liveGameStore.ts"
      via: "final-minutes 'plan next period' entry opens the shared GamePlanModal on the next period; on pin write plannedRotation.nextPeriod* (reuses Wave-1 setPlannedRotation)"
      pattern: "nextPeriod"
    - from: "src/components/live/QuarterBreak.tsx"
      to: "src/lib/stores/liveGameStore.ts"
      via: "seed the initial draft from plannedRotation.nextPeriod* when it targets this break; reconcile invalid players; clear the pin on start"
      pattern: "plannedRotation"
    - from: "src/components/netball/NetballQuarterBreak.tsx"
      to: "src/lib/stores/liveGameStore.ts"
      via: "seed initialDraft/lineup from the pinned next-period lineup (mirror AFL)"
      pattern: "plannedRotation"
    - from: "src/components/league/LeagueLiveGame.tsx"
      to: "src/lib/stores/liveGameStore.ts"
      via: "seed the next-period forwards/backs from the pinned lineup at the between-periods break (mirror AFL)"
      pattern: "plannedRotation"
---

<objective>
ROTPLAN-02 / F2: let the coach build the NEXT period's lineup during the
dying minutes of the current one, so they reach the break with a plan
already in place — across ALL sports. This plan REUSES the Wave-1
foundation unchanged (the shared `GamePlanModal` extension, the pure
`projectUpcomingRotation` adapter, and the `plannedRotation` store slice)
and adds only: (a) a "final minutes" entry point on the live surface that
opens the shared modal on the NEXT period, and (b) the consumption side —
each sport's break path opens pre-seeded from the pinned next-period
lineup.

No new modal, no new store slice, no migration (Success Criterion #3 +
reuse-before-fork). AFL is the reference; netball + league mirror.

DECISIONS LOCKED (see 11-CONTEXT.md):
- D-11 reuse the Wave-1 surface; the entry appears during the final
  minutes (derived from the live clock, never hardcoded "quarter"); the
  coach edits the NEXT period tab.
- D-12 honour = each sport's break path opens PRE-SEEDED from
  plannedRotation.nextPeriod* (AFL QuarterBreak draft, netball
  NetballQuarterBreak initialDraft, league between-periods lineup). The
  AFL QuarterBreak `lastAppliedModeRef` guard must not stomp the seed.
- D-13 stale-plan guard at the break: reconcile the pinned lineup
  against current squad/availability/caps (drop invalid → bench / let the
  suggester fill); red-first across all three sports.
- D-14 pin cleared once that period starts.

Purpose: the coach plans the next period before the siren, and the break
opens exactly where they left it — for footy, netball, and league.
Output: a cross-sport "plan next period" entry reusing the Wave-1 surface
+ each break pre-seeding from the validated pin, with red-first unit
coverage (3 sports) + e2e build-next-period.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MATCH-DAY-CHANGES-SPEC.md
@.planning/phases/11-plan-the-rotation-ahead-of-the-break/11-CONTEXT.md
@.planning/phases/11-plan-the-rotation-ahead-of-the-break/11-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- All paths against the MAIN checkout. Use Bash (cd into main checkout) or
     absolute-path Read — file-search tools default to a stale worktree. -->

REUSED FROM WAVE 1 (do not re-create):
- src/lib/game-plan/live.ts — `projectUpcomingRotation(...)` (seed period from
  current state) + `PlannedRotation` type. F2 opens the modal on the NEXT
  period tab and pins the `nextPeriod*` fields.
- src/components/game-plan/GamePlanModal.tsx — already extended with
  `initialPlan` + `onPin` + `initialPeriodIndex` (open on the next period).
- src/lib/stores/liveGameStore.ts — `plannedRotation` slice + setPlannedRotation
  / clearPlannedRotation, already in `partialize`, keyed by gameId. F2 uses the
  `nextPeriodGroups` / `nextPeriodBench` / `nextPeriodIndex` fields (D-06).

"FINAL MINUTES" gate (sport-agnostic, D-11 — never hardcode "quarter"):
- AFL LiveGame already computes msUntilDue + quarterMs (:1096-1118) and
  `periodPhase(currentQuarter, ageGroup.periodCount, ...)` (:1027). Derive a
  "final window" predicate (e.g. quarter elapsed >= quarterMs - WINDOW, or the
  last sub interval of the period) and gate the entry's visibility on it. Use
  the SAME math each sport already has for its clock; resolve the period noun
  via periodPhase, not a literal.
- Do NOT show the "plan next period" entry on the LAST period (no next period
  to plan) — periodPhase isLastPeriod.

AFL break consumer (REFERENCE; src/components/live/QuarterBreak.tsx):
- `draft`/`setDraft` (:255), `lineupMode` (:280), `suggestedLineup` (:406),
  `manualLineup` (:458), and the critical `lastAppliedModeRef` guard effect
  (:484-497) that only re-derives draft on an EXPLICIT mode change. Seed `draft`
  from plannedRotation.nextPeriod* for THIS upcoming period (convert
  groupId->zone playerIds into a Lineup), and make the guard treat the pinned
  seed as the initial applied state so it isn't immediately overwritten by
  suggestedLineup. Add a "planned" mode/indication (D-15 visible). `nextQuarter`
  / the period this break leads into is already in scope to match
  `nextPeriodIndex`.
- handleConfirmStart (:990) commits recordLineupSet + startQuarterAction — after
  a successful start, call clearPlannedRotation() (D-14).

Netball break consumer (MIRROR; src/components/netball/):
- NetballLiveGame.tsx mounts <NetballQuarterBreak> (:1711); it already supports
  `initialDraft`/`initialLineup` seeding (:165-172, :250, :1506). Seed those
  from plannedRotation.nextPeriod* (positions are the groupIds). Clear the pin
  after the netball start commit.
- NetballQuarterBreak.tsx — the reshuffle/draft surface; honour the seeded
  initial lineup the same way AFL does.

Rugby-league break consumer (MIRROR; src/components/league/LeagueLiveGame.tsx):
- Between-periods section: suggestLeagueSubs (:516), "Ready for {period} {n+1}"
  CTA (:1631-1639), setStartPeriodConfirmOpen, the start-period commit path.
  Seed the next-period forwards/backs from plannedRotation.nextPeriod*
  (groupIds "forwards"/"backs"); reconcile against current availability; clear
  the pin after the start commit. League has no separate break component — the
  seed lands in this file's between-periods lineup state.

STALE-PLAN RECONCILE (D-13): reuse the same availability semantics the break
already applies (injured/loaned/out → bench). A pinned player who is no longer
available for the upcoming period must NOT be fielded; drop them to bench and
let the existing suggester/draft fill the slot. This mirrors the Wave-1
resolveHonouredSwaps guard, applied to a full lineup instead of swap pairs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — cross-sport next-period seed + stale-reconcile unit spec</name>
  <read_first>
    - src/lib/__tests__/plannedRotationHonour.test.ts (Wave-1 honour spec — mirror its fixture style + the PlannedRotation shape)
    - The Wave-1 PlannedRotation type + any pure seed/reconcile helper exported from src/lib/game-plan/live.ts
    - src/components/live/QuarterBreak.tsx:406-497 (how draft/lineupMode/suggestedLineup interact) + src/lib/types.ts Lineup shape (zones + bench) — to know the target seed shape for AFL
    - src/lib/sports/netball/fairness.ts (GenericLineup positions shape) + src/components/league/LeagueLiveGame.tsx forwards/backs lineup shape — the netball + league seed targets
  </read_first>
  <action>
Create src/lib/__tests__/nextPeriodPlanSeed.test.ts. Extract the seed/reconcile
decision as a PURE helper (implement in Task 2, e.g.
`seedNextPeriodLineup({ pin, periodIndex, availableIds, groupIds })` returning a
sport-neutral groupId->playerIds + bench, with invalid players reconciled out).
Cover, RED-first:
  - SEED MATCH: a pin with nextPeriodIndex === the upcoming period and all
    players available → the helper returns the pinned groups/bench unchanged
    (the break should open on exactly this lineup).
  - WRONG-PERIOD pin: nextPeriodIndex !== upcoming period → helper returns null
    / "no seed" so the break falls back to its own suggestion.
  - STALE pin (D-13): a pinned on-field player is now unavailable
    (injured/loaned/out) → the helper drops them to bench (or omits them) so
    they are NOT in any on-field group; a still-valid teammate remains placed.
  - Shape parity across sports: run the same assertions for AFL zone groupIds,
    netball position groupIds, and league forwards/backs groupIds (parameterise).
Reference `plannedRotation` in the spec. Write RED-FIRST (helper absent →
fails). Commit: `test(11-02): add failing cross-sport next-period seed + reconcile spec`.
  </action>
  <verify>
    <automated>cd "/c/Users/steve/OneDrive/Documents/Auskick manager" && npx vitest run src/lib/__tests__/nextPeriodPlanSeed.test.ts 2>&1 | tail -25; echo "EXPECT: RED — seed/reconcile helper not implemented yet"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/__tests__/nextPeriodPlanSeed.test.ts` exists, references `plannedRotation`, and covers seed-match, wrong-period (no seed), and stale-reconcile for AFL + netball + league groupIds
    - The spec FAILS against current code (RED)
    - Commit message matches `test(11-02):`
  </acceptance_criteria>
  <done>Red-first cross-sport spec for the next-period seed + stale-reconcile helper is committed and fails against pre-implementation code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN (AFL reference) — final-minutes "plan next period" entry + QuarterBreak pre-seed</name>
  <read_first>
    - src/lib/game-plan/live.ts (the Wave-1 adapter + PlannedRotation + honour helper — add the pure seedNextPeriodLineup helper here next to them)
    - src/components/live/LiveGame.tsx:1027 (periodPhase), :1096-1118 (clock/quarterMs math for the final-minutes predicate), :1245-1268 (where <QuarterBreak> mounts + props), and the Wave-1 "plan ahead" entry from 11-01 (reuse its modal-open wiring, just target the NEXT period)
    - src/components/live/QuarterBreak.tsx:255-497 (draft/lineupMode/suggestedLineup/lastAppliedModeRef) + :990-1027 (handleConfirmStart) + how props arrive from LiveGame
    - src/lib/types.ts (Lineup) + src/lib/stores/liveGameStore.ts (plannedRotation actions)
    - the Task-1 spec (seedNextPeriodLineup contract)
  </read_first>
  <action>
STEP A — implement the pure `seedNextPeriodLineup(...)` in
src/lib/game-plan/live.ts per the Task-1 contract (seed-match / wrong-period /
stale-reconcile). Export via the barrel.

STEP B — LiveGame (AFL final-minutes entry): add a "Plan next {period}" entry on
the live surface, visible ONLY when the derived final-minutes predicate is true
AND `!isLastPeriod` (periodPhase). On tap, open the SAME shared GamePlanModal
(Wave 1) seeded via `projectUpcomingRotation(...)` with `initialPeriodIndex` =
the NEXT period (so the modal opens on that tab). On pin →
`setPlannedRotation({ gameId, nextPeriodIndex, nextPeriodGroups, nextPeriodBench
})` derived from the edited next-period of the plan. One-handed reachable
(D-15); match the rhythm of the Wave-1 F1 entry.

STEP C — QuarterBreak pre-seed: when `plannedRotation` targets THIS upcoming
period (nextPeriodIndex === nextQuarter-equivalent) and gameId matches, seed the
initial `draft` from `seedNextPeriodLineup(...)` (converted to a Lineup) and set
a "planned" mode so `lastAppliedModeRef` treats it as the applied initial state
(NOT overwritten by suggestedLineup on mount). Show a visible "planned"
indication. Reconcile invalid players to bench (D-13). After
handleConfirmStart's successful start, call `clearPlannedRotation()` (D-14).

Run nextPeriodPlanSeed.test.ts → GREEN (AFL cases). Commit:
`feat(11-02): AFL plan-next-period entry + QuarterBreak pre-seed from pinned plan`.
  </action>
  <verify>
    <automated>cd "/c/Users/steve/OneDrive/Documents/Auskick manager" && npx tsc --noEmit && npx vitest run src/lib/__tests__/nextPeriodPlanSeed.test.ts -t AFL 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `seedNextPeriodLineup` is exported from src/lib/game-plan/live.ts and is pure (no supabase/react imports)
    - LiveGame shows the "plan next period" entry only under the final-minutes predicate AND not on the last period (grep periodPhase / isLastPeriod near the entry), opens the shared GamePlanModal on the NEXT period (`initialPeriodIndex`), and pins via `setPlannedRotation` with nextPeriod* fields
    - QuarterBreak seeds `draft` from `plannedRotation` when it targets this break and does NOT let `lastAppliedModeRef`/suggestedLineup stomp it (grep plannedRotation in QuarterBreak.tsx); clears the pin after a successful start
    - No new modal/component file created (reuse-before-fork)
    - `npx tsc --noEmit` exits 0; AFL cases in nextPeriodPlanSeed.test.ts PASS
    - Commit message matches `feat(11-02):`
  </acceptance_criteria>
  <done>AFL reference: the coach can build the next quarter's lineup in the dying minutes and the QuarterBreak opens pre-seeded with it (invalid players reconciled); pin cleared on start.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN (mirror) — netball + rugby-league pre-seed + DoD gates</name>
  <read_first>
    - src/components/netball/NetballLiveGame.tsx (final-minutes entry equivalent + how it mounts NetballQuarterBreak at :1711 with initialDraft/initialLineup :165-172/:1506) + src/components/netball/NetballQuarterBreak.tsx (draft/reshuffle surface)
    - src/components/league/LeagueLiveGame.tsx (between-periods section :1631-1690, suggestLeagueSubs :516, the start-period commit path) — league has NO separate break component
    - src/components/live/QuarterBreak.tsx (the AFL reference seeding from Task 2 — mirror its rhythm)
    - the Task-1 spec netball + league cases
    - playwright.config.ts (Phase-9 environmental protocol)
  </read_first>
  <action>
STEP A — netball: add the same final-minutes "plan next period" entry to
NetballLiveGame (gate via periodPhase, never hardcode "quarter"; not on the last
period), opening the shared GamePlanModal on the next period and pinning
nextPeriod* (positions are groupIds). Seed NetballQuarterBreak's
`initialDraft`/`initialLineup` from `seedNextPeriodLineup(...)` when the pin
targets the upcoming period; reconcile invalid players; clear the pin after the
netball start commit. Mirror AFL's rhythm/indication.

STEP B — rugby league: add the entry to LeagueLiveGame's live surface (same
gate); pin nextPeriod* (groupIds "forwards"/"backs"). At the between-periods
break, seed the next-period forwards/backs lineup from the pin (reconciled),
instead of only suggestLeagueSubs; clear the pin after the start-period commit.

STEP C — run nextPeriodPlanSeed.test.ts (all three sports) → GREEN. Extend
e2e/tests/plan-ahead-rotation.spec.ts with a build-next-period case (pin the
next period during play → reach the break → assert the break opens pre-seeded
with the pinned lineup). Then run ALL FOUR DoD gates: `npx tsc --noEmit`,
`npm run lint`, `npm test`, `npm run e2e` (Phase-9 environmental protocol if the
full multi-worker run is flaky — isolate impacted specs with `--workers=1`,
record the caveat in the SUMMARY). Update only specs whose behaviour genuinely
shifted; do not loosen.
Commit: `feat(11-02): mirror plan-next-period pre-seed into netball + league + DoD gates`.
  </action>
  <verify>
    <automated>cd "/c/Users/steve/OneDrive/Documents/Auskick manager" && npx tsc --noEmit && npm run lint && npm test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - NetballLiveGame + LeagueLiveGame each show the final-minutes "plan next period" entry (gated by periodPhase, not a hardcoded quarter; hidden on the last period) and pin nextPeriod* via the shared setPlannedRotation
    - NetballQuarterBreak seeds its initialDraft/initialLineup from `plannedRotation`; LeagueLiveGame seeds the between-periods forwards/backs from it (grep plannedRotation in both files); both reconcile invalid players and clear the pin after start
    - All three sports' cases in nextPeriodPlanSeed.test.ts PASS
    - `e2e/tests/plan-ahead-rotation.spec.ts` includes a build-next-period assertion (break opens pre-seeded)
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0; `npm test` exits 0; `npm run e2e` green (or impacted specs green in isolation with the Phase-9 caveat in the SUMMARY)
    - Commit message matches `feat(11-02):`
  </acceptance_criteria>
  <done>Netball + rugby-league break paths open pre-seeded from the pinned next-period lineup (reconciled), mirroring AFL; all three unit cases + the build-next-period e2e GREEN; all four DoD gates green (e2e per Phase-9 protocol if needed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coach edits next period -> plannedRotation (client store) | Reuses the Wave-1 client-local, gameId-keyed slice; no server trust, no new network input. |
| plannedRotation.nextPeriod* -> break draft -> recordLineupSet | The pin only seeds what the break SHOWS; the actual next-period lineup is committed through the existing RLS-guarded recordLineupSet/start actions, only after the coach taps "Start period". |
| stale next-period pin -> fielded lineup | A pin authored minutes earlier may include a player who is marked out/injured/loaned at the break, or be invalid if on-field-size changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-A | Tampering / Integrity (stale next-period plan) | seedNextPeriodLineup + each break seed | mitigate | D-13 reconcile: the pinned lineup is validated against current availability/caps before seeding; invalid players drop to bench and the suggester fills, so an unavailable player is never fielded. Red-first nextPeriodPlanSeed.test.ts covers the stale-reconcile case for all three sports. |
| T-11-02-B | Elevation / unauthorized mutation | recordLineupSet via seeded break | accept | No new server action. The next-period lineup is committed only through the existing per-sport start/lineup_set actions (auth/RLS enforced) and only on the coach's explicit "Start period" tap. The pin is advisory. |
| T-11-02-C | Cross-game / cross-period bleed | plannedRotation.nextPeriod* | mitigate | gameId-keyed (Wave 1) + `nextPeriodIndex` must match the upcoming period for the seed to apply (wrong-period → no seed); the pin is cleared once that period starts (D-14). A stale pin can't seed a different game or a later period. |
| T-11-02-D | Correctness regression | QuarterBreak lastAppliedModeRef / netball/league seeding | mitigate | The seed integrates with each break's existing draft/mode machinery (AFL `lastAppliedModeRef` treats the seed as the applied initial state; netball reuses `initialDraft`; league reuses its between-periods lineup state) so no flicker/overwrite regression. Covered by the build-next-period e2e + existing break specs. |

No migration, no schema change, no new auth surface, no new game-event type, no
new store slice — all additive reuse of the Wave-1 foundation.
</threat_model>

<verification>
- During the final minutes (sport-agnostically resolved, never hardcoded
  "quarter") and not on the last period, the coach can open the SAME Wave-1
  surface on the next period and build its lineup — AFL, netball, league.
- Each sport's break opens PRE-SEEDED from the pinned next-period lineup
  (QuarterBreak draft / NetballQuarterBreak initialDraft / LeagueLiveGame
  between-periods), not a fresh suggestion (nextPeriodPlanSeed.test.ts GREEN ×3
  + build-next-period e2e GREEN).
- A stale/invalid pinned lineup is reconciled (invalid players benched, never
  fielded); the pin is cleared once the period starts.
- No new component/modal/store-slice/migration — reuse-before-fork honoured.
- All four DoD gates green (e2e per the Phase-9 environmental protocol if the
  full multi-worker run is flaky on this box).
</verification>

<success_criteria>
ROADMAP Phase 11 criteria #2, #3, #4 (F2 slice) satisfied:
- #2: during the final minutes of a period a coach can build/preview the next
  period's lineup, arriving at the break with a plan in place, across all sports.
- #3: F2 shares the ONE "edit an upcoming rotation" surface from Wave 1 (no
  forked per-sport modal; reuse of projectUpcomingRotation + GamePlanModal +
  plannedRotation).
- #4: the control is one-handed reachable on the live surface and the e2e spec
  exercises build-next-period through the UI.
(Criterion #1 — override the imminent sub — is delivered by plan 11-01.)
</success_criteria>

<output>
After completion, create `.planning/phases/11-plan-the-rotation-ahead-of-the-break/11-02-SUMMARY.md`
</output>
