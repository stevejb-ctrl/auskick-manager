---
phase: 04-netball-verification-on-merged-trunk
plan: 07
subsystem: gauntlet-and-handoff
tags: [gauntlet, regression, phase-handoff, NETBALL-01, NETBALL-02, NETBALL-03, NETBALL-04, NETBALL-05, NETBALL-06, NETBALL-07, NETBALL-08, ABSTRACT-03, TEST-05]
type: execute
wave: 6
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05, 04-06]
files_modified:
  - .planning/phases/04-netball-verification-on-merged-trunk/04-EVIDENCE.md
autonomous: false
requirements: [NETBALL-01, NETBALL-02, NETBALL-03, NETBALL-04, NETBALL-05, NETBALL-06, NETBALL-07, NETBALL-08, ABSTRACT-03, TEST-05]
tech_stack: [typescript, playwright, vitest, eslint, supabase, git]
key_files:
  - .planning/phases/04-netball-verification-on-merged-trunk/04-EVIDENCE.md (new — Phase 4 final evidence ledger)
  - All Phase 4 SUMMARYs (read-only — 04-01..06)
  - Phase 3 03-MERGE-LOG.md (read-only — invariants to re-check)
key_decisions:
  - D-CONTEXT-test-coverage-scope (gauntlet runs the full e2e suite to catch any AFL regressions caused by 04-04's source changes)
  - D-CONTEXT-failure-handling (gaps surfaced here are gap-closure plans for re-execution; non-blocker polish becomes Phase 5 input)
  - The Phase 5 hand-off needs explicit confirmation that all 8 NETBALL-N + ABSTRACT-03 acceptance gates have an automated assertion, and an explicit ledger of TEST-05's Kotara state
requirements_mapping:
  NETBALL-01: covered by netball-live-flow.spec.ts (state-machine entry tests)
  NETBALL-02: covered by netball-quarter-break.spec.ts + Vitest netballFairness.test.ts
  NETBALL-03: covered by netball-live-flow.spec.ts (goal flow + undo tests)
  NETBALL-04: covered by netball-walkthrough.spec.ts + netball-summary.spec.ts + netball-live-flow.spec.ts (live-shell mirror) + plan 04-04 source fix + checkpoint
  NETBALL-05: covered by netball-stats.spec.ts
  NETBALL-06: covered by netball-summary.spec.ts (after 04-04 source fix)
  NETBALL-07: covered by netball-walkthrough.spec.ts + Vitest netballWalkthroughSteps.test.ts
  NETBALL-08: covered by netball-live-flow.spec.ts (long-press / replacement / late-arrival)
  ABSTRACT-03: covered by netball-live-flow.spec.ts (team + game quarter_length_seconds override tests)
  TEST-05: state recorded — Kotara presence audited by 04-01's helper; either fully present (PASS) or absent with a noted Phase 5 follow-up
metrics:
  files_touched: 1
  context_estimate: 22%

must_haves:
  truths:
    - "`npx tsc --noEmit` exits 0 on the merged trunk"
    - "`npm run lint` exits 0"
    - "`npm test` exits 0 (all Vitest unit tests green, including netballFairness + netballWalkthroughSteps)"
    - "`npm run e2e` exits 0 (full Playwright suite, all specs green, with PROD-04 fixme intact and no specs accidentally un-fixme'd)"
    - "Every NETBALL-N (1..8) acceptance gate has at least one automated test asserting it"
    - "Phase 3 invariants are intact: pre-merge tags frozen, PROD-04 fixme present, D-26/D-27 quarterMs wiring sites unchanged"
    - "TEST-05 state recorded: Kotara Koalas present OR absent-with-clear-Phase-5-followup-note"
  artifacts:
    - path: ".planning/phases/04-netball-verification-on-merged-trunk/04-EVIDENCE.md"
      provides: "Phase 4 final evidence ledger with full gauntlet output, NETBALL-N×ABSTRACT-03×TEST-05 traceability table, and Phase 5 hand-off"
      min_lines: 200
      contains: "NETBALL-01|NETBALL-08|ABSTRACT-03|gauntlet"
  key_links:
    - from: "04-EVIDENCE.md gauntlet section"
      to: "Full e2e + tsc + lint + Vitest output"
      via: "Direct command output captured + per-spec PASS/FAIL counts"
      pattern: "tsc.*0|lint.*0|Vitest.*passed|Playwright.*passed"
    - from: "04-EVIDENCE.md NETBALL-N traceability"
      to: "Each requirement → one or more spec files + one or more automated test names"
      via: "Table mapping requirement ID → file:test_name"
      pattern: "NETBALL-0[1-8]"
---

<objective>
Run the full Phase 4 gauntlet — `tsc --noEmit && npm test && npm run lint && npm run e2e` — against the merge trunk's HEAD after plans 04-01..06 ship, and capture an EVIDENCE.md that:

1. Records pass/fail of each automated suite,
2. Maps every NETBALL-N + ABSTRACT-03 + TEST-05 acceptance gate to the spec(s) and Vitest contract(s) that prove it,
3. Re-verifies the Phase 3 hand-off invariants (pre-merge tags untouched, PROD-04 fixme intact, D-26/D-27 sites unchanged),
4. Notes any gaps that surface during the gauntlet for Phase 5 to pick up.

This is the Phase 4 → Phase 5 hand-off plan. Per CONTEXT D-CONTEXT-failure-handling, blocker bugs found here must be fixed in-Phase (which means a gap-closure cycle re-running plan 04-04..06 as needed); non-blocker polish becomes Phase 5 input.

Output: `.planning/phases/04-netball-verification-on-merged-trunk/04-EVIDENCE.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-netball-verification-on-merged-trunk/04-CONTEXT.md
@.planning/phases/03-branch-merge-abstraction-integrity/03-MERGE-LOG.md
@.planning/phases/04-netball-verification-on-merged-trunk/04-01-SUMMARY.md
@.planning/phases/04-netball-verification-on-merged-trunk/04-02-SUMMARY.md
@.planning/phases/04-netball-verification-on-merged-trunk/04-03-SUMMARY.md
@.planning/phases/04-netball-verification-on-merged-trunk/04-04-SUMMARY.md
@.planning/phases/04-netball-verification-on-merged-trunk/04-05-SUMMARY.md
@.planning/phases/04-netball-verification-on-merged-trunk/04-06-SUMMARY.md
@CLAUDE.md
@e2e/helpers/seed-audit.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full gauntlet and capture raw output</name>
  <files>.planning/phases/04-netball-verification-on-merged-trunk/04-EVIDENCE.md (created in Task 2)</files>
  <action>
Run each command and tee the output to a temp log so it can be embedded in EVIDENCE.md:

```
npx tsc --noEmit 2>&1 | tee /tmp/04-07-tsc.log
npm run lint 2>&1 | tee /tmp/04-07-lint.log
npm test -- --run 2>&1 | tee /tmp/04-07-vitest.log
npm run e2e 2>&1 | tee /tmp/04-07-e2e.log
```

For each: capture exit code (`echo $?`) immediately after, before the next command runs.

If `npm run e2e` fails: scan the failure list. If it's a track_scoring or netball test that should be green per plans 04-04..06, this is a gap — escalate to a gap-closure cycle (re-run the relevant plan with the failure as input). Per CONTEXT D-CONTEXT-failure-handling.

Re-run the per-spec PROD-01 gauntlet from Phase 3 §5 to confirm AFL post-fork fix specs still green:
```
npm run e2e -- --workers=1 e2e/tests/availability.spec.ts e2e/tests/injury-replacement.spec.ts e2e/tests/live-swaps.spec.ts e2e/tests/live-scoring.spec.ts e2e/tests/live-quarters.spec.ts 2>&1 | tee /tmp/04-07-prod01.log
```
This re-validates PROD-01 carry-forward — Phase 4 must not have broken any AFL post-fork fix.

Re-verify Phase 3 invariants:
```
git rev-parse pre-merge/main
git rev-parse pre-merge/multi-sport
grep -c "test\\.fixme" e2e/tests/playhq-import.spec.ts
grep -nE 'quarterMs' src/components/live/LiveGame.tsx | wc -l
grep -nE 'quarterMs' src/lib/stores/liveGameStore.ts | wc -l
```
Expected outputs (per Phase 3 MERGE-LOG):
- `pre-merge/main` = `e9073dd205bdd8eae8e7b66097e3b2275c4b5958`
- `pre-merge/multi-sport` = `e13e787cb8abe405c18aca73e66c7c928eb359d8`
- `test.fixme` count in playhq-import.spec.ts = 1
- `quarterMs` hits in LiveGame.tsx ≥ 5
- `quarterMs` hits in liveGameStore.ts ≥ 4

Run the Kotara Koalas audit one final time so the EVIDENCE.md captures current state:
```typescript
// Run as a one-off script or via npx tsx
import { createAdminClient } from "./e2e/fixtures/supabase";
import { auditKotaraKoalas } from "./e2e/helpers/seed-audit";
const admin = createAdminClient();
console.log(await auditKotaraKoalas(admin));
```

Capture the result; it goes into the TEST-05 row of the traceability table in Task 2.
  </action>
  <verify>
    <automated>test -f /tmp/04-07-tsc.log && test -f /tmp/04-07-lint.log && test -f /tmp/04-07-vitest.log && test -f /tmp/04-07-e2e.log && test -f /tmp/04-07-prod01.log</automated>
  </verify>
  <done>
All 5 logs exist on disk. Each command's exit code captured. The Kotara audit result is recorded. Phase 3 invariants checked. Any failures triggered a gap-closure cycle BEFORE proceeding to Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 04-EVIDENCE.md with gauntlet output + traceability table + hand-off</name>
  <files>.planning/phases/04-netball-verification-on-merged-trunk/04-EVIDENCE.md</files>
  <action>
Create the evidence file with these sections (use the Write tool — do NOT cat/heredoc):

```markdown
# Phase 4 Evidence: Netball verification on merged trunk

**Phase:** 04-netball-verification-on-merged-trunk
**Closed:** {date}
**Branch:** merge/multi-sport-trunk @ {git rev-parse HEAD}
**Phase 3 hand-off branch HEAD (start of Phase 4):** bd8761f

## §1 Full gauntlet output

### `npx tsc --noEmit`
- Exit code: {0/1}
- Output: {first 30 lines of /tmp/04-07-tsc.log; "[empty]" if log is empty meaning success}

### `npm run lint`
- Exit code: {0/1}
- Output: {first 30 lines of /tmp/04-07-lint.log}

### `npm test --run` (Vitest)
- Exit code: {0/1}
- Tests passed / failed / skipped: {N / 0 / 0}
- New Vitest tests added in Phase 4: 0 (all pure-function netball logic was already covered by netballFairness.test.ts + netballWalkthroughSteps.test.ts on the multi-sport branch; Phase 4 added e2e coverage only)
- Output tail: {last 20 lines of /tmp/04-07-vitest.log}

### `npm run e2e` (Playwright — full suite)
- Exit code: {0/1}
- Specs total: {N}
- Tests passed / failed / skipped: {N / 0 / K}
- Per-spec breakdown: {extract from /tmp/04-07-e2e.log — list each spec with PASS/FAIL count}
- Phase 4 new specs: netball-walkthrough.spec.ts (4), netball-stats.spec.ts (2), netball-summary.spec.ts (2), netball-live-flow.spec.ts (9-11), netball-quarter-break.spec.ts (4)

### PROD-01 per-spec re-run (workers=1)
- Each AFL post-fork fix spec rerun, confirming Phase 4's source fix did not regress AFL flows.
- {table from /tmp/04-07-prod01.log}

## §2 NETBALL-N × ABSTRACT-03 × TEST-05 traceability

| Requirement | Acceptance gate | Spec file → test name(s) | Status |
|-------------|-----------------|--------------------------|--------|
| NETBALL-01 | Six-state machine + countdown + auto-end | e2e/tests/netball-live-flow.spec.ts → "pre-kickoff renders the netball lineup picker", "live state renders score bug + court + bench when Q1 is in progress" | PASS / FAIL |
| NETBALL-02 | 5 fairness tiers + tie-breaks | e2e/tests/netball-quarter-break.spec.ts → 4 tests; src/lib/__tests__/netballFairness.test.ts (Vitest pure-function contract) | PASS / FAIL |
| NETBALL-03 | Goal flow + 8s undo + persistent chip | e2e/tests/netball-live-flow.spec.ts → "tapping GS opens confirm sheet…" + "undo writes score_undo event…" | PASS / FAIL |
| NETBALL-04 | track_scoring=false suppression on every surface | e2e/tests/netball-walkthrough.spec.ts (track_scoring=false test); e2e/tests/netball-summary.spec.ts (track_scoring=false test); e2e/tests/netball-live-flow.spec.ts (live-shell mirror tests); plan 04-04 human checkpoint | PASS / FAIL |
| NETBALL-05 | 5 stats sections + AFL aggregator non-leak | e2e/tests/netball-stats.spec.ts → 2 tests | PASS / FAIL |
| NETBALL-06 | Summary card with track_scoring gates | e2e/tests/netball-summary.spec.ts → 2 tests | PASS / FAIL |
| NETBALL-07 | Walkthrough first-visit + persistence + scoring step gate | e2e/tests/netball-walkthrough.spec.ts → 4 tests; src/lib/__tests__/netballWalkthroughSteps.test.ts (Vitest) | PASS / FAIL |
| NETBALL-08 | Long-press + mid-quarter replacement + late-arrival | e2e/tests/netball-live-flow.spec.ts → "long-press on a court player opens NetballPlayerActions modal", "marking a court player injured prompts replacement…", "late arrival adds a previously-unavailable squad member…" | PASS / FAIL |
| ABSTRACT-03 | quarter_length_seconds priority chain (game → team → ageGroup) | e2e/tests/netball-live-flow.spec.ts → "team.quarter_length_seconds override fires auto-hooter…", "game.quarter_length_seconds OVERRIDES team.quarter_length_seconds" | PASS / FAIL |
| TEST-05 | Kotara Koalas seed present + usable | e2e/helpers/seed-audit.ts (audit invoked at start of Phase 4 + end of Phase 4); netball-quarter-break.spec.ts Kotara-optional test | PRESENT / ABSENT-FOLLOWUP-NOTED |

## §3 Phase 3 invariants — re-verified at end of Phase 4

| Invariant | Expected (from Phase 3 close) | Actual at Phase 4 close | Status |
|-----------|-------------------------------|--------------------------|--------|
| `pre-merge/main` SHA | e9073dd205bdd8eae8e7b66097e3b2275c4b5958 | {git rev-parse pre-merge/main} | UNCHANGED / DRIFTED |
| `pre-merge/multi-sport` SHA | e13e787cb8abe405c18aca73e66c7c928eb359d8 | {git rev-parse pre-merge/multi-sport} | UNCHANGED / DRIFTED |
| PROD-04: `test.fixme` count in playhq-import.spec.ts | 1 | {grep -c "test\\.fixme" e2e/tests/playhq-import.spec.ts} | UNCHANGED |
| D-26/D-27: `quarterMs` hits in LiveGame.tsx | ≥5 | {count} | UNCHANGED |
| D-26/D-27: `quarterMs` hits in liveGameStore.ts | ≥4 | {count} | UNCHANGED |
| ABSTRACT-01: AFL conditionals outside src/lib/sports/ | 4 (UI-presentation only — pre-classified acceptable) | {grep -count} | UNCHANGED |

## §4 Source-tree changes in Phase 4 (`src/` diff vs Phase 3 close `bd8761f`)

`git diff --stat bd8761f..HEAD -- src/`:

```
{output of git diff --stat bd8761f..HEAD -- src/}
```

Expected: ONLY plan 04-04 modifications — `src/components/netball/NetballLiveGame.tsx`, `src/components/netball/NetballGameSummaryCard.tsx`, `src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx`, optionally `src/app/run/[token]/page.tsx`. No other `src/` drift.

## §5 Phase 5 hand-off

### Final state at Phase 4 close

| Item | Value |
|------|-------|
| Branch | `merge/multi-sport-trunk` |
| HEAD SHA | {git rev-parse HEAD} |
| pre-merge/main | UNCHANGED |
| pre-merge/multi-sport | UNCHANGED |
| Migration set | 27 (unchanged from Phase 3) |
| Source-tree state | tsc green, lint green, Vitest green, Playwright green |
| New e2e specs added in Phase 4 | 5 (netball-walkthrough, netball-stats, netball-summary, netball-live-flow, netball-quarter-break) |
| New helper added in Phase 4 | `e2e/helpers/seed-audit.ts` |
| `.gitignore` update | playwright-report/ + playwright/ + test-results/ added |
| Source code modified in Phase 4 | NetballLiveGame.tsx + NetballGameSummaryCard.tsx + live/page.tsx (+ run/[token]/page.tsx if it dispatches netball) — track_scoring prop wired through |
| NETBALL-01..08 status | All 8 acceptance gates have automated coverage, all green |
| ABSTRACT-03 status | Verified end-to-end via netball-live-flow quarter-length override tests |

### What Phase 5 should know

- **Phase 5's job is the full-gauntlet green sweep + Kotara Koalas seed.** All NETBALL-N functional gates are covered by Phase 4. Phase 5's incremental work is:
  - Re-run the gauntlet on a clean local DB (`db:reset` first) to confirm no DB-state drift
  - Address the three Phase 3 deferred side-findings (#2 stale-dev-server detection in scripts/e2e-setup.mjs, #3 admin-membership hydration helper) — both are in Phase 5 scope per CONTEXT D-CONTEXT-side-finding-triage
  - Resolve TEST-05 if the Kotara Koalas seed was absent at Phase 4 close: either re-seed, document a clear seed-creation path, or escalate to a deferred item

- **Three side-findings deferred to Phase 5** (carry forward from Phase 3):
  1. `scripts/e2e-setup.mjs` stale-dev-server detection (lsof on port 3000 + reuse-or-abort)
  2. `await waitForAdminHydration(page)` Playwright fixture (extract from settings/roster/game-edit specs' duplicated boilerplate)
  3. (Reminder) Phase 3 ABSTRACT-01 CI guard + PROD-04 fixme presence check are still backlog — Phase 5 may pick up if scope allows

- **Pause-event persistence bug** still deferred (cross-cutting, neither AFL nor netball blocked by it for v1).
- **`games.quarter_length_seconds` UI exposure** still deferred to v2 (the column works end-to-end per ABSTRACT-03 verification, but there's no team-edit UI to set it; admin can set via DB or factories — sufficient for current coach workflow per CONTEXT.md scope).

### Open questions / known gaps

{Capture any failures, flaky tests, or issues observed during the gauntlet. If everything green, write "None — all gates green; ready for Phase 5".}

---

*Phase 4 closed {date}. Ready to dispatch `/gsd-execute-phase 5` on `merge/multi-sport-trunk`.*
```

Replace every `{...}` placeholder with the captured values. The traceability table's PASS/FAIL column comes from the gauntlet output (Task 1's logs). The git rev-parse outputs go into the invariant table verbatim.

DO NOT commit. The orchestrator commits Phase 4 in the closing step per CONTEXT instructions.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-netball-verification-on-merged-trunk/04-EVIDENCE.md && wc -l .planning/phases/04-netball-verification-on-merged-trunk/04-EVIDENCE.md | awk '{print $1}' | xargs -I{} test {} -ge 200</automated>
  </verify>
  <done>
04-EVIDENCE.md exists, ≥200 lines, with all five sections populated. Every NETBALL-N row has a PASS or FAIL marker (no placeholders). Phase 3 invariants table populated with actual SHAs and counts. Phase 5 hand-off section names the next steps. Source-tree diff captured.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plan 04-07 ran the full Phase 4 gauntlet and produced 04-EVIDENCE.md. Every NETBALL-N + ABSTRACT-03 + TEST-05 acceptance gate is mapped to its automated coverage with a PASS/FAIL marker.

Automated evidence:
- tsc + lint + Vitest + Playwright full suite all green (or gaps captured for Phase 5)
- PROD-01 per-spec re-run all green
- Phase 3 invariants intact (tags + PROD-04 fixme + D-26/D-27 sites + ABSTRACT-01 4 matches)
- Source-tree changes limited to plan 04-04's 3-4 files
- TEST-05 state recorded
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/04-netball-verification-on-merged-trunk/04-EVIDENCE.md`.
2. Confirm every NETBALL-N row in §2 has a PASS marker (or, if FAIL, that the failure has been escalated to a gap-closure cycle and resolved).
3. Confirm §3 invariants table shows UNCHANGED in every row.
4. Confirm §4 src/ diff shows only plan 04-04's expected files.
5. Confirm §5 hand-off section is complete.
6. (Optional) Run `git log --oneline bd8761f..HEAD` and confirm Phase 4 commits are coherent and reviewable per CLAUDE.md "small reviewable commits" guidance.

Type "approved" to close Phase 4 and dispatch /gsd-execute-phase 5; or describe issues for gap closure.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All four gauntlet commands exit 0.
- 04-EVIDENCE.md exists with ≥200 lines and all 5 sections populated.
- Every NETBALL-N row has a PASS marker (or a clearly-resolved FAIL with gap-closure breadcrumb).
- Phase 3 invariants UNCHANGED.
- `git diff --stat bd8761f..HEAD -- src/` lists only plan 04-04's 3-4 files.
- Human checkpoint signoff captured.
</verification>

<success_criteria>
1. All 8 NETBALL-N gates + ABSTRACT-03 verified end-to-end on the merged trunk.
2. TEST-05 state recorded (Kotara present OR absent-with-Phase-5-followup).
3. Phase 3 invariants intact.
4. Phase 5 hand-off complete.
5. No Phase 4 commits (orchestrator commits in the closing step per CONTEXT direction).
</success_criteria>

<uat>
This plan IS the UAT. The 04-EVIDENCE.md is the artefact a reviewer reads to verify Phase 4 is truly done. The human checkpoint is the gate.
</uat>

<output>
After completion, the orchestrator commits the entire Phase 4 plan set + summaries + EVIDENCE.md. No SUMMARY.md is required for plan 04-07 itself — 04-EVIDENCE.md serves as both the per-plan summary AND the phase close-out artefact.
</output>
