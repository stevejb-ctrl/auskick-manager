---
phase: 04-netball-verification-on-merged-trunk
plan: 04
subsystem: netball-live-shell
tags: [src-fix, NETBALL-04, NETBALL-07, track_scoring, fragile-area]
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - src/components/netball/NetballLiveGame.tsx
  - src/components/netball/NetballGameSummaryCard.tsx
  - src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx
  # NOTE: src/app/run/[token]/page.tsx renders <LiveGame> (AFL-only) — verified
  # by grep, no NetballLiveGame import. NOT touched in this plan.
autonomous: false
requirements: [NETBALL-04, NETBALL-07]
tech_stack: [react, typescript, next.js]
key_files:
  - src/components/netball/NetballLiveGame.tsx (FRAGILE — modify with care; per CONCERNS.md the netball live shell is the highest-risk area in the merged trunk)
  - src/components/netball/NetballGameSummaryCard.tsx
  - src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx (call site — already reads track_scoring at line 221, currently passes to AFL LiveGame at line 299, NOT to NetballLiveGame at line 188-207)
  # src/app/run/[token]/page.tsx — verified AFL-only (renders <LiveGame>, no NetballLiveGame import). Not modified.
  - src/components/netball/netballWalkthroughSteps.ts (read-only — already accepts trackScoring)
key_decisions:
  - D-CONTEXT-failure-handling (NETBALL-N blockers fixed in-Phase; this is one)
  - Discovery during planning: NetballLiveGame.tsx:801 hard-codes `trackScoring: true` for the walkthrough builder, and the `+G` opponent-goal button + GS/GA tap-to-score affordances have NO track_scoring gate. This BLOCKS NETBALL-04 and NETBALL-07 acceptance.
  - The summary card (NetballGameSummaryCard.tsx) does NOT gate the result+goals lines on track_scoring; only suppresses the Goals line when there are no scorers. NETBALL-06 acceptance demands explicit suppression.
  - Plan 04-02's track_scoring=false walkthrough test + plan 04-03's track_scoring=false summary test go GREEN after this plan ships.
requirements_mapping:
  NETBALL-04: track_scoring=false suppresses GS/GA scoring confirm flow, hides +G opponent-goal button, hides score numbers in score bug, drops walkthrough scoring step, suppresses summary card result+goals lines. Long-press still opens actions modal.
  NETBALL-07: Walkthrough scoring-step gate is wired to the team's actual track_scoring (not hard-coded true).
metrics:
  files_touched: 4
  context_estimate: 28%

must_haves:
  truths:
    - "When team.track_scoring=true, the netball live shell renders +G opponent goal button, GS/GA tokens fire the scoring confirm flow, undo chip appears post-goal, score numbers visible in score bug"
    - "When team.track_scoring=false, +G is hidden, GS/GA tap is a no-op for scoring (long-press still opens actions), undo chip never appears, score numbers hidden in score bug, walkthrough scoring step dropped, summary card result+goals lines suppressed"
    - "NetballLiveGame.tsx accepts a `trackScoring: boolean` prop and consumes it consistently across all gating sites"
    - "Sole netball call site (live/page.tsx) passes team.track_scoring through to NetballLiveGame. run/[token]/page.tsx is AFL-only and not affected."
    - "NetballGameSummaryCard.tsx accepts a `trackScoring` prop and conditionally suppresses the result + goals lines"
  artifacts:
    - path: "src/components/netball/NetballLiveGame.tsx"
      provides: "Updated component with trackScoring prop + gates at +G, GS/GA confirm flow, undo chip, score-bug numbers, walkthrough builder, score-bug bottom hint copy"
      contains: "trackScoring"
    - path: "src/components/netball/NetballGameSummaryCard.tsx"
      provides: "Updated component with trackScoring prop + gates on result+goals lines"
      contains: "trackScoring"
    - path: "src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx"
      provides: "trackScoring prop passed to NetballLiveGame"
      contains: "trackScoring={trackScoring}"
  key_links:
    - from: "live/page.tsx (netball branch ~line 188)"
      to: "<NetballLiveGame trackScoring={trackScoring} ...>"
      via: "explicit prop pass-through (already-fetched track_scoring at line 221 — currently used only for AFL <LiveGame> at line 299)"
      pattern: "trackScoring=\\{trackScoring\\}"
    - from: "NetballLiveGame internal walkthrough builder"
      to: "buildNetballWalkthroughSteps({ trackScoring })"
      via: "useMemo dependency on the new prop instead of hard-coded `true`"
      pattern: "buildNetballWalkthroughSteps\\(\\{ trackScoring"
    - from: "NetballLiveGame summary-card mount in finalised branch"
      to: "<NetballGameSummaryCard trackScoring={trackScoring} ...>"
      via: "prop drill into the card"
      pattern: "trackScoring=\\{trackScoring\\}"

user_setup: []
---

<objective>
Wire the `track_scoring` flag through the netball live shell + summary card so NETBALL-04 and NETBALL-07 acceptance gates land. This is a SOURCE-CODE plan (touches `src/`), distinct from the e2e specs in 04-02 / 04-03 / 04-05. It's a `fragile-area` plan per Phase 3 CONCERNS.md — the netball live shell is the highest-risk surface in the merged trunk.

Purpose: Without this wiring, NETBALL-04's six subclauses cannot pass, plan 04-02's track_scoring=false walkthrough test stays red, plan 04-03's track_scoring=false summary test stays red, and the next-wave live-flow spec (Plan 04-05) cannot assert the suppression-on-false matrix. Locked decision D-CONTEXT-failure-handling makes this an in-Phase fix.

Output: NetballLiveGame + NetballGameSummaryCard accept and consume a `trackScoring: boolean` prop; both page.tsx call sites pass `team.track_scoring` through; the e2e specs from waves 1-2 flip GREEN on the suppression matrix.

NOTE on autonomy: this plan is `autonomous: false` because it touches a fragile area (netball live shell, ~1600 LOC) AND introduces an end-of-plan checkpoint where the developer (user) signs off on the visual suppression behaviour before Plan 04-05 builds on top. Per CONTEXT D-CONTEXT-failure-handling + Phase 3 CONCERNS.md fragile-area policy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-netball-verification-on-merged-trunk/04-CONTEXT.md
@.planning/phases/03-branch-merge-abstraction-integrity/03-MERGE-LOG.md
@CLAUDE.md
@src/components/netball/NetballLiveGame.tsx
@src/components/netball/NetballGameSummaryCard.tsx
@src/components/netball/netballWalkthroughSteps.ts
@src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx
@src/app/run/[token]/page.tsx
@e2e/tests/netball-walkthrough.spec.ts
@e2e/tests/netball-summary.spec.ts

<interfaces>
<!-- Sites in NetballLiveGame.tsx that need the gate (verified by grep) -->

Hard-coded `trackScoring: true` (line 800-803):
```typescript
const walkthroughSteps = useMemo(
  () => buildNetballWalkthroughSteps({ trackScoring: true }),  // ← gate on prop
  [],
);
```

Score affordances that need gating:
- Line 487-525: handleTokenTap → if positionId is GS/GA, opens pendingGoal sheet → calls recordNetballGoal. Gate: short-circuit when !trackScoring (long-press path still works because it's onTokenLongPress, not onTokenTap).
- Line 1122 (live branch render) + line 1394 (NetballScoreBug render): `onOpponentGoal={handleOpponentGoal}` — gate by passing undefined when !trackScoring so the +G button hides (NetballScoreBug already conditionally renders the +G button via `{onOpponentGoal && (...)}`).
- Line 1130-1162 (lastScore + undo chip block): gate render on trackScoring (no scoring → no undo chip).
- Line ~1380-1392 (NetballScoreBug score numbers): the score bug always renders `team.goals` and `opponent.goals` numbers. NETBALL-04 demands score numbers HIDDEN when track_scoring=false. Add a `showScores: boolean` prop on NetballScoreBug or thread trackScoring directly.
- Line 1196-1199 (bottom hint copy): "Tap GS or GA to score (with confirm). Long-press any player for actions." — when !trackScoring, the first sentence is misleading. Either drop the scoring sentence or replace with "Long-press any player for actions."

Call sites currently NOT passing trackScoring (verified):

```typescript
// src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx
const trackScoring = teamRow?.track_scoring ?? false;  // line 221
// ...
<NetballLiveGame                                       // line 188 — netball branch
  ...                                                  // NO trackScoring prop passed currently
/>
// ...
<LiveGame trackScoring={trackScoring} ... />            // line 299 — AFL passes it correctly
```

```typescript
// src/app/run/[token]/page.tsx
const trackScoring = teamRow?.track_scoring ?? false;  // line 38
// Verify: does this page render NetballLiveGame? (Phase 4 must check.) If yes,
// pass through. If no (token-auth runner is AFL-only currently), no change.
```

NetballGameSummaryCard.tsx — currently NO trackScoring prop. Buildsummary at line 73-145 always emits "{X} def {Y}" / "drew with" lines + "🥅 Goals:" line if scorers exist.

</interfaces>

<phase_3_handoff_invariants>
- pre-merge tags MUST stay frozen
- PROD-04 fixme MUST stay
- Existing AFL e2e specs MUST NOT regress (full e2e suite green per Phase 3 baseline)
- D-26/D-27 quarterMs wiring at LiveGame.tsx:122/151/699/717/794 + liveGameStore.ts:339 MUST NOT be touched
- Pause-event persistence bug NOT addressed (deferred)
- Surface 4 (QuarterBreak time bars) NOT redirected per Phase 3 RESEARCH §4
</phase_3_handoff_invariants>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add trackScoring prop to NetballLiveGame and gate all scoring affordances</name>
  <files>src/components/netball/NetballLiveGame.tsx</files>
  <behavior>
    - When trackScoring=false, +G button hidden in score bug (existing `{onOpponentGoal && (...)}` gate fires because we pass undefined)
    - When trackScoring=false, GS/GA token tap does NOT open pendingGoal sheet (handleTokenTap short-circuits the score path; long-press path still works)
    - When trackScoring=false, lastScore undo chip block does not render
    - When trackScoring=false, score-bug numeric goal counts not shown (replace with em-dash or hide entirely; whichever matches AFL's existing pattern in src/components/live/ScoreBug.tsx — read that file once, mirror the convention)
    - When trackScoring=false, bottom hint copy reads "Long-press any player for actions." (drop the "Tap GS or GA to score" sentence)
    - Walkthrough builder receives the trackScoring prop value (NOT hard-coded true)
    - The prop default is `false` to match the codebase convention (`teamRow?.track_scoring ?? false` at every call site)
    - All existing trackScoring=true behaviour is unchanged — every existing assertion in netball-walkthrough/summary specs (the ones that currently PASS) must still pass.
  </behavior>
  <action>
1. Read `src/components/live/ScoreBug.tsx` (or equivalent AFL score-bug component if path differs — locate via `grep -rn "ScoreBug" src/components/live/`). Note how it gates score numbers when AFL track_scoring=false — there's an existing pattern to mirror so netball + AFL behave consistently.

2. In `src/components/netball/NetballLiveGame.tsx`:

   a. Add `trackScoring: boolean` to the Props interface (find it; it's the props object destructured at the top of the function ~line 150-200).

   b. Destructure `trackScoring` at the function signature.

   c. Replace line 800-803:
      ```typescript
      const walkthroughSteps = useMemo(
        () => buildNetballWalkthroughSteps({ trackScoring }),
        [trackScoring],
      );
      ```

   d. Gate `handleTokenTap` (around line 487-525): inside the function, after the `if (!SCORING_POSITIONS.has(positionId)) return;` early-return, add:
      ```typescript
      if (!trackScoring) return;  // NETBALL-04 — scoring affordance suppressed
      ```
      This stops the pendingGoal sheet from opening and the recordNetballGoal action from firing. Long-press path is in `handleTokenLongPress`, which is untouched.

   e. Gate `onOpponentGoal` prop on the NetballScoreBug render (line 1122):
      ```typescript
      onOpponentGoal={trackScoring ? handleOpponentGoal : undefined}
      ```
      The existing `{onOpponentGoal && (...)}` gate inside NetballScoreBug (line 1394) fires correctly when undefined → +G hides.

   f. Gate the lastScore undo chip block (line 1130-1162). Wrap in:
      ```jsx
      {trackScoring && lastScore && (
        <div ...>...</div>
      )}
      ```

   g. Gate score-bug numeric goal counts: NetballScoreBug currently renders `{team.goals}` and `{opponent.goals}` unconditionally (line ~1380-1392). Add a `showScores: boolean` prop on NetballScoreBug (the inner sub-component within the same file — it's defined locally), default true. When false, render an em-dash or empty string in place of the numeric count. Pass `showScores={trackScoring}` from the live branch (around line 1117). The pre-kickoff branch + finalised branch may also render the score bug; pass the prop everywhere it's used (search for `<NetballScoreBug` in the file — there are typically 3-4 sites).

   h. Gate the bottom hint copy (line 1196-1199):
      ```jsx
      <p className="text-center text-xs text-neutral-500">
        {trackScoring
          ? "Tap GS or GA to score (with confirm). Long-press any player for actions. The quarter ends automatically when the clock reaches zero."
          : "Long-press any player for actions. The quarter ends automatically when the clock reaches zero."}
      </p>
      ```

   i. Pass `trackScoring={trackScoring}` to `<NetballGameSummaryCard>` in the finalised branch (locate via `grep -n NetballGameSummaryCard src/components/netball/NetballLiveGame.tsx`). The card is rendered around line 1080+ in the finalised branch.

3. Confirm `npx tsc --noEmit` exits 0 after edits.

4. Run the existing Vitest contract that covers buildNetballWalkthroughSteps step-drop logic:
   ```
   npm test -- --run src/lib/__tests__/netballWalkthroughSteps.test.ts
   ```
   Expect zero new failures (this test is unit-level, not affected by our component change, but a sanity check).

5. Run plan 04-02's spec to confirm the track_scoring=false walkthrough test now passes:
   ```
   npm run e2e -- e2e/tests/netball-walkthrough.spec.ts
   ```
   All 4 cases must be green.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run src/lib/__tests__/netballWalkthroughSteps.test.ts && npm run e2e -- e2e/tests/netball-walkthrough.spec.ts</automated>
  </verify>
  <done>
NetballLiveGame.tsx accepts `trackScoring: boolean` in its Props interface and consumes it at the 6 documented sites. `tsc --noEmit` exits 0. `npm test -- --run src/lib/__tests__/netballWalkthroughSteps.test.ts` exits 0 (existing Vitest contract still green). `npm run e2e -- e2e/tests/netball-walkthrough.spec.ts` reports 4/4 PASS. The hard-coded `trackScoring: true` at line 801 is gone (`grep -nE 'trackScoring:\\s*true' src/components/netball/NetballLiveGame.tsx` returns 0 matches).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add trackScoring prop to NetballGameSummaryCard and gate result+goals lines</name>
  <files>src/components/netball/NetballGameSummaryCard.tsx</files>
  <behavior>
    - When trackScoring=true, summary text contains "🏐 Full time", result line ("def" / "drew with" / "lost to" or whichever copy is canonical), "🥅 Goals:" line when scorers exist, "👟 N players", "⏱ Game time" block, per-player time rows
    - When trackScoring=false, summary text contains "🏐 Full time", "👟 N players", "⏱ Game time" block, per-player time rows BUT NO result line ("def" / "drew with" substring absent), NO "🥅 Goals:" line
    - The Copy-for-group-chat button still functions; it copies whichever text is currently rendered
  </behavior>
  <action>
1. Add `trackScoring: boolean` to the Props interface at the top of `NetballGameSummaryCard.tsx`.

2. Destructure `trackScoring` in the component function.

3. Update `buildSummary` (line 73-145) to accept `trackScoring` as a new parameter. When false:
   - Skip the result-line emission (lines 88-101 — the def/drew with/lost to block).
   - Skip the "🥅 Goals:" line emission (lines 105-115).
   - Keep "🏐 Full time" header line, the "👟 N players" line, and the "⏱ Game time" block.

4. Pass `trackScoring` from the component call to buildSummary.

5. Update the Copy-for-group-chat affordance — no change needed, it already copies whatever the buildSummary returned.

6. Confirm tsc clean. Run plan 04-03's summary spec — both cases must pass:
   ```
   npm run e2e -- e2e/tests/netball-summary.spec.ts
   ```
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run e2e -- e2e/tests/netball-summary.spec.ts</automated>
  </verify>
  <done>
NetballGameSummaryCard.tsx Props includes `trackScoring: boolean`. buildSummary correctly suppresses result+goals lines when false. `tsc --noEmit` exits 0. `npm run e2e -- e2e/tests/netball-summary.spec.ts` reports 2/2 PASS.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pass trackScoring through live/page.tsx to NetballLiveGame</name>
  <files>src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx</files>
  <action>
1. In `src/app/(app)/teams/[teamId]/games/[gameId]/live/page.tsx`:
   - Around line 188-207 (the netball branch's `<NetballLiveGame ...>`), add `trackScoring={trackScoring}`. The local `trackScoring` constant already exists (line 221 — but it's used only AFL-side). Hoist it ABOVE the netball branch (move the line `const trackScoring = teamRow?.track_scoring ?? false;` from line 221 up to ~line 90 — before the `if (sport === "netball")` block). Then use it in both branches.

2. Run-token route (`src/app/run/[token]/page.tsx`) is AFL-only: it renders `<LiveGame>` (not `<NetballLiveGame>`), so no change is required here. The plan-checker confirmed this; included as a comment in frontmatter.

3. Run the full e2e gauntlet for waves 1-3 specs to confirm no regression:
   ```
   npm run e2e -- e2e/tests/netball-walkthrough.spec.ts e2e/tests/netball-summary.spec.ts e2e/tests/netball-stats.spec.ts e2e/tests/multi-sport-schema.spec.ts e2e/tests/live-quarters.spec.ts e2e/tests/live-scoring.spec.ts
   ```
   Expect every test green.

4. Run `npx tsc --noEmit` and `npm run lint` — both must exit 0.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run e2e -- e2e/tests/netball-walkthrough.spec.ts e2e/tests/netball-summary.spec.ts e2e/tests/netball-stats.spec.ts e2e/tests/multi-sport-schema.spec.ts e2e/tests/live-quarters.spec.ts e2e/tests/live-scoring.spec.ts</automated>
  </verify>
  <done>
`trackScoring` prop flows from page.tsx → NetballLiveGame on both routes (or just live/page.tsx if run/[token]/page.tsx doesn't dispatch netball). `tsc --noEmit` + lint exit 0. The 6-spec gauntlet listed above is fully green. AFL specs unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plan 04-04 wired track_scoring through the netball live shell + summary card. Tasks 1-3 changed the source code.

Automated evidence:
- All 4 netball-walkthrough.spec.ts cases green
- Both netball-summary.spec.ts cases green
- Both netball-stats.spec.ts cases green
- multi-sport-schema, live-quarters, live-scoring all still green
- tsc + lint clean
  </what-built>
  <how-to-verify>
This is a fragile-area change (netball live shell, ~1600 LOC). Before Plan 04-05 builds the live-flow spec on top, manually confirm visual behaviour matches NETBALL-04's six subclauses:

1. Start `npm run dev`. Sign in as super-admin.
2. Use a netball team with track_scoring=true (or factory.makeTeam + admin update). Visit its live page.
   - Confirm: GS/GA tap opens scoring confirm sheet
   - Confirm: +G button visible in score bug (top right)
   - Confirm: After a goal, undo chip appears for 8s then becomes muted persistent chip
   - Confirm: Score numbers shown in score bug
   - Confirm: Walkthrough (clear localStorage) includes "Recording scores" step
3. Toggle track_scoring=false on the SAME team via team settings or admin DB update. Refresh the live page.
   - Confirm: GS/GA tap is a no-op for scoring (long-press still works — opens NetballPlayerActions modal)
   - Confirm: +G button HIDDEN
   - Confirm: Undo chip never appears
   - Confirm: Score numbers HIDDEN (em-dash or empty)
   - Confirm: Bottom hint copy says "Long-press any player for actions" without the "Tap GS or GA to score" sentence
   - Confirm: Walkthrough (clear localStorage) does NOT include the "Recording scores" step
4. Finalise a track_scoring=false netball game (or seed events directly per netball-summary.spec.ts's pattern). Visit the live page.
   - Confirm: Game summary card renders with 🏐 + 👟 + ⏱ but NO result line and NO 🥅 Goals line
5. Repeat with track_scoring=true and confirm summary card shows result line + 🥅 Goals line.

After verifying all six suppression points + the toggle-back-on path, type "approved" or describe issues.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run lint` exits 0.
- 6-spec gauntlet (4 netball + 2 AFL) all green.
- Existing Vitest unit tests still green (`npm test`).
- `grep -c 'trackScoring' src/components/netball/NetballLiveGame.tsx` returns ≥ 8 (Props interface, destructure, useMemo, gates at the 6 sites).
- `grep -c 'trackScoring' src/components/netball/NetballGameSummaryCard.tsx` returns ≥ 4.
- `grep -nE 'trackScoring:\\s*true' src/components/netball/NetballLiveGame.tsx | grep -v 'trackScoring: true (when applicable)' | grep -v test` returns 0 (no remaining hard-codes).
- Phase 3 invariants intact: pre-merge tags untouched, PROD-04 fixme intact, AFL e2e suite untouched.
- D-26/D-27 quarterMs wiring sites untouched: `grep -nE 'quarterMs' src/components/live/LiveGame.tsx src/lib/stores/liveGameStore.ts` matches Phase 3 baseline (≥ 5 + 4 hits respectively per MERGE-LOG §4).
</verification>

<success_criteria>
1. NETBALL-04: all six suppression subclauses (GS/GA scoring confirm, +G hidden, undo hidden, score numbers hidden, walkthrough scoring step dropped, summary result+goals lines suppressed) verified by automated specs AND human checkpoint.
2. NETBALL-07: walkthrough scoring-step gate is wired to the team's actual track_scoring (not hard-coded true).
3. AFL flow regression-free: live-quarters + live-scoring + multi-sport-schema all green.
4. tsc + lint + Vitest all green.
5. Phase 3 invariants intact (tags, PROD-04, D-26/D-27 sites).
</success_criteria>

<uat>
**NETBALL-04 — UAT** (covered by checkpoint task above).

Specifically the six suppression subclauses on track_scoring=false:
- GS/GA tap is no-op for scoring; long-press still opens actions modal.
- +G hidden.
- Undo chip never appears.
- Score numbers hidden in score bug.
- Walkthrough scoring step dropped.
- Summary card "def/drew with" + "🥅 Goals:" lines suppressed.

**NETBALL-07** — covered by plan 04-02's now-green track_scoring=false test.
</uat>

<output>
After completion, create `.planning/phases/04-netball-verification-on-merged-trunk/04-04-SUMMARY.md` with:
- Files modified (3 or 4 depending on whether run/[token]/page.tsx needed touching)
- Each of the 6 NETBALL-04 suppression sites with file:line references
- Confirmation that plan 04-02 walkthrough spec is now 4/4 green
- Confirmation that plan 04-03 summary spec is now 2/2 green
- Confirmation that AFL specs (live-quarters, live-scoring) still green
- Phase 3 invariants intact (D-26/D-27 wiring sites unchanged, tags frozen, PROD-04 fixme intact)
- Human checkpoint signoff captured
</output>
