---
phase: 09-availability-that-holds-pre-game-and-at-breaks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/live/QuarterBreak.tsx
  - src/components/netball/NetballQuarterBreak.tsx
  - src/components/league/LeagueLiveGame.tsx
  - e2e/tests/break-availability-afl.spec.ts
  - e2e/tests/break-availability-netball.spec.ts
  - e2e/tests/break-availability-league.spec.ts
autonomous: true
requirements: [AVAIL-02]

must_haves:
  truths:
    - "At a period break a coach can add a newly-arrived player into the game (AFL, netball, rugby league)"
    - "At a period break a coach can mark a present player out, which forces a replacement pick for the vacated on-field spot"
    - "At a period break a coach can mark a player injured (existing affordance retained)"
    - "Add-arrived uses the canonical addLateArrival writer — no new availability writer is forked"
    - "The 'Manage availability' entry matches AFL's break-surface rhythm across all three sports"
  artifacts:
    - path: "src/components/live/QuarterBreak.tsx"
      provides: "AFL reference: Manage-availability entry (add-arrived + mark-out via forced replacement) at the break"
      contains: "addLateArrival"
    - path: "src/components/netball/NetballQuarterBreak.tsx"
      provides: "Netball break Manage-availability entry mirroring AFL"
      contains: "addLateArrival"
    - path: "src/components/league/LeagueLiveGame.tsx"
      provides: "League break (isAtQbreak) Manage-availability entry mirroring AFL"
      contains: "addLateArrival"
  key_links:
    - from: "src/components/live/QuarterBreak.tsx"
      to: "addLateArrival (live/actions.ts:595)"
      via: "enqueueLiveAction('addLateArrival', ...) for add-arrived"
      pattern: "addLateArrival"
    - from: "src/components/live/QuarterBreak.tsx"
      to: "InjuryReplacementModal"
      via: "mark-out forced-replacement reuse (D-09)"
      pattern: "InjuryReplacementModal"
---

<objective>
B2 / AVAIL-02: at any period break a coach can ADD a newly-arrived player,
MARK a present player OUT, or MARK a player INJURED — on the shared break
surface, across all three sports (D-07).

Placement (D-08): a single "Manage availability" entry on the break surface,
matching AFL's existing Lend / Mark-injured panel rhythm. Because
`AvailabilityList` is an async Server Component it CANNOT be mounted inline in
the `"use client"` break surfaces — so reuse the CLIENT `AvailabilityRow`
component and the SlotFillSheet rhythm the break surfaces already use, wired to
the SAME canonical writers (`addLateArrival` for arrivals, the injury/swap
two-step for mark-out). Do NOT fork a new writer or row UI.

Mark-out (D-09): mirrors the injury flow — reuse `InjuryReplacementModal` to
force a replacement pick for the vacated on-field spot. "Out" differs from
"injured" only in the recorded reason, not the mechanic.

Cross-sport (D-10): ship to all three break surfaces — AFL `QuarterBreak.tsx`
is the REFERENCE; netball `NetballQuarterBreak.tsx` and the league `isAtQbreak`
path in `LeagueLiveGame.tsx` mirror its placement, token palette, and modal
rhythm (reuse-before-fork, CLAUDE.md).

Purpose: the break-time control feels like the same availability surface the
coach knows from pre-game — one mental model, no bespoke mini-UI.
Output: a Manage-availability entry on all three break surfaces + red-first
e2e specs per sport.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-availability-that-holds-pre-game-and-at-breaks/09-CONTEXT.md
@.planning/phases/09-availability-that-holds-pre-game-and-at-breaks/09-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Extracted from codebase — executor should use these directly. -->

addLateArrival — canonical "add an arrived player" writer (DO NOT FORK,
Success Criterion #4). Sets game_availability -> 'available' THEN emits a
player_arrived event. Input is an OBJECT:
From src/app/(app)/teams/[teamId]/games/[gameId]/live/actions.ts:595-634:
```typescript
export async function addLateArrival(
  auth: LiveAuth, gameId: string,
  input: { player_id: string; quarter: number; elapsed_ms: number },
  idempotencyKey?: string,
): Promise<ActionResult>
```
Canonical client invocation (LeagueLiveGame.tsx:1003-1022 handleAddLateArrival):
```typescript
const { flushed } = enqueueLiveAction("addLateArrival", [
  auth, game.id,
  { player_id: playerId, quarter: Math.max(1, state.currentQuarter), elapsed_ms: elapsedMs },
]);
await flushed; router.refresh();
```
Netball already wires it: NetballLiveGame.tsx:1321 handleLateArrival.

setAvailability — toggle behind every AvailabilityRow (mark a present player
out = status "unavailable"). src/app/(app)/teams/[teamId]/games/[gameId]/actions.ts:54.
Client invokes via write queue (AvailabilityRow.tsx:130-135):
`enqueueLiveAction("setAvailability", [auth, gameId, playerId, next])`.

InjuryReplacementModal — reuse for mark-out forced replacement (D-09).
Props (InjuryReplacementModal.tsx:18-37): injuredPlayer, zone (AFL Zone OR
plain string like "forward"/"back"), candidates: InjuryReplacementCandidate[]
({ player, totalMs }, pre-sorted least-played first), onPickReplacement,
onSkipReplacement, onCancel. Canonical invocation + handler in LeagueLiveGame.tsx:
- render block :1851-1882
- handleInjuryReplacement :1090-1125 — fires markInjury THEN a swap
  (recordLeagueSwap for league; recordSwap for AFL) in FIFO order so the
  replacement lands at the vacated slot.

Break-surface rhythm to mirror (AFL QuarterBreak.tsx is the REFERENCE):
- Existing panels: Lend (markLoan) + Mark-injured (markInjury) inside the
  Match-adjustments collapse. Each affordance is a dashed-border "+ {verb}"
  button (:1095-1099) that opens a SlotFillSheet (lend :1109-1130, injured
  :1136-1157). onPick -> a toggle handler that fires a server action via the
  write queue.
- Toggle handler shape (handleLoanToggle :541-560 / handleInjuryToggle
  :566-581): optimistic local flip, startTransition, server action, rollback
  on failure.
- Picker open state: useState(false) per picker (lendPickerOpen :539,
  injuredPickerOpen :608).
- Action imports at :13-22.
- NOTE: AFL QuarterBreak does NOT currently import InjuryReplacementModal — its
  mark-injured uses markInjury directly with no replacement prompt. To honour
  D-09 at the break you will IMPORT InjuryReplacementModal into QuarterBreak and
  wire it the way LeagueLiveGame does.

SlotFillSheet candidate shape (LateArrivalMenu.tsx:51-60 + QuarterBreak
:1115-1123): candidates = squad mapped to { id, name, jerseyNumber }. For
add-arrived the candidate set = squad members NOT in availableIds.

periodPhase() (Phase 8, src/lib/live/periodPhase.ts:9-21): returns
{ isAtFullTime, isBetweenPeriods, isLastPeriod }. Resolve "is this a break"
sport-agnostically — never hardcode currentQuarter >= 4. League break state is
isAtQbreak (LeagueLiveGame.tsx:1224-1228); full time isAtFinalQ (:1229-1232)
hands off to LeagueFullTimeReview — the Manage-availability entry belongs on the
break (isAtQbreak) surface, NOT the full-time review.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — break-availability regression specs (add-arrived, mark-out, mark-injured) × 3 sports</name>
  <read_first>
    - e2e/tests/break-availability-afl.spec.ts, break-availability-netball.spec.ts, break-availability-league.spec.ts (the files being CREATED)
    - e2e/tests/quarter-break-rotation.spec.ts (AFL break seed: lineup_set + quarter_start + quarter_end events to land mid-game AT a quarter break)
    - e2e/tests/netball-quarter-break.spec.ts (netball break seed pattern)
    - e2e/tests/injury-replacement.spec.ts:1-75 (the InjuryReplacementModal assertion pattern: open the picker, pick a replacement, assert BOTH an injury/out event AND a swap event land; fixture preamble)
  </read_first>
  <action>
Create THREE specs (one per sport) exercising the new break "Manage
availability" entry. Each seeds a mid-game paused AT a period break (use the
break-seed event sequence from quarter-break-rotation.spec.ts: lineup_set +
quarter_start + quarter_end so the live page renders the break surface), then:

  1. ADD ARRIVED: a squad member who was NOT available pre-game (no
     game_availability 'available' row, not on field). Open "Manage
     availability", add them. ASSERT a `player_arrived` event lands AND a
     game_availability row flips to 'available' (poll game_events for
     player_arrived with that player_id, and game_availability for status
     'available' — reuse the expect.poll pattern from availability.spec.ts).
  2. MARK OUT: an on-field player. Open "Manage availability", mark them out,
     and in the forced-replacement modal pick a bench player. ASSERT an
     injury/out event lands with the OUT reason (distinct from injury) AND a
     swap event lands placing the bench player at the vacated slot (mirror the
     dual-event assertion in injury-replacement.spec.ts).
  3. MARK INJURED: confirm the existing mark-injured affordance still works at
     the break (regression guard — the new entry must not break it).

Sports: AFL spec uses the { back,hback,mid,hfwd,fwd,bench } lineup; netball
uses court positions; league uses { forwards, backs, bench }. Reference the
sport-specific break surfaces: AFL QuarterBreak, netball NetballQuarterBreak,
league isAtQbreak in LeagueLiveGame.

Write RED-FIRST: against current code the add-arrived and mark-out flows do not
exist at the break, so those assertions MUST FAIL. Do NOT implement the UI in
this task. Commit: `test(09-02): add failing break-availability specs (3 sports)`.
  </action>
  <verify>
    <automated>npx playwright test e2e/tests/break-availability-afl.spec.ts e2e/tests/break-availability-netball.spec.ts e2e/tests/break-availability-league.spec.ts --reporter=line; if [ $? -eq 0 ]; then echo "UNEXPECTED PASS — specs must be RED"; exit 1; else echo "RED confirmed"; fi</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist: `e2e/tests/break-availability-afl.spec.ts`, `break-availability-netball.spec.ts`, `break-availability-league.spec.ts`
    - Each contains an add-arrived assertion (`player_arrived`), a mark-out assertion (a swap event + the out reason), and a mark-injured regression check
    - Each seeds a mid-game break via `quarter_end` (grep for `quarter_end`)
    - Running the three specs against current code FAILS (RED) — add-arrived/mark-out at the break do not yet exist
    - Commit message matches `test(09-02):`
  </acceptance_criteria>
  <done>Three per-sport break specs created covering all three actions; RED against current code; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — AFL QuarterBreak reference: Manage-availability entry (add-arrived + mark-out forced replacement)</name>
  <read_first>
    - src/components/live/QuarterBreak.tsx (imports :13-22; lend handler handleLoanToggle :541-560; injury handler handleInjuryToggle :566-581; picker open state :539/:608; the dashed "+ {verb}" button :1095-1099; SlotFillSheet lend :1109-1130 / injured :1136-1157)
    - src/components/league/LeagueLiveGame.tsx:1003-1022 (handleAddLateArrival — copy the addLateArrival enqueue), :1090-1125 (handleInjuryReplacement two-step markInjury+swap), :1851-1882 (InjuryReplacementModal render + candidates sort)
    - src/components/live/InjuryReplacementModal.tsx:18-37 (props contract)
    - src/components/live/LateArrivalMenu.tsx:51-60 (candidate-mapping shape for the not-yet-available add-arrived list)
    - src/app/(app)/teams/[teamId]/games/[gameId]/live/actions.ts:595-634 (addLateArrival signature)
  </read_first>
  <action>
Add a third entry, "Manage availability", to the AFL QuarterBreak
Match-adjustments collapse, matching the existing Lend / Mark-injured dashed
"+" button rhythm (:1095-1099) and SlotFillSheet pattern. This is the REFERENCE
implementation netball + league will mirror in Task 3/4.

It surfaces TWO new actions (mark-injured already exists — keep it, D-07):

ADD ARRIVED:
  - A dashed "+ Add arrived player" button opens a SlotFillSheet whose
    candidates = squad members NOT in the current availableIds (i.e. not on
    field, not already available). Map candidates to { id, name, jerseyNumber }
    (LateArrivalMenu.tsx:51-60 shape).
  - onPick fires addLateArrival via the write queue — COPY
    LeagueLiveGame.tsx:1003-1022 handleAddLateArrival verbatim (the canonical
    writer; do NOT fork). Use the break's current quarter for `quarter` and the
    break elapsed_ms (0 at a between-periods break is acceptable — match how
    LeagueLiveGame computes elapsedMs). `await flushed; router.refresh()`.

MARK OUT (D-09 — forced replacement, mirrors injury):
  - A dashed "+ Mark a player out" button opens a SlotFillSheet of on-field
    players. onPick sets state that opens InjuryReplacementModal (IMPORT it into
    QuarterBreak — it is not currently imported here). Build `candidates` =
    swappable bench mapped to { player, totalMs } sorted least-played-first
    (copy LeagueLiveGame.tsx:1851-1882). zone = the vacated player's zone.
  - onPickReplacement fires the same two-step as
    LeagueLiveGame.handleInjuryReplacement (:1090-1125): record the OUT event
    THEN a swap (recordSwap for AFL) in FIFO order so the replacement lands at
    the vacated slot. RECORD THE DISTINCT "out" REASON (Discretion: a distinct
    event type/reason vs a metadata flag — planner's call; the reason MUST be
    display-recoverable. Recommended: reuse the injury writer with a
    `reason: "out"` metadata flag so display can distinguish out vs injured).
    onSkipReplacement / onCancel mirror the league handlers.
  - The player KEEPS time-on-ground already earned (D-09) — do NOT zero their
    minutes; this is the same mechanic as injury.

Use periodPhase()-derived break flags already present in the surface; never
hardcode "quarter". Match AFL token palette / placement (Claude's discretion on
exact label/position, but visually consistent with the Lend/Injured panels).

Run the AFL spec from Task 1 — it must now GO GREEN. Commit:
`feat(09-02): add break-time Manage-availability to AFL QuarterBreak (reference)`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test e2e/tests/break-availability-afl.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `QuarterBreak.tsx` contains `addLateArrival` (the add-arrived enqueue) and `InjuryReplacementModal` (imported + rendered)
    - `QuarterBreak.tsx` contains a mark-out handler that records a distinct out reason (grep for `"out"` near the injury/swap call) and fires a swap (`recordSwap`)
    - The add-arrived candidate list excludes already-available players (grep shows a filter against availableIds / available set)
    - `npx tsc --noEmit` exits 0
    - The AFL break spec from Task 1 now PASSES (GREEN)
    - Commit message matches `feat(09-02):`
  </acceptance_criteria>
  <done>AFL QuarterBreak has the Manage-availability entry (add-arrived via addLateArrival + mark-out via InjuryReplacementModal forced replacement); existing mark-injured retained; AFL spec GREEN; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — mirror into Netball + League break surfaces; full DoD gates</name>
  <read_first>
    - src/components/live/QuarterBreak.tsx (the REFERENCE implementation just built in Task 2 — mirror its placement/rhythm)
    - src/components/netball/NetballQuarterBreak.tsx (Match-adjustments collapse :880; lend panel :1122-1155 + injured panel :1157-1192; markInjury/markLoan via write queue :729/:747/:927/:941; SlotFillSheet pickers :1205-1219/:1225-1240; lendPickerOpen/injuredPickerOpen state)
    - src/components/netball/NetballLiveGame.tsx:1321 (handleLateArrival — netball's existing addLateArrival wiring to lift into the break)
    - src/components/league/LeagueLiveGame.tsx (isAtQbreak break state :1224-1228; isAtFinalQ :1229-1232 hands to LeagueFullTimeReview — entry belongs on isAtQbreak, NOT final review; handleAddLateArrival :1003-1022 + handleInjuryReplacement :1090-1125 + InjuryReplacementModal render :1851-1882 are ALREADY here — surface a Manage-availability entry that reuses them at the break)
  </read_first>
  <action>
Mirror the AFL Task-2 entry into the other two sports (reuse-before-fork,
CLAUDE.md — netball + league match AFL's placement, token palette, column
counts, and SlotFillSheet/modal rhythm; AFL is the reference; deviate only with
a commented reason).

NETBALL (NetballQuarterBreak.tsx): add the same "Manage availability" entry to
the Match-adjustments collapse (:880), matching the existing lend/injured panel
rhythm (:1122-1192). Add-arrived: lift the addLateArrival wiring from
NetballLiveGame.tsx:1321 handleLateArrival into the break. Mark-out: reuse
InjuryReplacementModal with the same two-step + "out" reason as AFL (netball
candidates from swappable bench; zone = court position). Keep mark-injured.

LEAGUE (LeagueLiveGame.tsx): surface the Manage-availability entry on the
isAtQbreak break surface (:1224-1228). League ALREADY has handleAddLateArrival
(:1003-1022), handleInjuryReplacement (:1090-1125), and the
InjuryReplacementModal render (:1851-1882) — wire a break-surface entry that
reuses them (add-arrived button -> handleAddLateArrival; mark-out button ->
opens the existing InjuryReplacementModal with the "out" reason recorded).
Do NOT add the entry to the isAtFinalQ / LeagueFullTimeReview surface.

Run ALL FOUR DoD gates (CLAUDE.md) and fix anything red:
`npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run e2e` (the latter runs
all three break specs + the existing break/injury specs to confirm no
cross-sport regression). Commit:
`feat(09-02): mirror break-time Manage-availability into netball + league`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test && npm run e2e</automated>
  </verify>
  <acceptance_criteria>
    - `NetballQuarterBreak.tsx` contains `addLateArrival` and `InjuryReplacementModal`
    - `LeagueLiveGame.tsx` surfaces the Manage-availability entry on the `isAtQbreak` path (grep shows the add-arrived / mark-out controls gated by `isAtQbreak`, not `isAtFinalQ`)
    - Netball + league mark-out record the same distinct "out" reason as AFL (grep `"out"` near the injury/swap call in each)
    - The netball and league break specs from Task 1 now PASS (GREEN)
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0; `npm test` exits 0; `npm run e2e` exits 0
    - No new availability writer was forked — only `addLateArrival` / `setAvailability` / the injury+swap writers are used (grep confirms no new `export async function ...Availability` in component files)
    - Commit message matches `feat(09-02):`
  </acceptance_criteria>
  <done>Netball + league break surfaces mirror the AFL Manage-availability entry (add-arrived + mark-out forced replacement, mark-injured retained); all three break specs GREEN; all four DoD gates green; no forked writer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| break-surface client -> setAvailability / addLateArrival | A coach (or runner token) mutates mid-game availability and forces a replacement swap |
| break-surface client -> injury/out + swap writers | Mark-out commits an out event + a swap event placing a bench player on field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Elevation of Privilege | setAvailability / addLateArrival / injury+swap writers invoked from the break | mitigate | All writers go through `resolveWriter` (team path gates admin/game_manager; token path validates share_token + parent/runner scope). The break entry reuses these EXISTING gated writers — no new server action, no new surface. |
| T-09-06 | Tampering | mark-out swap payload (which bench player lands on field) | mitigate | The swap reuses the existing recordSwap/recordLeagueSwap writers and FIFO slot logic already validated by injury-replacement; candidate set is server-readable squad members. No new trust path beyond the existing injury flow. |
| T-09-07 | Repudiation | "out" vs "injured" distinction recoverable | accept | The recorded reason ("out" metadata flag/type) is display-recoverable and attributed via created_by on the event — same auditability as the existing injury event; low risk. |
| T-09-08 | Information Disclosure | add-arrived candidate list (not-yet-available squad members) | accept | Candidates are squad members already visible to the coach in the pre-game availability screen; no new data exposure. |
</threat_model>

<verification>
- Three per-sport break specs (`break-availability-{afl,netball,league}.spec.ts`) pass: add-arrived lands a player_arrived event + flips availability; mark-out lands an out event + a swap; mark-injured still works.
- Add-arrived uses the canonical `addLateArrival` writer in all three sports (grep confirms; no forked writer).
- Mark-out reuses `InjuryReplacementModal` for the forced replacement in all three sports.
- League entry is on the `isAtQbreak` surface, not the full-time review.
- All four DoD gates green (`npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run e2e`).
- Cross-sport visual consistency: netball + league mirror AFL's placement/token palette (CLAUDE.md reuse-before-fork).
</verification>

<success_criteria>
ROADMAP Phase 9 criteria #3 + #4 satisfied:
- At any period break a coach can add a newly-arrived player, mark a present player out, and mark a player injured, on the shared break surface, across all sports.
- Regression tests written red-first cover each break-time availability action through the UI; `addLateArrival` is wired into the break surface rather than a new writer (reuse-before-fork).
</success_criteria>

<output>
After completion, create `.planning/phases/09-availability-that-holds-pre-game-and-at-breaks/09-02-SUMMARY.md`
</output>
