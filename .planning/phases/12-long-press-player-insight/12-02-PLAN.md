---
phase: 12-long-press-player-insight
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/sports/netball/fairness.ts
  - src/lib/sports/rugby_league/fairness.ts
  - src/lib/__tests__/playedZoneMsByPeriod.netball.test.ts
  - src/lib/__tests__/playedZoneMsByPeriod.league.test.ts
  - src/components/netball/NetballPlayerActions.tsx
  - src/components/netball/NetballLiveGame.tsx
  - src/components/league/LeagueLiveGame.tsx
  - e2e/tests/player-insight.spec.ts
autonomous: true
requirements: [PLAYERVIEW-01, PLAYERVIEW-02]

must_haves:
  truths:
    - "Long-pressing a player in NETBALL shows the SAME player-insight summary (in-game per-zone/third time + time-since-last-sub + per-period breakdown + season per-zone percentages) — rendered inside the existing NetballPlayerActions host via a shared insight slot, NOT a new modal"
    - "Long-pressing a player in RUGBY LEAGUE shows the SAME summary inside the existing (already shared) LockModal host"
    - "Both netball and league reuse the SHARED buildPlayerInsight builder + PlayerInsightSummary component from plan 12-01 verbatim — no per-sport fork of the summary"
    - "The per-period minutes-per-zone breakdown is derived from each sport's event replay (netball per-third, league per-field) — the netball/league equivalents of the AFL playedZoneMsByPeriod"
    - "Zones come from getAgeGroupConfig(sport, ageGroup).zones (netball's 5 thirds; rugby league's single 'field') — never hardcoded — so each sport's zone labels render correctly, and season output is percentages only"
  artifacts:
    - path: "src/lib/__tests__/playedZoneMsByPeriod.netball.test.ts"
      provides: "Red-first: netball per-period x per-third ms derived from the event replay"
      contains: "playedZoneMsByPeriod"
    - path: "src/lib/__tests__/playedZoneMsByPeriod.league.test.ts"
      provides: "Red-first: rugby-league per-period field ms derived from the event replay"
      contains: "playedZoneMsByPeriod"
    - path: "src/components/netball/NetballPlayerActions.tsx"
      provides: "Optional insight?: React.ReactNode slot rendered under the header (mirror of the LockModal slot)"
      contains: "insight"
    - path: "e2e/tests/player-insight.spec.ts"
      provides: "Extended with a netball case and a rugby-league case (long-press → summary in-game + season sections render)"
      contains: "netball"
  key_links:
    - from: "src/components/netball/NetballLiveGame.tsx"
      to: "src/lib/player-insight.ts"
      via: "build the view-model for the long-pressed player (config thirds, playerThirdMs in-game, per-period from replay, season position-counts->thirds as %, lastSubbedOnMs) and pass <PlayerInsightSummary/> into NetballPlayerActions' insight slot"
      pattern: "buildPlayerInsight"
    - from: "src/components/league/LeagueLiveGame.tsx"
      to: "src/lib/player-insight.ts"
      via: "build the view-model (single 'field' config zone, playerMsOnField in-game, per-period field ms from replay, lastSubbedOnMs) and pass <PlayerInsightSummary/> into the existing LockModal insight slot"
      pattern: "buildPlayerInsight"
    - from: "src/components/netball/NetballPlayerActions.tsx"
      to: "src/components/live/PlayerInsightSummary.tsx"
      via: "render the shared summary block via the new insight slot"
      pattern: "insight"
---

<objective>
PLAYERVIEW-01 + PLAYERVIEW-02 / F3 (netball + rugby-league mirror): make the
long-press player-insight summary work in netball and rugby league, reusing the
shared `buildPlayerInsight` builder and `PlayerInsightSummary` component from
plan 12-01 VERBATIM. The only per-sport work is (a) deriving the per-period
breakdown from each sport's own replay, (b) feeding the builder sport-correct
inputs (config zones, in-game per-zone ms, season per-zone as a consistent
unit, lastSubbedOnMs), and (c) opening the summary inside each sport's EXISTING
long-press host: `NetballPlayerActions` (add the same `insight` slot LockModal
got) for netball, and the already-shared `LockModal` for rugby league.

No new modal, no new summary component, no migration, no new store slice, no new
GameEventType, no new server action (D-01/D-02/D-08). AFL (plan 12-01) is the
reference; this plan mirrors it.

DECISIONS LOCKED (see 12-CONTEXT.md):
- D-01 reuse the existing long-press host per sport (NetballPlayerActions /
  LockModal); add an optional `insight` slot to NetballPlayerActions to match the
  one LockModal already has.
- D-02 reuse the shared `buildPlayerInsight` + `PlayerInsightSummary` from 12-01.
- D-03 zones from `getAgeGroupConfig(...).zones` (netball thirds; RL single
  "field"), never hardcoded.
- D-04 season percentages only (the builder is unit-agnostic for %; netball may
  pass season third-quarters/minutes, RL the single field unit — keep each
  sport's season unit consistent with its in-game unit).
- D-05 per-period breakdown derived from each engine's replay (netball per-third,
  league per-field) — the netball/league equivalents of the AFL
  `playedZoneMsByPeriod`.
- D-06 time-since-last-sub reuses each replay's existing `lastSubbedOnMs`.

DEV note (honest scope): rugby league's config `zones` is a SINGLE "field" zone,
so RL's per-zone view collapses to total field time and a 100%-field season
split — correct given the config (forwards/backs are vests/roles, not config
zones; re-modelling them is out of scope). The MEANINGFUL RL signals are
time-since-last-sub + per-period field minutes, which are real.

Output: netball + rugby-league long-press summaries reusing the shared core,
with red-first per-period replay coverage for both engines and e2e cases for
both sports; all four DoD gates green; Phase 12 functionally complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MATCH-DAY-CHANGES-SPEC.md
@.planning/phases/12-long-press-player-insight/12-CONTEXT.md
@.planning/phases/12-long-press-player-insight/12-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- All paths against the MAIN checkout. Use Bash (cd into main checkout) or
     absolute-path Read — file-search tools default to a stale worktree. -->

REUSED FROM WAVE 1 (do not re-create):
- src/lib/player-insight.ts — `buildPlayerInsight(input)` + PlayerInsight types.
  Sport-neutral: feed it config ZoneDef[], in-game per-zone ms, per-period zoneMs,
  season per-zone (any consistent unit — % is unit-agnostic), lastSubbedOnMs,
  nowAbsMs.
- src/components/live/PlayerInsightSummary.tsx — the shared summary block
  (`player-insight-ingame` / `-periods` / `-season` testids). Consume verbatim.
- src/components/live/LockModal.tsx — already has the optional `insight?` slot
  (added in 12-01). Rugby league uses LockModal, so RL gets the slot for free.

NETBALL (src/components/netball/):
- Host: NetballPlayerActions.tsx (the netball long-press fork) — ADD an optional
  `insight?: React.ReactNode` prop and render `{insight}` under the
  "Player actions" header (mirror the LockModal slot). Existing callers
  unaffected.
- NetballLiveGame.tsx already imports `playerThirdMs` (:77, used :1025/:1076),
  `seasonPositionCounts`/`gamePositionCounts` (:76/:78), has `seasonEvents`
  (:254), `replayNetballGame` state (incl. `lastSubbedOnMs`), `ageGroup`, and
  `handleTokenLongPress` → `actionsTarget` → <NetballPlayerActions> (:2125).
  - In-game per-third ms = `playerThirdMs(...)` for the long-pressed player.
  - Per-period per-third ms = the netball equivalent of playedZoneMsByPeriod
    (Task 1/2): extend `replayNetballGame` with a per-period third accumulator, OR
    add a per-period variant of `playerThirdMs`. Choose the lower-risk option;
    cover red-first.
  - Season per-zone(third) % = map `seasonPositionCounts(seasonEvents)` to thirds
    (position -> allowedZones/third) and feed as `seasonZoneMs` (unit =
    quarters/counts; the builder turns it into %). Keep the in-game unit and the
    season unit each internally consistent.
  - zones = `getAgeGroupConfig('netball', ageGroup).zones` mapped to the netball
    ZoneDef[] (the 5 thirds with labels).
  - lastSubbedOnMs from the netball replay state; nowAbsMs from the netball
    completed-period + current-elapsed frame already used for its recency guard.

RUGBY LEAGUE (src/components/league/LeagueLiveGame.tsx):
- Host: the EXISTING shared LockModal (:2271) — already has the `insight` slot.
- Already imports `playerMsOnField` (:65, used :404) + `playerZoneMsOnField`
  (:66/:413), has `seasonEvents` (:137), `replayLeagueGame` state (incl.
  `lastSubbedOnMs`), `ageGroup`, `isLastPeriod` (:582), and
  `actionSheetPlayer`/`actionSheetPlayerId` (:1565).
  - zones = `getAgeGroupConfig('rugby_league', ageGroup).zones` → the single
    "field" ZoneDef.
  - In-game per-zone ms = `playerMsOnField(...)` for the player (single "field"
    bucket).
  - Per-period field ms = the league equivalent of playedZoneMsByPeriod (Task
    1/2): extend `replayLeagueGame` with a per-period on-field-ms accumulator, OR a
    per-period variant of `playerMsOnField`. Cover red-first.
  - Season = single field unit → builder yields 100% field (or 0). Honest given
    the config.
  - lastSubbedOnMs from the league replay state; nowAbsMs from the league
    completed-period + current-elapsed frame.

NO HARDCODED PERIOD/ZONE: gate any period labelling via `ageGroup.periodCount` /
period noun (league already uses periodCount; netball mirror). Zones strictly
from config.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — netball + rugby-league per-period replay specs</name>
  <read_first>
    - src/lib/__tests__/playedZoneMsByPeriod.test.ts (the AFL reference spec from 12-01 — mirror its event-fixture style)
    - src/lib/sports/netball/fairness.ts:828-980 (replayNetballGame + state), :981-1144 (playerThirdMs + ThirdLookup), :246 (seasonPositionCounts)
    - src/lib/sports/rugby_league/fairness.ts:91-210 (replayLeagueGame + state), :1560-1741 (playerMsOnField), :1751 (playerZoneMsOnField / LeagueZoneMs)
    - src/lib/sports/netball/index.ts:30-115 (NETBALL_ZONES + age-group zones) + src/lib/sports/rugby_league/index.ts:48-350 (RL_ZONES = single "field")
  </read_first>
  <action>
Create TWO red-first specs that assert each engine derives a per-PERIOD x
per-ZONE ms breakdown from the event replay (the netball/league equivalents of
the AFL `playedZoneMsByPeriod`):

(1) src/lib/__tests__/playedZoneMsByPeriod.netball.test.ts — build a synthetic
netball event stream across ≥2 quarters with players moving between thirds and
a mid-quarter sub (when allowed); assert the per-period per-third ms credits the
RIGHT period and third, and that summing a player's per-period buckets equals
their full-game per-third ms from `playerThirdMs(...)` (cross-check against the
existing output).

(2) src/lib/__tests__/playedZoneMsByPeriod.league.test.ts — build a synthetic RL
event stream across the 2 halves (use ageGroup.periodCount, not a literal);
assert the per-period on-field ms credits the right half, summing to the player's
full-game on-field ms from `playerMsOnField(...)`.

Use the SAME accessor name the AFL replay used (`playedZoneMsByPeriod`) keyed by
config zone id (netball third ids; RL "field"), so the live wiring is uniform.
Write RED-first (the new accessor/field is absent → fails). Commit:
`test(12-02): add failing netball + rugby-league per-period zone-ms replay specs`.
  </action>
  <verify>
    <automated>cd "/c/Users/steve/OneDrive/Documents/Auskick manager" && npx vitest run src/lib/__tests__/playedZoneMsByPeriod.netball.test.ts src/lib/__tests__/playedZoneMsByPeriod.league.test.ts 2>&1 | tail -25; echo "EXPECT: RED — per-period accessors not implemented yet for netball/league"</automated>
  </verify>
  <acceptance_criteria>
    - Both specs exist, reference `playedZoneMsByPeriod`, and cross-check per-period buckets sum to the existing per-zone output (`playerThirdMs` for netball, `playerMsOnField` for league)
    - The league spec derives the period count from `ageGroup.periodCount` (no hardcoded 2/4)
    - Both specs FAIL against current code (RED)
    - Commit message matches `test(12-02):`
  </acceptance_criteria>
  <done>Red-first per-period replay specs for netball + rugby league are committed and fail against pre-implementation code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — extend netball + league replays; wire both hosts</name>
  <read_first>
    - the Task-1 specs (the contracts)
    - src/lib/sports/netball/fairness.ts (replayNetballGame credit/flush sites + playerThirdMs) ; src/lib/sports/rugby_league/fairness.ts (replayLeagueGame credit sites + playerMsOnField)
    - src/components/netball/NetballPlayerActions.tsx (Props interface + header block) ; src/components/live/LockModal.tsx (the insight slot added in 12-01, as the pattern)
    - src/components/netball/NetballLiveGame.tsx (:1025/:1076 playerThirdMs usage, the actionsTarget/<NetballPlayerActions> render :2125, ageGroup, seasonEvents, replay state) ; src/components/league/LeagueLiveGame.tsx (:404 playerMsOnField, the LockModal render :2271, ageGroup, seasonEvents, replay state)
    - src/lib/player-insight.ts + src/components/live/PlayerInsightSummary.tsx (the shared core to reuse verbatim)
  </read_first>
  <action>
STEP A — extend the replays to satisfy Task-1: add the `playedZoneMsByPeriod`
accessor (per-period x per-zone ms, keyed by config zone id) to
`replayNetballGame` (per third) and `replayLeagueGame` (per "field"), at the
existing per-zone credit sites; do NOT change existing outputs.

STEP B — netball host slot: add an optional `insight?: React.ReactNode` prop to
NetballPlayerActions and render `{insight}` under the "Player actions" header
(mirror the LockModal slot). Existing callers unaffected.

STEP C — netball wiring: in NetballLiveGame, when `actionsTarget` is open, build
the view-model with `buildPlayerInsight(...)` — zones from
`getAgeGroupConfig('netball', ageGroup).zones` (the labelled thirds), in-game
per-third ms from `playerThirdMs(...)`, per-period from the netball
`playedZoneMsByPeriod`, season from `seasonPositionCounts(seasonEvents)` mapped to
thirds (as %), lastSubbedOnMs from the netball replay, nowAbsMs from the netball
frame — and pass `insight={<PlayerInsightSummary insight={vm} />}` into
<NetballPlayerActions>.

STEP D — rugby-league wiring: in LeagueLiveGame, when `actionSheetPlayer` is set,
build the view-model — zones = the single "field" ZoneDef from
`getAgeGroupConfig('rugby_league', ageGroup).zones`, in-game from
`playerMsOnField(...)`, per-period from the league `playedZoneMsByPeriod`, season
single-field, lastSubbedOnMs from the league replay, nowAbsMs from the league
frame — and pass `insight={<PlayerInsightSummary insight={vm} />}` into the
existing <LockModal> (do NOT add a new modal).

Run both Task-1 specs → GREEN. Commit:
`feat(12-02): netball + rugby-league per-period zone-ms + long-press insight summary`.
  </action>
  <verify>
    <automated>cd "/c/Users/steve/OneDrive/Documents/Auskick manager" && npx tsc --noEmit && npx vitest run src/lib/__tests__/playedZoneMsByPeriod.netball.test.ts src/lib/__tests__/playedZoneMsByPeriod.league.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `replayNetballGame` and `replayLeagueGame` each expose `playedZoneMsByPeriod` keyed by config zone id; existing outputs unchanged (existing netball/league fairness specs still pass)
    - NetballPlayerActions has an optional `insight?` prop rendered under its header; netball + league live surfaces pass `<PlayerInsightSummary .../>` built via the SHARED `buildPlayerInsight` (grep `buildPlayerInsight` in both NetballLiveGame and LeagueLiveGame)
    - No new summary component and no new modal created; rugby league uses the existing LockModal slot; netball uses the existing NetballPlayerActions (grep: no new `*PlayerInsight*`/`*Summary*` component beyond the shared one from 12-01)
    - Zones built from `getAgeGroupConfig(...).zones` in both wirings (no hardcoded third/zone arrays)
    - `npx tsc --noEmit` exits 0; both Task-1 specs PASS
    - Commit message matches `feat(12-02):`
  </acceptance_criteria>
  <done>Netball + rugby-league replays derive the per-period breakdown and both long-press hosts render the SHARED insight summary, reusing the 12-01 core verbatim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — netball + league e2e + DoD gates</name>
  <read_first>
    - e2e/tests/player-insight.spec.ts (the AFL case from 12-01 — extend it) + e2e/tests/netball-live-flow.spec.ts + e2e/tests/rugby-league-chips.spec.ts (netball/league game setup + long-press trigger patterns)
    - e2e/README.md (when-to-add-a-test) + playwright.config.ts (Phase-9 environmental protocol; known flakes: demo-picker netball, rugby-league-full-game-playthrough)
  </read_first>
  <action>
Extend e2e/tests/player-insight.spec.ts with:
  - a NETBALL case: set up a netball game with ≥1 completed quarter, long-press a
    player to open NetballPlayerActions, and assert the `player-insight-ingame` and
    `player-insight-season` testids render (and the netball actions still show).
  - a RUGBY-LEAGUE case: set up an RL game with ≥1 completed period, long-press a
    player to open LockModal, and assert the `player-insight-ingame` and
    `player-insight-season` testids render.
Reuse each sport's existing setup helpers; keep each case deterministic and
single-purpose.

Then run ALL FOUR DoD gates: `npx tsc --noEmit`, `npm run lint`, `npm test`,
`npm run e2e` (Phase-9 environmental protocol — if the full multi-worker run is
flaky on this box, isolate `player-insight.spec.ts` with `--workers=1` and record
the caveat in the SUMMARY). Update only specs whose behaviour genuinely shifted;
do not loosen. Commit:
`feat(12-02): netball + rugby-league long-press insight e2e + DoD gates`.
  </action>
  <verify>
    <automated>cd "/c/Users/steve/OneDrive/Documents/Auskick manager" && npx tsc --noEmit && npm run lint && npm test 2>&1 | tail -15 && npm run e2e -- player-insight.spec.ts --workers=1 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/tests/player-insight.spec.ts` includes a netball case and a rugby-league case, each opening the long-press host and asserting `player-insight-ingame` + `player-insight-season` render
    - All four Task specs in the per-period replay family (AFL from 12-01 + netball + league) pass
    - No migration / new GameEventType / new store slice / new server action introduced (supabase/migrations diff empty for this plan)
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0 (no NEW warnings); `npm test` exits 0; the player-insight e2e (AFL + netball + league) is GREEN (`--workers=1` acceptable per Phase-9, note in SUMMARY)
    - Commit message matches `feat(12-02):`
  </acceptance_criteria>
  <done>Netball + rugby-league long-press summaries are exercised end-to-end through the real UI alongside AFL; all four DoD gates green; Phase 12 functionally complete across all three sports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| long-pressed player -> insight view-model (client, netball/league) | Same as 12-01: pure in-memory derivation from replay state + the season events already fetched for each sport's live surface. No new network input. |
| season events -> season per-zone % | Netball/league season events are already fetched via the existing team-scoped, RLS-guarded path and passed as `seasonEvents`. F3 only re-shapes them into percentages. |
| in-game replay state -> per-period breakdown | Additive `playedZoneMsByPeriod` accessor on each engine; existing outputs untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-A | Information disclosure (cross-team leak) | netball/league season % | accept | Reuses each sport's existing team-scoped `seasonEvents`; no new query, no new exposure. Same data already shown on tile bars / season views. |
| T-12-02-B | Tampering / Integrity (wrong numbers) | replayNetballGame/replayLeagueGame per-period + builder | mitigate | Per-period accessors are additive and covered red-first, cross-checked to sum to the existing per-zone outputs (`playerThirdMs` / `playerMsOnField`); existing outputs asserted unchanged. The shared builder is already covered in 12-01. |
| T-12-02-C | Elevation / unauthorized mutation | whole feature | accept | Read-only: no new server action, write path, GameEventType, migration, or store slice. The hosts' existing mutating actions are untouched (NetballPlayerActions/LockModal action sets unchanged). |
| T-12-02-D | Consistency / cross-sport drift | shared builder + summary reused | mitigate | Reuse-before-fork: netball + league consume the SAME `buildPlayerInsight` + `PlayerInsightSummary` from 12-01 verbatim; only sport-correct inputs differ. Grep asserts no forked summary/modal. |

No migration, no schema change, no new auth surface, no new game-event type, no
new store slice — additive read-only replay accessor + presentational reuse.
</threat_model>

<verification>
- Netball long-press opens NetballPlayerActions with the SHARED summary (in-game
  per-third + last-sub, per-period, season %); rugby-league long-press opens the
  shared LockModal with the same summary (single "field" zone) — both reuse the
  12-01 builder + component verbatim (no fork).
- Each engine derives `playedZoneMsByPeriod` consistent with its existing per-zone
  output; existing fairness specs unchanged (netball + league per-period specs
  GREEN).
- Zones come from `getAgeGroupConfig(...).zones` (netball thirds; RL "field");
  season is percentages only.
- Read-only: no migration / new event type / new store slice / new server action.
- All four DoD gates green (e2e per the Phase-9 environmental protocol if the
  full multi-worker run is flaky on this box).
</verification>

<success_criteria>
ROADMAP Phase 12 criteria #1-#4 now satisfied ACROSS ALL THREE SPORTS:
- #1: in-game per-zone time + time-since-last-sub + per-period minutes-per-zone
  (derived from the event replay) — netball + rugby league join AFL.
- #2: season per-zone split as percentages only — all sports.
- #3: zones enumerated from `getAgeGroupConfig(...).zones` — netball thirds + RL
  "field" render correctly.
- #4: the summary opens from the existing long-press host in each sport
  (NetballPlayerActions / LockModal — reuse-before-fork) and an e2e verifies the
  in-game + season sections render for netball + league.
</success_criteria>

<output>
After completion, create `.planning/phases/12-long-press-player-insight/12-02-SUMMARY.md`
</output>
