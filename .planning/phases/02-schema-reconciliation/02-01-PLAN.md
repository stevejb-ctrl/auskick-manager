---
phase: 02-schema-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-04
must_haves:
  truths:
    - "02-SCHEMA-PLAN.md exists and contains §1 Hash verification, §2 File ops for Phase 3, §3 SCHEMA-02 audit, §4 SCHEMA-04 audit (sections §5 and §6 are pre-stubbed, filled by Plan 03)"
    - "§1 records both sha256 hashes from `git show` and confirms they equal 1761d4042751b82e3cd284d73ff5c9110741ca7686ceb10dcba2a74fb4693051 (D-10 deletion is safe to plan)"
    - "§2 documents — does not execute — the deletion of main:supabase/migrations/0024_super_admin.sql for Phase 3's merge resolution (D-10, D-11)"
    - "§3 confirms SCHEMA-02 is satisfied as-shipped: 0024_multi_sport.sql lines 25-27 perform `add column sport text not null default 'afl'` atomically — no extra backfill statement needed"
    - "§4 confirms SCHEMA-04 migration-content side: zero `DROP TABLE`/`DROP COLUMN`/`DROP POLICY`/`DROP TRIGGER`/`DROP FUNCTION` across the three new migrations; the only DROPs are two `drop constraint` lines (teams_age_group_check + game_events_type_check), both safe (D-16)"
    - "supabase/migrations/ on this branch is unchanged at end of plan (D-11 — read-only invariant)"
    - "git status against src/, e2e/, supabase/, scripts/ remains clean throughout"
  artifacts:
    - path: ".planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md"
      provides: "Renumbering plan + SCHEMA-02/SCHEMA-04 audit deliverable for Phase 3 to act on (D-18)"
      contains: "1761d4042751b82e3cd284d73ff5c9110741ca7686ceb10dcba2a74fb4693051"
      min_lines: 80
  key_links:
    - from: "02-SCHEMA-PLAN.md §2 (File ops for Phase 3)"
      to: "Phase 3 merge resolution"
      via: "Documented — but not executed — rename/delete steps for supabase/migrations/"
      pattern: "supabase/migrations/0024_super_admin.sql"
    - from: "02-SCHEMA-PLAN.md §3 (SCHEMA-02 audit)"
      to: "Phase 6 prod-clone runtime check (deferred per D-17)"
      via: "Atomicity claim recorded; Phase 6 verifies live"
      pattern: "not null default 'afl'"
    - from: "02-SCHEMA-PLAN.md §4 (SCHEMA-04 audit)"
      to: "Phase 6 prod-clone runtime check (D-17)"
      via: "Migration-content audit complete; live AFL data verification deferred"
      pattern: "DROP TABLE|DROP COLUMN|DROP POLICY|DROP TRIGGER|DROP FUNCTION"
---

<objective>
Produce §§1-4 of `.planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md`: re-verify the sha256 equality of main:0024_super_admin.sql vs multi-sport:0025_super_admin.sql (locking in D-10), document the file-ops Phase 3's merge resolution will perform on `supabase/migrations/`, and audit the three new multi-sport migrations (`0024_multi_sport.sql`, `0026_team_quarter_seconds.sql`, `0027_game_quarter_seconds.sql`) for SCHEMA-02 (backfill atomicity) and SCHEMA-04 (no destructive ops on existing AFL data).

**Purpose:** Phase 3's merge can rename/delete migration files only if Phase 2 has produced a written plan to do so. Phase 6's prod-clone validation can confidently run only if the migration-content audit has already established that nothing destructive ships in the new migrations. This plan produces both as deterministic, grep-verifiable audit output captured in `02-SCHEMA-PLAN.md`.

**Output:** `.planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md` with §§1-4 fully populated, plus stub headings for §§5-6 (filled by Plan 03).

**Hard scope discipline (read-only on source tree):**
- NO writes to `supabase/migrations/` on this worktree (D-11). Reads only.
- NO source code edits (`src/`, `e2e/`, `scripts/`).
- NO migration runtime checks on this branch — `supabase migration up` runs are Phase 3+ work.
- NO prod-clone validation (Phase 6, deferred per D-17).
- ONLY writes: `.planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/02-schema-reconciliation/02-CONTEXT.md
@.planning/phases/02-schema-reconciliation/02-RESEARCH.md
@.planning/phases/02-schema-reconciliation/02-PATTERNS.md
@.planning/phases/01-divergence-inventory-merge-plan/01-MERGE-NOTES.md
@CLAUDE.md

<known_facts>
- Hash equality re-confirmed in research session: both `git show main:supabase/migrations/0024_super_admin.sql | sha256sum` and `git show multi-sport:supabase/migrations/0025_super_admin.sql | sha256sum` produce `1761d4042751b82e3cd284d73ff5c9110741ca7686ceb10dcba2a74fb4693051 *-` (RESEARCH §3)
- Live grep run in research session: `grep -nE "DROP|RENAME|REVOKE" ../multi-sport/supabase/migrations/0024_multi_sport.sql ../multi-sport/supabase/migrations/0026_team_quarter_seconds.sql ../multi-sport/supabase/migrations/0027_game_quarter_seconds.sql` returns exactly two matches, both `drop constraint` in `0024_multi_sport.sql` (lines 48 and 67) — RESEARCH §1
- Live grep run in research session: `grep -E "DROP TABLE|DROP COLUMN|DROP POLICY|DROP TRIGGER|DROP FUNCTION" ../multi-sport/supabase/migrations/0024_multi_sport.sql ../multi-sport/supabase/migrations/0026_team_quarter_seconds.sql ../multi-sport/supabase/migrations/0027_game_quarter_seconds.sql` exits 1 with no matches — RESEARCH §1
- `0024_multi_sport.sql:25-27` content: `alter table public.teams \n  add column if not exists sport text not null default 'afl' \n    check (sport in ('afl','netball'));` — atomic single ALTER TABLE — RESEARCH §1 backfill semantics
- Multi-sport sibling worktree at `../multi-sport/` is checked out at HEAD `1277068`; cross-worktree reads acceptable (CONTEXT.md discretion)
- `supabase/migrations/` collision is a single file: main has `0024_super_admin.sql`, multi-sport has `0024_multi_sport.sql` + `0025_super_admin.sql` (byte-identical to main's 0024) + `0026_team_quarter_seconds.sql` + `0027_game_quarter_seconds.sql` — Phase 1 §2 confirms; D-10 locks deletion of main's 0024
</known_facts>

<storage_decision>
**File location: `.planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md` (D-18 — Phase 2's own phase directory, NOT an extension of `01-MERGE-NOTES.md`).**

**Justification:** Phase 1's MERGE-NOTES is sealed; Phase 2 owns its own audit deliverable. Phase 3 reads §2 (file ops) directly from this path; Phase 6 reads §6 (handoff) — kept in the same file so the handoff inherits the audit's authority. Locking this in CONTEXT.md D-18.
</storage_decision>

<read_only_enforcement>
- All audit grep/sed/git-show commands are read-only.
- The two `git show` invocations operate on branch refs, not the working tree.
- The cross-worktree reads (`../multi-sport/supabase/migrations/...`) operate on the sibling worktree's committed files — never modify them.
- Verification: `git status -- supabase/migrations/ src/ e2e/ scripts/` must remain clean before AND after every task.
</read_only_enforcement>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hash re-verification + file-ops plan (§§1-2 of 02-SCHEMA-PLAN.md)</name>
  <files>.planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md</files>
  <read_first>
    - .planning/phases/02-schema-reconciliation/02-CONTEXT.md (D-10, D-11, D-18 — locked decisions; do not propose alternatives)
    - .planning/phases/02-schema-reconciliation/02-RESEARCH.md §3 "Hash equality re-confirmed" + §5 "Hash verification" + §9 item 1 (D-10/D-05 locked, do not revisit)
    - .planning/phases/02-schema-reconciliation/02-PATTERNS.md §"02-SCHEMA-PLAN.md (planning doc)" — structural template (top matter, §-numbered headings, fenced bash blocks, italicised metadata footer)
    - .planning/phases/01-divergence-inventory-merge-plan/01-MERGE-NOTES.md §2 (Migration-number conflicts) — the original collision analysis Phase 2 is now acting on
  </read_first>
  <action>
**Step A — Re-verify the hash equality live, before writing the doc.**

Run these two commands in the current worktree (works in git-bash on Windows per RESEARCH §5; `sha256sum` is part of msys/git-bash core):

```bash
git show main:supabase/migrations/0024_super_admin.sql | sha256sum
git show multi-sport:supabase/migrations/0025_super_admin.sql | sha256sum
```

Both MUST output exactly:
```
1761d4042751b82e3cd284d73ff5c9110741ca7686ceb10dcba2a74fb4693051 *-
```

If either hash differs from this expected value → STOP, do NOT write the doc, surface the divergence to the user (D-10 invariant broken; the de-dup plan is no longer safe and the locked decision must be re-discussed).

Capture the literal stdout of both commands verbatim — they go into §1 of the doc as evidence.

**Step B — Create the doc skeleton with all six section headings.**

Use the Write tool (NOT heredoc) to create `.planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md` with this exact skeleton (top matter follows 01-MERGE-NOTES.md pattern; section numbering uses `## §N` style as flagged in the deep-work-rules acceptance-criteria library):

```markdown
# Phase 2 — Schema reconciliation plan: migration de-dup, audit, e2e spec design, Phase 6 handoff

**Generated:** 2026-04-29
**Phase:** 02-schema-reconciliation
**Requirements:** SCHEMA-01, SCHEMA-02, SCHEMA-03, SCHEMA-04
**Consumers:** Phase 3 (merge resolution acts on §2), Phase 6 (prod-clone validation acts on §6)
**Read-only invariant on this branch:** No writes to `supabase/migrations/` (D-11). The rename/delete documented in §2 is executed by Phase 3, not Phase 2.

## §1 Hash verification (D-10 lock)
[fill — Task 1]

## §2 File ops for Phase 3 (D-10 + D-11 — documented, not executed)
[fill — Task 1]

## §3 SCHEMA-02 audit — backfill atomicity
[fill — Task 2]

## §4 SCHEMA-04 audit — destructive-ops absence on existing AFL data
[fill — Task 2]

## §5 SCHEMA-03 e2e spec design (filled by Plan 03)
_To be filled by Plan 03 Task 2 — spec design notes pointing at `e2e/tests/multi-sport-schema.spec.ts`._

## §6 Phase 6 handoff — prod-clone acceptance criteria for SCHEMA-04 (filled by Plan 03)
_To be filled by Plan 03 Task 2 — five acceptance criteria from CONTEXT.md `<specifics>` "Phase 6 handoff acceptance criteria"._

*Generated: 2026-04-29 by /gsd-execute-phase 02 Plan 01.*
```

**Step C — Fill §1 Hash verification.**

Replace `[fill — Task 1]` under §1 with this exact content (substituting in the live stdout from Step A; both outputs MUST match the expected hash, otherwise Step A's STOP rule has already fired):

```markdown
**Decision lock:** D-10 — main's `supabase/migrations/0024_super_admin.sql` is byte-identical to multi-sport's `supabase/migrations/0025_super_admin.sql`. Phase 3's merge resolution deletes main's 0024 outright (no rename, no preservation as no-op).

**Re-verification commands run on `claude/vibrant-banzai-a73b2f` HEAD ee6ca17 on 2026-04-29:**

\```bash
$ git show main:supabase/migrations/0024_super_admin.sql | sha256sum
1761d4042751b82e3cd284d73ff5c9110741ca7686ceb10dcba2a74fb4693051 *-

$ git show multi-sport:supabase/migrations/0025_super_admin.sql | sha256sum
1761d4042751b82e3cd284d73ff5c9110741ca7686ceb10dcba2a74fb4693051 *-
\```

Both hashes equal `1761d4042751b82e3cd284d73ff5c9110741ca7686ceb10dcba2a74fb4693051`. **D-10 deletion of main:0024_super_admin.sql is safe.** Phase 1 §2 captured the same equality at main@80a04eb / multi-sport@1277068; this re-verification at vibrant-banzai-a73b2f@ee6ca17 confirms no drift.
```

(Use a single backslash before each backtick fence inside the markdown — when the executor writes the file, the fenced block renders as a literal bash example.)

**Step D — Fill §2 File ops for Phase 3.**

Replace `[fill — Task 1]` under §2 with this exact content:

```markdown
**Decision locks:** D-10 (delete main's 0024_super_admin.sql), D-11 (this branch does NOT modify `supabase/migrations/` — Phase 3 executes during merge resolution).

**File ops Phase 3's merge resolution MUST execute (in this order, on the merged trunk — NOT this branch):**

| # | Operation | File | Rationale |
|---|-----------|------|-----------|
| 1 | DELETE | `supabase/migrations/0024_super_admin.sql` (main's copy — coming from main side of the merge) | Byte-identical to multi-sport's `0025_super_admin.sql` per §1 hash verification. Keeping both would produce duplicate-numbered migrations and a redundant SQL run. |
| 2 | KEEP unchanged | `supabase/migrations/0024_multi_sport.sql` (from multi-sport side) | Sport column + age-group constraint relaxation; no main-side equivalent. |
| 3 | KEEP unchanged | `supabase/migrations/0025_super_admin.sql` (from multi-sport side) | The canonical super-admin migration after the rename equivalence; identical to main's old 0024. |
| 4 | KEEP unchanged | `supabase/migrations/0026_team_quarter_seconds.sql` (from multi-sport side) | Per-team quarter-length override; no main-side equivalent. |
| 5 | KEEP unchanged | `supabase/migrations/0027_game_quarter_seconds.sql` (from multi-sport side) | Per-game quarter-length override; no main-side equivalent. |

**Post-merge migration ordering** (final state Phase 3 produces):

```
supabase/migrations/
  0001_… through 0023_…    (shared between branches, identical)
  0024_multi_sport.sql      (multi-sport-only)
  0025_super_admin.sql      (formerly main's 0024 — same bytes, renumbered)
  0026_team_quarter_seconds.sql
  0027_game_quarter_seconds.sql
```

**This is monotonic and unique** — SCHEMA-01 satisfied as-of-Phase-3-merge-resolution. Phase 2 documents the plan; Phase 3 executes it.

**Phase 3 verification command (NOT to be run on this branch):**

\```bash
# After Phase 3 merge resolution lands the file ops above:
npm run db:reset  # equivalent to: supabase db reset --no-confirm
supabase migration list  # must show 27 migrations applied in order, no pending, no duplicates
\```

**Read-only invariant verification (run NOW, on this branch, after writing this doc):**

\```bash
git status -- supabase/migrations/  # must be clean — Phase 2 does NOT modify migrations
\```
```

**Step E — Read-only invariant verification.**

After writing the doc, run `git status -- supabase/migrations/` and confirm clean output. If anything in `supabase/migrations/` shows as modified → STOP, surface to user, D-11 is broken.
  </action>
  <acceptance_criteria>
    - File `.planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md` exists.
    - `grep -c "1761d4042751b82e3cd284d73ff5c9110741ca7686ceb10dcba2a74fb4693051" .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md` returns at least 3 (decision header + two `sha256sum` outputs).
    - `grep -E "^## §" .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md | wc -l` returns exactly 6 (§1 through §6 headings present).
    - `grep -E "^## §[1-2]" .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md | grep -v 'To be filled' | wc -l` returns 2 (§1 and §2 are populated, not stubs).
    - `grep -c 'D-10' .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md` returns at least 1 (decision lock cited).
    - `grep -c 'D-11' .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md` returns at least 1 (read-only invariant cited).
    - `git status --porcelain -- supabase/migrations/` returns empty (zero lines) — read-only invariant held.
    - `git status --porcelain -- src/ e2e/ scripts/ package.json` returns empty (zero lines) — Plan 01 doesn't touch source/test/config.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md &amp;&amp; [ "$(grep -E '^## §' .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md | wc -l)" -eq 6 ] &amp;&amp; [ "$(grep -c '1761d4042751b82e3cd284d73ff5c9110741ca7686ceb10dcba2a74fb4693051' .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md)" -ge 3 ] &amp;&amp; [ -z "$(git status --porcelain -- supabase/migrations/ src/ e2e/ scripts/ package.json)" ]</automated>
  </verify>
  <done>
    - 02-SCHEMA-PLAN.md exists with all six §-numbered headings present (§§5-6 are stubs marked `_To be filled by Plan 03 Task 2_`).
    - §1 contains the live `git show ... | sha256sum` outputs and confirms equality at the expected hash.
    - §2 contains the file-ops table for Phase 3 (5 rows: 1 DELETE, 4 KEEP) and the post-merge ordering listing 24 + 0024_multi_sport + 0025_super_admin + 0026_team + 0027_game.
    - Read-only invariant verified: `supabase/migrations/`, `src/`, `e2e/`, `scripts/`, `package.json` are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: SCHEMA-02 + SCHEMA-04 audit (§§3-4 of 02-SCHEMA-PLAN.md)</name>
  <files>.planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md</files>
  <read_first>
    - .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md (the file Task 1 just wrote — replace the §3 and §4 stubs only; do NOT touch §1, §2, §5, §6)
    - .planning/phases/02-schema-reconciliation/02-CONTEXT.md (D-16 audit scope; D-17 deferral to Phase 6)
    - .planning/phases/02-schema-reconciliation/02-RESEARCH.md §1 "Migration Runtime Semantics" (NOT NULL DEFAULT atomicity proof, the two grep audit outputs verified live)
    - ../multi-sport/supabase/migrations/0024_multi_sport.sql (read line-by-line; the file is verified by RESEARCH but Task 2 writes its own audit lines, so re-read to confirm)
    - ../multi-sport/supabase/migrations/0026_team_quarter_seconds.sql (read full)
    - ../multi-sport/supabase/migrations/0027_game_quarter_seconds.sql (read full)
  </read_first>
  <action>
**Step A — Re-run the two audit grep commands live and capture stdout verbatim.**

```bash
# Audit 1 — DROP/RENAME/REVOKE scan (expected: exactly 2 matches, both `drop constraint` in 0024_multi_sport.sql)
grep -nE "DROP|RENAME|REVOKE" \
  ../multi-sport/supabase/migrations/0024_multi_sport.sql \
  ../multi-sport/supabase/migrations/0026_team_quarter_seconds.sql \
  ../multi-sport/supabase/migrations/0027_game_quarter_seconds.sql

# Audit 2 — destructive-ops scan (expected: exit 1, no output)
grep -E "DROP TABLE|DROP COLUMN|DROP POLICY|DROP TRIGGER|DROP FUNCTION" \
  ../multi-sport/supabase/migrations/0024_multi_sport.sql \
  ../multi-sport/supabase/migrations/0026_team_quarter_seconds.sql \
  ../multi-sport/supabase/migrations/0027_game_quarter_seconds.sql
```

**Audit 1 expected output (per RESEARCH §1):**
```
../multi-sport/supabase/migrations/0024_multi_sport.sql:48:    alter table public.teams drop constraint teams_age_group_check;
../multi-sport/supabase/migrations/0024_multi_sport.sql:67:    alter table public.game_events drop constraint game_events_type_check;
```
(Exact line numbers may shift if the file changed; the count must be exactly 2 and both must be `drop constraint` in `0024_multi_sport.sql`. If audit 1 returns more or fewer matches, or any match is in `0026_*` or `0027_*`, or any match is `DROP TABLE`/`DROP COLUMN`/etc. → STOP, surface to user, SCHEMA-04 audit assumption is broken.)

**Audit 2 expected output:** empty (exit code 1). If audit 2 returns ANY output → STOP, surface to user, SCHEMA-04 is in danger.

**Step B — Re-read 0024_multi_sport.sql lines 25-27 and capture verbatim.**

```bash
sed -n '25,27p' ../multi-sport/supabase/migrations/0024_multi_sport.sql
```

Expected output (per RESEARCH §1 backfill semantics):
```
alter table public.teams
  add column if not exists sport text not null default 'afl'
    check (sport in ('afl','netball'));
```

If the output differs (e.g. the line range shifts or the statement is split across more lines or the default is not `'afl'`) → STOP, surface to user, SCHEMA-02 audit assumption is broken.

**Step C — Use the Edit tool to replace the §3 stub with this content.**

Locate the line `## §3 SCHEMA-02 audit — backfill atomicity` and the next `[fill — Task 2]` placeholder under it. Replace the placeholder with:

```markdown
**Requirement:** SCHEMA-02 — migration backfills `teams.sport = 'afl'` for every existing team row before any code path treats sport as non-null (DEFAULT then NOT NULL, or backfill-then-alter pattern).

**Audit verdict:** SCHEMA-02 is satisfied **as-shipped** by `0024_multi_sport.sql`. No additional backfill statement is needed.

**Evidence — `0024_multi_sport.sql` lines 25-27 (verbatim from `sed -n '25,27p'` on 2026-04-29):**

\```sql
alter table public.teams
  add column if not exists sport text not null default 'afl'
    check (sport in ('afl','netball'));
\```

**Postgres atomicity proof** (per RESEARCH.md §1 "ADD COLUMN ... NOT NULL DEFAULT 'value' semantics", verified against Postgres docs):

A single `ALTER TABLE ... ADD COLUMN ... NOT NULL DEFAULT 'afl' CHECK (...)` statement, when executed inside Supabase CLI's implicit per-migration-file transaction:

1. Stores the default expression `'afl'` on the column metadata.
2. Populates existing rows with that default value (since Postgres 11, this is a metadata-only constant-time operation for non-volatile defaults — no full table rewrite).
3. Enforces the NOT NULL constraint — passes because every row already has `'afl'`.
4. Adds the CHECK constraint `sport IN ('afl','netball')` — passes because every existing row has `'afl'`.

**All four steps occur within the implicit transaction wrapping the file.** Either the entire migration succeeds atomically or rolls back. **There is no observable window in which existing rows have `sport IS NULL`.**

**Phase 6 runtime confirmation (deferred per D-17):** After Phase 3 merge + Phase 5 test-green, Phase 6 applies the migration set against a Supabase prod-clone and runs `select count(*) from teams where sport is null` — must return 0. Captured in §6.
```

(The `\```sql` and closing `\```` are literal triple-backtick fences when written to the file.)

**Step D — Use the Edit tool to replace the §4 stub with this content.**

Locate the line `## §4 SCHEMA-04 audit — destructive-ops absence on existing AFL data` and the next `[fill — Task 2]` placeholder. Replace with:

```markdown
**Requirement:** SCHEMA-04 — existing AFL data (teams, players, games, game_events, game_availability, share_tokens) survives the migration intact and is queryable through the merged code without RLS or null errors.

**Decision lock:** D-16 — Phase 2 covers the **migration-content side only**. D-17 — "queryable through merged code" is deferred to Phase 6 prod-clone validation; captured in §6.

**Audit 1 — DROP/RENAME/REVOKE scan across the three new migrations (verbatim stdout from 2026-04-29):**

\```bash
$ grep -nE "DROP|RENAME|REVOKE" \
    ../multi-sport/supabase/migrations/0024_multi_sport.sql \
    ../multi-sport/supabase/migrations/0026_team_quarter_seconds.sql \
    ../multi-sport/supabase/migrations/0027_game_quarter_seconds.sql
[paste live stdout here — exactly 2 lines, both `drop constraint` in 0024_multi_sport.sql]
\```

**Match accounting** (matches the live grep output above):

| File | Line | Statement | Classification | Safety |
|------|------|-----------|----------------|--------|
| `0024_multi_sport.sql` | ~48 | `alter table public.teams drop constraint teams_age_group_check;` | Constraint relaxation | **SAFE** — relaxes AFL-only `U8..U17` whitelisting to allow netball age groups; existing AFL `age_group` text values (U8..U17) remain valid as plain strings (no narrow-enum enforcement after drop). Per CONTEXT D-16. |
| `0024_multi_sport.sql` | ~67 | `alter table public.game_events drop constraint game_events_type_check;` | Constraint widen-and-replace | **SAFE** — immediately re-created (lines ~75-92) with widened enum that includes `period_break_swap` plus all existing event types; no existing `game_events` row becomes invalid. |

**Zero matches** in `0026_team_quarter_seconds.sql` or `0027_game_quarter_seconds.sql`.
**Zero matches** of any `RENAME` or `REVOKE` across all three files.

**Audit 2 — destructive-ops scan across the three new migrations (verbatim stdout from 2026-04-29):**

\```bash
$ grep -E "DROP TABLE|DROP COLUMN|DROP POLICY|DROP TRIGGER|DROP FUNCTION" \
    ../multi-sport/supabase/migrations/0024_multi_sport.sql \
    ../multi-sport/supabase/migrations/0026_team_quarter_seconds.sql \
    ../multi-sport/supabase/migrations/0027_game_quarter_seconds.sql
$ echo $?
1
\```

**Verdict:** Zero `DROP TABLE`, zero `DROP COLUMN`, zero `DROP POLICY`, zero `DROP TRIGGER`, zero `DROP FUNCTION`. **SCHEMA-04 migration-content side is satisfied** — none of the three new migrations destroys data, columns, policies, triggers, or functions that existing AFL queries depend on.

**New columns added by these migrations:**

| File | Column | Type | NULL? | Existing rows after migration |
|------|--------|------|-------|-------------------------------|
| `0024_multi_sport.sql:25-27` | `teams.sport` | `text` | NOT NULL | Populated with `'afl'` via DEFAULT in same statement (atomic per §3) |
| `0026_team_quarter_seconds.sql:11-12` | `teams.quarter_length_seconds` | `integer` | NULL | Existing rows get `NULL` — semantically "use inherited age-group default" |
| `0027_game_quarter_seconds.sql:12-13` | `games.quarter_length_seconds` | `integer` | NULL | Existing rows get `NULL` — semantically "inherit team override or age-group default" |

**No NOT NULL constraint is ever enforced on existing rows that did not have a value provided.** SCHEMA-04 migration-content side is fully satisfied.

**Phase 6 deferred items (per D-17):** "Queryable through merged code without RLS or null errors" requires a running Postgres against the merged code. Captured in §6.
```

**Step E — Read-only invariant verification.**

Run:
```bash
git status --porcelain -- supabase/migrations/ src/ e2e/ scripts/ package.json
```
Output MUST be empty. If not → STOP, surface to user.
  </action>
  <acceptance_criteria>
    - `grep -c "_To be filled by Plan 03" .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md` returns exactly 2 (only §5 and §6 remain stubs).
    - `grep -c "_To be filled by Task 2" .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md` returns 0 (§3 and §4 are no longer stubs — alternate phrasing `\[fill — Task 2\]` also must not appear).
    - `grep -cE "\[fill — Task 2\]" .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md` returns 0.
    - `grep -c "SCHEMA-02" .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md` returns at least 1 (audit references the requirement ID).
    - `grep -c "SCHEMA-04" .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md` returns at least 1.
    - `grep -c "not null default 'afl'" .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md` returns at least 1 (verbatim 0024_multi_sport.sql:26 line captured).
    - `grep -c "drop constraint teams_age_group_check" .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md` returns at least 1 (audit 1 stdout pasted).
    - `grep -c "drop constraint game_events_type_check" .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md` returns at least 1.
    - `grep -c "DROP TABLE\|DROP COLUMN\|DROP POLICY\|DROP TRIGGER\|DROP FUNCTION" .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md` returns at least 1 (audit 2 command quoted).
    - Live re-run of `grep -E "DROP TABLE|DROP COLUMN|DROP POLICY|DROP TRIGGER|DROP FUNCTION" ../multi-sport/supabase/migrations/0024_multi_sport.sql ../multi-sport/supabase/migrations/0026_team_quarter_seconds.sql ../multi-sport/supabase/migrations/0027_game_quarter_seconds.sql` exits 1 with no output (the audit's underlying claim still holds).
    - `git status --porcelain -- supabase/migrations/ src/ e2e/ scripts/ package.json` returns empty.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -c '_To be filled by Plan 03' .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md)" -eq 2 ] &amp;&amp; [ "$(grep -cE '\[fill — Task 2\]' .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md)" -eq 0 ] &amp;&amp; grep -q "not null default 'afl'" .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md &amp;&amp; grep -q 'drop constraint teams_age_group_check' .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md &amp;&amp; grep -q 'drop constraint game_events_type_check' .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md &amp;&amp; ! grep -E "DROP TABLE|DROP COLUMN|DROP POLICY|DROP TRIGGER|DROP FUNCTION" ../multi-sport/supabase/migrations/0024_multi_sport.sql ../multi-sport/supabase/migrations/0026_team_quarter_seconds.sql ../multi-sport/supabase/migrations/0027_game_quarter_seconds.sql &amp;&amp; [ -z "$(git status --porcelain -- supabase/migrations/ src/ e2e/ scripts/ package.json)" ]</automated>
  </verify>
  <done>
    - §3 contains: SCHEMA-02 audit verdict (satisfied as-shipped), verbatim `0024_multi_sport.sql:25-27` excerpt, the four-step Postgres atomicity proof, and the Phase 6 deferral note.
    - §4 contains: SCHEMA-04 audit verdict (migration-content side satisfied), verbatim stdout from both audit grep commands, the match-accounting table for the two `drop constraint` lines, and the new-columns table.
    - §1, §2, §5, §6 are unchanged from Task 1's state.
    - Live re-run of audit 2 still exits 1 (zero matches) — the audit's claim has not silently become wrong between Task 1 and Task 2.
    - Read-only invariant held: `supabase/migrations/`, `src/`, `e2e/`, `scripts/`, `package.json` unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → planning | Audit claims in `02-SCHEMA-PLAN.md` are consumed by Phase 3 and Phase 6 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `02-SCHEMA-PLAN.md` audit claims | mitigate | Each audit claim quotes verbatim stdout from a deterministic, idempotent grep/sed/git-show command — Phase 3 / Phase 6 can re-run the same commands against the same refs and verify equivalence |
| T-02-01-02 | Information Disclosure | none | accept | No PII; no credentials; no service-role usage |
| T-02-01-03 | Tampering | `supabase/migrations/` on this branch | mitigate | Read-only invariant enforced via `git status --porcelain -- supabase/migrations/` check after every task; D-11 |

**Severity:** low. No new auth surface, no new RLS, no new IO, no service-role admin client usage. The only "write" is markdown to `.planning/`. Existing super-admin storageState pattern is not invoked. ASVS L1 not impacted.
</threat_model>

<verification>
**Plan-level verification — run these after both tasks complete:**

1. **Doc structure:**
   ```bash
   test -f .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md
   [ "$(grep -E '^## §' .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md | wc -l)" -eq 6 ]
   ```

2. **§§1-4 are populated, §§5-6 are stubs:**
   ```bash
   [ "$(grep -c '_To be filled by Plan 03' .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md)" -eq 2 ]
   ! grep -E '\[fill — Task [12]\]' .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md
   ```

3. **Hash equality recorded twice (decision header + two stdout lines):**
   ```bash
   [ "$(grep -c '1761d4042751b82e3cd284d73ff5c9110741ca7686ceb10dcba2a74fb4693051' .planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md)" -ge 3 ]
   ```

4. **Audit claims verifiable against the underlying source files:**
   ```bash
   # The two `drop constraint` lines remain present in 0024_multi_sport.sql:
   [ "$(grep -cE '^\s*alter table.*drop constraint' ../multi-sport/supabase/migrations/0024_multi_sport.sql)" -ge 2 ]
   # Zero destructive ops still hold:
   ! grep -E "DROP TABLE|DROP COLUMN|DROP POLICY|DROP TRIGGER|DROP FUNCTION" \
       ../multi-sport/supabase/migrations/0024_multi_sport.sql \
       ../multi-sport/supabase/migrations/0026_team_quarter_seconds.sql \
       ../multi-sport/supabase/migrations/0027_game_quarter_seconds.sql
   ```

5. **Read-only invariant held throughout the plan:**
   ```bash
   [ -z "$(git status --porcelain -- supabase/migrations/ src/ e2e/ scripts/ package.json)" ]
   ```
   The only file modified by this plan is `.planning/phases/02-schema-reconciliation/02-SCHEMA-PLAN.md`.
</verification>

<success_criteria>
- **SCHEMA-01 partially satisfied:** §1 hash verification + §2 file-ops table for Phase 3 are written. The actual rename/delete is Phase 3's job (D-11); Phase 2 owns the plan, not the action.
- **SCHEMA-02 satisfied (audit side):** §3 records `0024_multi_sport.sql:25-27` verbatim, the Postgres atomicity proof, and the Phase 6 deferral. No additional backfill statement is needed.
- **SCHEMA-04 satisfied (migration-content side per D-16):** §4 records both audit grep outputs verbatim, classifies the two `drop constraint` matches as safe, and confirms zero destructive ops on tables/columns/policies/triggers/functions. Phase 6 runtime side deferred per D-17.
- **D-10, D-11, D-16, D-18 honoured:** decisions cited in the doc, supabase/migrations/ unchanged on this branch, audit lives in `02-SCHEMA-PLAN.md` not in MERGE-NOTES.md.
- **Read-only invariant held:** `git status -- supabase/migrations/ src/ e2e/ scripts/ package.json` clean before AND after every task.
</success_criteria>

<output>
After both tasks complete, create `.planning/phases/02-schema-reconciliation/02-01-SUMMARY.md` recording:
- The two `git show ... | sha256sum` outputs from §1 (re-quoted, with timestamp).
- The §2 file-ops table summary: 1 DELETE (`main:0024_super_admin.sql`) + 4 KEEP entries.
- The §3 SCHEMA-02 verdict: satisfied as-shipped.
- The §4 SCHEMA-04 verdict: migration-content side satisfied, two safe constraint drops accounted for, zero destructive ops.
- Confirmation that `02-SCHEMA-PLAN.md` §§5-6 remain stubbed for Plan 03.
- Confirmation that `git status -- supabase/migrations/ src/ e2e/ scripts/ package.json` was clean throughout.
- Hand-off note for Plan 03: "§5 (e2e spec design) and §6 (Phase 6 handoff) await Plan 03 Task 2; the spec source file `e2e/tests/multi-sport-schema.spec.ts` is referenced — Plan 03 authors it and back-fills the doc."
</output>
