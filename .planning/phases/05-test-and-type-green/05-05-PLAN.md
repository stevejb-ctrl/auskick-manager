---
phase: 05-test-and-type-green
plan: 05
type: execute
wave: 5
subsystem: phase-close-out
tags: [gauntlet, evidence-aggregation, phase-6-handoff, TEST-01, TEST-02, TEST-03, TEST-04, TEST-05]
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - .planning/phases/05-test-and-type-green/05-EVIDENCE.md  # NEW close-out artefact
autonomous: false   # final phase close-out; mirrors Plan 04-07 pattern (also non-autonomous) — final human sign-off

# Dependency graph
requires:
  - phase: 05-01
    provides: "Kotara Koalas seed (TEST-05) — auditKotaraKoalas { present: true, gameCount: 5, playerCount: 9 }"
  - phase: 05-02
    provides: "e2e/helpers/admin-hydration.ts + 3 specs refactored (Side-finding #3 closed)"
  - phase: 05-03
    provides: "scripts/e2e-setup.mjs port-3000 probe (Side-finding #2 closed)"
  - phase: 05-04
    provides: "netball-actions revalidatePath + NetballLiveGame/QuarterBreak router.refresh; 3 page.reload workarounds removed (Phase 4 deferred items #1+#2 closed)"
  - phase: 04-07
    provides: "EVIDENCE.md template + per-NETBALL-N traceability + Phase 3 invariant re-verification pattern this plan reuses"
provides:
  - "05-EVIDENCE.md — close-out aggregate: full gauntlet outputs, per-TEST-N traceability, Phase 3 + Phase 4 invariant re-verification, source-tree drift bound, Phase 6 hand-off block"
  - "Phase 5 acceptance signal: TEST-01..05 ALL CLOSED with passing automated assertions"
  - "Phase 6 entry signal: gauntlet green is the prerequisite for Phase 6 'Preview deploy + manual validation'"
affects: [06-preview-deploy, 07-production-cutover]

# Requirements
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05]

user_setup: []

must_haves:
  truths:
    - "npm test (Vitest) exits 0 with ≥153 tests passing (TEST-01)"
    - "npm run e2e (Playwright) exits 0 with all specs green; ONLY skip is PROD-04 fixme (TEST-02)"
    - "npx tsc --noEmit exits 0 with no type errors (TEST-03)"
    - "npm run lint exits 0 with no ESLint errors (TEST-04)"
    - "auditKotaraKoalas returns { present: true, gameCount: 5, playerCount: 9 } on a fresh DB (TEST-05)"
    - "All 5 Phase 5 success criteria from ROADMAP.md are met"
    - "Phase 3 invariants re-verified: pre-merge tags frozen; D-26 LiveGame.tsx quarterMs hits = 5; D-27 liveGameStore.ts quarterMs hits = 4; ABSTRACT-01 outside src/lib/sports/ = 4; PROD-04 fixme count = 1"
    - "Phase 4 invariants re-verified: 5 netball e2e specs from Phase 4 still pass green; trackScoring prop chain intact"
    - "05-EVIDENCE.md exists in .planning/phases/05-test-and-type-green/ with the same 6-section structure as 04-EVIDENCE.md"
  artifacts:
    - path: ".planning/phases/05-test-and-type-green/05-EVIDENCE.md"
      provides: "Phase 5 close-out aggregate"
      min_lines: 200
  key_links:
    - from: "05-EVIDENCE.md §1"
      to: "Full gauntlet outputs (tsc + lint + vitest + e2e + per-spec re-runs)"
      via: "Captured logs at /tmp/05-{tsc,lint,vitest,e2e}.log"
      pattern: "Exit code"
    - from: "05-EVIDENCE.md §2"
      to: "TEST-01..05 traceability"
      via: "per-requirement table mapping each TEST-N to its passing assertion"
      pattern: "TEST-0\\d"
    - from: "05-EVIDENCE.md §3"
      to: "Phase 3 + Phase 4 invariant re-verification"
      via: "named-and-numbered inherited invariants table with PASS/FAIL per row"
      pattern: "UNCHANGED"

---

<objective>
Phase 5 close-out: run the full gauntlet, aggregate evidence into 05-EVIDENCE.md, re-verify all Phase 3 + Phase 4 invariants, and arm the Phase 6 entry signal.

Purpose: Phase 5 is the final automated-quality-gate phase before deployment work begins. Plans 05-01..05-04 land specific deferred items (Kotara seed, helper extraction, port probe, revalidatePath/router.refresh fixes); this plan proves they all integrate cleanly and don't drift Phase 3/4 invariants. After this plan, Phase 6 (Preview deploy + manual validation) can launch knowing the merged trunk has a fully green automated quality bar.

Output: `05-EVIDENCE.md` mirroring `04-EVIDENCE.md`'s six-section structure (gauntlet, requirement traceability, invariant re-verification, source-tree drift, Phase 6 hand-off, plan summary index). Final human sign-off via the checkpoint at end.

Locked per CONTEXT D-CONTEXT-quality-bar-carryover in 05-CONTEXT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-test-and-type-green/05-CONTEXT.md
@.planning/phases/04-netball-verification-on-merged-trunk/04-EVIDENCE.md
@.planning/phases/04-netball-verification-on-merged-trunk/04-07-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full gauntlet on a clean DB; capture per-suite logs</name>
  <files>(no files modified — verification only; logs to /tmp)</files>
  <action>
Run the four-suite gauntlet sequentially. Mirror Plan 04-07 §1's structure exactly so 05-EVIDENCE.md §1 can be filled in with consistent shape.

**Gauntlet sequence:**

```bash
# 0. Fresh DB so TEST-05 audit runs against a clean reset (Plan 05-01's seed must be present).
npm run db:reset 2>&1 | tee /tmp/05-dbreset.log
# Confirm: log shows the Kotara Koalas notice from supabase/seed.sql.

# 1. tsc
npx tsc --noEmit 2>&1 | tee /tmp/05-tsc.log
# Expect: exit 0, empty output.

# 2. lint
npm run lint 2>&1 | tee /tmp/05-lint.log
# Expect: exit 0; the same 3 pre-existing warnings as Phase 4 (LiveGame.tsx:810, FeatureSection.tsx:77, NetballLiveGame.tsx:489) — confirm count + line numbers UNCHANGED.

# 3. vitest
npm test --run 2>&1 | tee /tmp/05-vitest.log
# Expect: exit 0; ≥153 passing (Phase 4 had 169; should be 169 after Phase 5 since no source-fix in this milestone added new pure-function tests).

# 4. e2e (full suite, --workers=1 --reporter=line per Phase 4 pattern)
npm run e2e -- --workers=1 --reporter=line 2>&1 | tee /tmp/05-e2e.log
# Expect: 52 PASS / 0 FAIL / 1 SKIP (PROD-04 fixme only — Kotara-optional now PASSES).

# 5. PROD-01 per-spec re-run (AFL non-regression carry-forward).
npm run e2e -- e2e/tests/availability.spec.ts e2e/tests/injury-replacement.spec.ts e2e/tests/live-quarters.spec.ts e2e/tests/live-scoring.spec.ts e2e/tests/live-swaps.spec.ts --workers=1 --reporter=line 2>&1 | tee /tmp/05-prod01.log
# Expect: 9/9 PASS (1 setup + 8 worker tests; same as Phase 4 PROD-01).
```

**Capture exit codes for each suite:**
```bash
echo "TSC: $?"        # after each command
echo "LINT: $?"
echo "VITEST: $?"
echo "E2E: $?"
echo "PROD01: $?"
```

**If any suite fails:** STOP and debug. The failure is a real regression that 05-04 (or earlier) introduced. Fix in the relevant plan, then re-run the gauntlet from scratch. DO NOT carry forward partial green results.

**Per-spec breakdown:** From `/tmp/05-e2e.log`, extract the per-spec table (23 spec files, 53 tests expected). The Kotara-optional test in netball-quarter-break.spec.ts that was SKIP in Phase 4 must show PASS in Phase 5.
  </action>
  <verify>
    <automated>
      # All five logs exist and indicate green.
      test -s /tmp/05-tsc.log || echo "tsc log missing/non-empty unexpected"   # tsc log SHOULD be empty (0 bytes ideal)
      test -f /tmp/05-tsc.log

      test -s /tmp/05-lint.log
      grep -E "(✖|error|errors)" /tmp/05-lint.log
      # Expected: 0 occurrences of "error" (warnings only).

      grep -E "([0-9]+ passed|[0-9]+ failed)" /tmp/05-vitest.log
      # Expected: ≥153 passed, 0 failed.

      grep -E "([0-9]+ passed|[0-9]+ failed|[0-9]+ skipped)" /tmp/05-e2e.log
      # Expected: 52 passed, 0 failed, 1 skipped.

      grep -E "(passed|failed)" /tmp/05-prod01.log
      # Expected: 9 passed, 0 failed.
    </automated>
  </verify>
  <done>
    - Five logs captured at /tmp/05-{tsc,lint,vitest,e2e,prod01}.log.
    - tsc + lint + vitest + e2e + per-spec re-run ALL exit 0.
    - Vitest reports ≥153 passing.
    - e2e reports 52 PASS / 0 FAIL / 1 SKIP (PROD-04 fixme only).
    - PROD-01 per-spec re-run: 9/9 PASS.
    - Lint warnings UNCHANGED from Phase 4 baseline (same 3 lines).
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-verify Phase 3 + Phase 4 invariants</name>
  <files>(no files modified — invariant probes only)</files>
  <action>
Re-run every invariant check from Phase 4 plan 04-07 §3. Capture results for 05-EVIDENCE.md §3.

**Phase 3 invariants (carried from 04-EVIDENCE.md §3):**

```bash
# 1. pre-merge tags unmoved.
git rev-parse pre-merge/main
# Expected: e9073dd205bdd8eae8e7b66097e3b2275c4b5958

git rev-parse pre-merge/multi-sport
# Expected: e13e787cb8abe405c18aca73e66c7c928eb359d8

# 2. PROD-04 fixme present.
grep -c "test\.fixme" e2e/tests/playhq-import.spec.ts
# Expected: 1

# 3. D-26 quarterMs at LiveGame.tsx (AFL — must NOT change in Phase 5).
grep -v '^[[:space:]]*//' src/components/live/LiveGame.tsx | grep -c "quarterMs"
# Expected: 5

# 4. D-27 quarterMs at liveGameStore.ts (AFL).
grep -v '^[[:space:]]*//' src/lib/stores/liveGameStore.ts | grep -c "quarterMs"
# Expected: 4

# 5. ABSTRACT-01 — AFL conditionals outside src/lib/sports/.
# Per Phase 3 plan 03-06: 4 UI-presentation hits, pre-classified acceptable.
grep -rE "sport[^a-zA-Z_]*===?\s*['\"]afl['\"]" src/ --include="*.ts" --include="*.tsx" | grep -v "src/lib/sports/" | grep -v "^[[:space:]]*//" | wc -l
# Expected: 4 (or whatever Phase 4 EVIDENCE.md §3 documented as the baseline — verify match)
```

**Phase 4 invariants (newly added in Plan 04-07 EVIDENCE.md):**

```bash
# 6. trackScoring prop chain intact in NetballLiveGame.tsx (Phase 4 plan 04-04 wired this).
grep -c "trackScoring" src/components/netball/NetballLiveGame.tsx
# Expected: same count as Phase 4 close (capture this number from a quick git show on Phase 4's plan 04-04 commit; the count should be UNCHANGED).

# 7. The 5 Phase 4 netball specs still exist + are still in the active suite (no fixmes added).
ls e2e/tests/netball-walkthrough.spec.ts e2e/tests/netball-stats.spec.ts e2e/tests/netball-summary.spec.ts e2e/tests/netball-live-flow.spec.ts e2e/tests/netball-quarter-break.spec.ts
# All exist.
grep -c "test\.fixme" e2e/tests/netball-*.spec.ts
# Expected: 0 (no Phase 4 spec was fixme'd).
```

**Phase 5 NEW invariants (introduced this milestone — to bake in for future phases):**

```bash
# 8. Kotara Koalas seed presence (TEST-05).
node -e "
  require('dotenv').config({ path: '.env.test' });
  const { createClient } = require('@supabase/supabase-js');
  const admin = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);
  (async () => {
    const { data } = await admin.from('teams').select('id, sport, age_group').eq('id', '5ba1eb72-ee23-4b8e-9f9c-22a12fd0fc11').maybeSingle();
    console.log(data);
  })();
"
# Expected: { id: '5ba1eb72-...', sport: 'netball', age_group: 'go' }

# 9. revalidatePath count in netball-actions.ts (was N pre-Plan-05-04; now N+2).
grep -c "revalidatePath" src/app/\(app\)/teams/\[teamId\]/games/\[gameId\]/live/netball-actions.ts
# Expected: ≥5 (Plan 05-04 added at least 2 new calls).

# 10. router.refresh count in NetballLiveGame.tsx + NetballQuarterBreak.tsx.
grep -c "router\.refresh" src/components/netball/NetballLiveGame.tsx
# Expected: ≥3 (auto-hooter + manual end Q4 + start Q1).
grep -c "router\.refresh" src/components/netball/NetballQuarterBreak.tsx
# Expected: ≥1 (Start Qn handler).

# 11. page.reload Q-break workarounds removed.
grep -c "await page.reload()" e2e/tests/netball-live-flow.spec.ts
# Expected: 0
grep -c "await page.reload()" e2e/tests/netball-quarter-break.spec.ts
# Expected: 0

# 12. waitForAdminHydration helper exists + used in 3 places.
test -f e2e/helpers/admin-hydration.ts
grep -l "waitForAdminHydration" e2e/tests/settings.spec.ts e2e/tests/roster.spec.ts
# Both files should be listed.

# 13. scripts/e2e-setup.mjs has the port probe.
grep -q "probePort3000" scripts/e2e-setup.mjs
```

Capture every command's output for §3 of 05-EVIDENCE.md.
  </action>
  <verify>
    <automated>
      # All 13 invariant checks pass.
      # Each check has its own exit-code expectation in the action body;
      # this verify step is just confirming the SCRIPT itself ran end-to-end.
      git rev-parse pre-merge/main | grep -q "^e9073dd"
      git rev-parse pre-merge/multi-sport | grep -q "^e13e787c"
      [ "$(grep -c "test\.fixme" e2e/tests/playhq-import.spec.ts)" = "1" ]
      [ "$(grep -v '^[[:space:]]*//' src/components/live/LiveGame.tsx | grep -c "quarterMs")" = "5" ]
      [ "$(grep -v '^[[:space:]]*//' src/lib/stores/liveGameStore.ts | grep -c "quarterMs")" = "4" ]
    </automated>
  </verify>
  <done>
    - All 13 invariant checks pass (5 carried from Phase 3, 2 from Phase 4, 6 NEW from Phase 5 milestones).
    - Captured output ready for 05-EVIDENCE.md §3 table.
    - Any invariant that DOESN'T match expected value → flagged as a regression and fixed before proceeding to Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author 05-EVIDENCE.md aggregating all the above</name>
  <files>.planning/phases/05-test-and-type-green/05-EVIDENCE.md</files>
  <action>
Mirror 04-EVIDENCE.md's structure precisely. Six sections:

**§1 Full gauntlet output** — per-suite exit codes + Vitest tail + per-spec breakdown table for all 23 e2e specs + PROD-01 per-spec table. Use the captured logs from Task 1.

**§2 TEST-01..05 traceability** — per-requirement table mapping each TEST-N to its passing assertion:

| Requirement | Acceptance gate | Spec/command | Status |
|-------------|------------------|--------------|--------|
| **TEST-01** | npm test ≥153 passing | npm test --run | **PASS** (169/169 from /tmp/05-vitest.log) |
| **TEST-02** | npm run e2e all green | npm run e2e --workers=1 | **PASS** (52 PASS + 1 SKIP — PROD-04 fixme only) |
| **TEST-03** | npx tsc --noEmit clean | npx tsc --noEmit | **PASS** (exit 0; empty log) |
| **TEST-04** | npm run lint clean | npm run lint | **PASS** (exit 0; 3 pre-existing warnings unchanged) |
| **TEST-05** | Kotara Koalas team seed | supabase/seed.sql + auditKotaraKoalas | **PASS** (audit returns { present: true, gameCount: 5, playerCount: 9 }) |

**§3 Phase 3 + Phase 4 invariants re-verified** — extend 04-EVIDENCE.md §3's 6-row table with 7 more rows for Phase 4 + Phase 5 invariants. Use the 13 invariants from Task 2.

**§4 Source-tree changes in Phase 5** — `git diff bd8761f..HEAD -- src/ scripts/ supabase/ e2e/` showing exactly the touched files:

Expected diff scope (assuming all plans landed cleanly):

```
 src/app/(app)/teams/[teamId]/games/[gameId]/live/netball-actions.ts  | ~30 +/-   (Plan 05-04)
 src/components/netball/NetballLiveGame.tsx                           | ~15 +/-   (Plan 05-04)
 src/components/netball/NetballQuarterBreak.tsx                       | ~6  +/-   (Plan 05-04)
 supabase/seed.sql                                                    | ~120+/-   (Plan 05-01)
 scripts/e2e-setup.mjs                                                | ~70+/-    (Plan 05-03)
 scripts/seed-kotara-koalas.mjs (only if Plan 05-01 fallback fired)   | NEW       (Plan 05-01 fallback)
 e2e/helpers/admin-hydration.ts                                       | NEW ~50   (Plan 05-02)
 e2e/tests/settings.spec.ts                                           | ~6 +/-    (Plan 05-02)
 e2e/tests/roster.spec.ts                                             | ~10+/-    (Plan 05-02)
 e2e/tests/game-edit.spec.ts                                          | ~6 +/-    (Plan 05-02)
 e2e/tests/netball-live-flow.spec.ts                                  | ~10+/-    (Plan 05-04 — workarounds removed)
 e2e/tests/netball-quarter-break.spec.ts                              | ~10+/-    (Plan 05-04 — workarounds removed)
```

Total Phase 5 footprint: ~10 files (depends on whether Plan 05-01 used Option A or fallback Option B).

**§5 Phase 6 hand-off** — final state table + what Phase 6 needs to know:

| Item | Status | Phase 6 action |
|---|---|---|
| TEST-01..05 | DONE (all 5 closed) | Aggregate evidence; no fix needed |
| Side-finding #1 (gitignore) | CLOSED in Phase 4 | None |
| Side-finding #2 (port probe) | CLOSED in 05-03 | None |
| Side-finding #3 (admin-hydration helper) | CLOSED in 05-02 | None |
| Phase 4 deferred #1 (revalidatePath gap) | CLOSED in 05-04 | None |
| Phase 4 deferred #2 (router.refresh gap) | CLOSED in 05-04 | None |
| ABSTRACT-01 CI guard | DEFERRED to backlog (per Phase 3 CONTEXT) | Phase 6 may pick up if scope allows |
| PROD-04 CI guard | DEFERRED to backlog | Same |
| Pause-event persistence bug | DEFERRED to future milestone | None |
| games.quarter_length_seconds UI exposure | DEFERRED to v2 | None |
| Performance benchmarking | Phase 6/7 owns | Phase 6 to perform on preview |

**Phase 6 scope (DEPLOY-01, DEPLOY-02):**
- Vercel preview deploy of merged trunk against a Supabase prod clone.
- Manual validation: real AFL game flow + real netball game flow + stats dashboards + summary card + share-link viewing.

**Phase 6 prerequisites met by Phase 5:**
- Source-tree green-bar locked (TEST-01..04).
- Kotara Koalas seed available for netball validation (TEST-05).
- All deferred items resolved.
- Phase 3 + Phase 4 invariants intact.

**§6 Plan summary index** — table mapping plan → SUMMARY commit hash → key deliverable:

| Plan | Subsystem | SUMMARY commit | Key deliverable |
|---|---|---|---|
| 05-01 | seed-data | (commit hash) | supabase/seed.sql Kotara Koalas block (TEST-05 closed) |
| 05-02 | e2e-helpers | (commit hash) | e2e/helpers/admin-hydration.ts + 3 specs refactored (Side-finding #3) |
| 05-03 | e2e-bootstrap-script | (commit hash) | scripts/e2e-setup.mjs port-3000 probe (Side-finding #2) |
| 05-04 | netball-server-actions | (commit hash) | revalidatePath + router.refresh + 3 reload workarounds removed (Phase 4 deferred #1+#2) |
| 05-05 | phase-close-out | (this commit) | Full gauntlet + 05-EVIDENCE.md + Phase 6 hand-off |

**File length target:** ≥200 lines, ≤400 lines (Phase 4 EVIDENCE.md was 333 — aim for similar shape).

**Style:** Match 04-EVIDENCE.md's tone exactly — precise log excerpts, exact exit codes, named line numbers, explicit invariant probes. No prose-heavy summarizing.
  </action>
  <verify>
    <automated>
      # File exists and is well-formed.
      test -f .planning/phases/05-test-and-type-green/05-EVIDENCE.md
      wc -l .planning/phases/05-test-and-type-green/05-EVIDENCE.md
      # Expected: ≥200 lines.

      # Six sections present.
      grep -c "^## §[1-6]" .planning/phases/05-test-and-type-green/05-EVIDENCE.md
      # Expected: 6

      # All 5 TEST-N requirements named.
      grep -c "TEST-0[1-5]" .planning/phases/05-test-and-type-green/05-EVIDENCE.md
      # Expected: ≥5 (each TEST-N appears at least once)

      # All key invariant SHAs/values present.
      grep -q "e9073dd" .planning/phases/05-test-and-type-green/05-EVIDENCE.md          # pre-merge/main
      grep -q "e13e787c" .planning/phases/05-test-and-type-green/05-EVIDENCE.md          # pre-merge/multi-sport
      grep -q "5ba1eb72" .planning/phases/05-test-and-type-green/05-EVIDENCE.md          # Kotara team UUID
    </automated>
  </verify>
  <done>
    - .planning/phases/05-test-and-type-green/05-EVIDENCE.md exists, ≥200 lines.
    - All 6 sections present matching 04-EVIDENCE.md's structure.
    - TEST-01..05 each mapped to a passing assertion.
    - 13 invariants in §3 with PASS/UNCHANGED status per row.
    - §4 source-tree diff scope matches the actual git diff vs Phase 4 close (`bd8761f`).
    - §5 Phase 6 hand-off table populated; all deferred-from-Phase-4 items marked CLOSED.
    - §6 plan summary index populated.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Phase 5 sign-off — Phase 6 entry signal</name>
  <what-built>
    Phase 5 close-out:
    - Full gauntlet green: tsc 0 / lint 0 / vitest ≥153/≥153 / e2e 52 PASS + 1 SKIP / PROD-01 9/9.
    - 5 TEST-N requirements closed (TEST-01..04 via gauntlet; TEST-05 via Kotara seed).
    - 3 deferred-from-Phase-4 items closed (Side-finding #2 port probe, Side-finding #3 admin-hydration helper, revalidatePath/router.refresh gaps).
    - Phase 3 + Phase 4 invariants re-verified intact.
    - 05-EVIDENCE.md authored at .planning/phases/05-test-and-type-green/05-EVIDENCE.md.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/05-test-and-type-green/05-EVIDENCE.md` and skim §1, §2, §3 — do all five TEST-N rows show PASS? Do all 13 invariants show UNCHANGED?
    2. Run the gauntlet ONE MORE TIME from a clean shell to confirm reproducibility:
       - `npm run db:reset && npx tsc --noEmit && npm run lint && npm test --run && npm run e2e -- --workers=1 --reporter=line`
       - Expect: same green outcomes as captured in §1.
    3. Confirm pre-merge tags untouched:
       - `git tag -l --format='%(refname:short) %(objectname)' pre-merge/main pre-merge/multi-sport`
       - Expect: e9073dd... + e13e787c...
    4. Confirm PROD-04 fixme present:
       - `grep -c "test.fixme" e2e/tests/playhq-import.spec.ts` → 1
    5. (Optional but valuable) Spin up `npm run dev` for ~2 minutes — confirm both AFL and netball game-create flows still navigate cleanly. This is a sanity check, not a gate.
  </how-to-verify>
  <resume-signal>Type "approved" if every check passes. Phase 5 is closed; Phase 6 entry signal armed. If any check fails, debug the relevant earlier plan (or this plan's evidence aggregation) before signing off.</resume-signal>
</task>

</tasks>

<verification>
- Full gauntlet (tsc + lint + vitest + e2e + per-spec re-run) ALL exit 0.
- 5 TEST-N requirements closed with passing automated assertions.
- 3 deferred-from-Phase-4 items closed.
- Phase 3 invariants UNCHANGED (pre-merge tags, PROD-04 fixme, D-26/D-27 quarterMs hits, ABSTRACT-01 outside-sports-lib count).
- Phase 4 invariants UNCHANGED (5 netball specs intact, trackScoring prop chain unchanged).
- 05-EVIDENCE.md ≥200 lines mirroring 04-EVIDENCE.md's six-section structure.
- Phase 6 entry signal armed; final human sign-off received.
</verification>

<success_criteria>
1. `.planning/phases/05-test-and-type-green/05-EVIDENCE.md` exists with all 6 sections populated.
2. TEST-01..05 all marked PASS in §2.
3. 13 invariants in §3 all marked UNCHANGED/PASS.
4. Source-tree §4 matches actual git diff scope.
5. Phase 6 hand-off §5 names every deferred item with status.
6. Plan summary §6 table complete.
7. Final human-verify checkpoint approved.
8. ROADMAP.md Phase 5 status updated to COMPLETE.
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-and-type-green/05-05-SUMMARY.md` documenting:
- Total Phase 5 footprint (commits per plan, files touched, LoC delta)
- TEST-01..05 final status
- Phase 6 prerequisites confirmed met
- Any FRAGILE-area surprises encountered across all 5 plans

Then update `.planning/ROADMAP.md` Phase 5 entry: status → ✓ Complete; date → close-out date; tick all 5 plan checkboxes.

Then update `.planning/STATE.md`:
- `progress.completed_phases: 5`
- `progress.completed_plans: 22` (17 + 5)
- `progress.percent: 86` ((5/7)*100, rounded)
- `last_activity: <today>`
- `stopped_at`: "Phase 5 complete — full gauntlet green, TEST-01..05 closed, Phase 6 entry signal armed"
</output>
